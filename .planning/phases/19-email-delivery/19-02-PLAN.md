---
phase: 19-email-delivery
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/notification-worker/src/templates/email/booking-cancellation.hbs
  - services/notification-worker/src/templates/email/layout.hbs
  - services/notification-worker/src/consumers/booking-consumer.ts
  - services/notification-worker/src/schedulers/reminder-scheduler.ts
autonomous: true

must_haves:
  truths:
    - "Booking cancellation emails render from a proper Handlebars template (not inline HTML)"
    - "Booking confirmation emails display the actual company name (e.g., 'Tým Kosmetický Salon Eva'), not 'ScheduleBox'"
    - "Booking reminder emails display the actual company name, not 'ScheduleBox'"
    - "layout.hbs footer does not contain a broken unsubscribe link placeholder"
  artifacts:
    - path: "services/notification-worker/src/templates/email/booking-cancellation.hbs"
      provides: "Czech cancellation email template with customer name, service, date, reason, company name"
      contains: "{{customer_name}}"
    - path: "services/notification-worker/src/templates/email/layout.hbs"
      provides: "Email layout without broken {{unsubscribe_url}} placeholder"
    - path: "services/notification-worker/src/consumers/booking-consumer.ts"
      provides: "Booking consumer with real company name lookup and cancellation template reference"
      contains: "renderTemplateFile('booking-cancellation'"
    - path: "services/notification-worker/src/schedulers/reminder-scheduler.ts"
      provides: "Reminder scheduler with real company name lookup from DB"
      contains: "companies.name"
  key_links:
    - from: "services/notification-worker/src/consumers/booking-consumer.ts"
      to: "services/notification-worker/src/templates/email/booking-cancellation.hbs"
      via: "renderTemplateFile('booking-cancellation', 'email', templateData)"
      pattern: "renderTemplateFile.*booking-cancellation"
    - from: "services/notification-worker/src/consumers/booking-consumer.ts"
      to: "companies DB table"
      via: "db.select({ name: companies.name }).from(companies).where(eq(companies.id, companyId))"
      pattern: "companies\\.name"
---

<objective>
Fix three code gaps in the notification worker: create the missing booking cancellation email template, replace hardcoded company names with real DB lookups, and remove the broken unsubscribe link placeholder from the shared email layout.

Purpose: Cancellation emails currently render as raw inline HTML fragments (not styled, not consistent with other templates). Two files hardcode 'ScheduleBox' as the company name — every tenant's customers receive emails saying "Tým ScheduleBox" instead of their actual company name. The layout.hbs footer contains `{{unsubscribe_url}}` which is never populated — broken links are flagged by spam filters and threaten the 9/10 mail-tester.com score. All three gaps must be fixed before email delivery goes live.

Output: `booking-cancellation.hbs` Handlebars template, updated `layout.hbs` with broken placeholder removed, updated `booking-consumer.ts` to use the template and fetch real company name, updated `reminder-scheduler.ts` to fetch real company name.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-email-delivery/19-RESEARCH.md
@services/notification-worker/src/templates/email/layout.hbs
@services/notification-worker/src/templates/email/booking-confirmation.hbs
@services/notification-worker/src/templates/email/booking-reminder.hbs
@services/notification-worker/src/consumers/booking-consumer.ts
@services/notification-worker/src/schedulers/reminder-scheduler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create booking cancellation template and fix layout unsubscribe placeholder</name>
  <files>
    services/notification-worker/src/templates/email/booking-cancellation.hbs
    services/notification-worker/src/templates/email/layout.hbs
  </files>
  <action>
    **Part A: Fix `layout.hbs` unsubscribe placeholder.**

    Read `services/notification-worker/src/templates/email/layout.hbs`. The footer contains:
    ```html
    <p><a href="{{unsubscribe_url}}" style="color: #666;">Odhlásit se z odběru</a></p>
    ```
    This `{{unsubscribe_url}}` is never populated for transactional booking emails — it renders as a broken link that spam filters flag. Replace the entire anchor line with a static note:
    ```html
    <p style="color: #999; font-size: 11px;">Tento e-mail byl odeslán automaticky. Jedná se o transakční zprávu.</p>
    ```
    This removes the broken link without harming any email flow. Transactional emails (confirmations, reminders, cancellations) do not need an unsubscribe link under Czech law.

    **Part B: Create `services/notification-worker/src/templates/email/booking-cancellation.hbs`.**

    Model it after `booking-confirmation.hbs` and `booking-reminder.hbs` to maintain visual consistency (same layout structure, same CSS classes, same Czech language). First read those two files to understand the exact layout pattern used — whether they reference layout.hbs via a partial (`{{> layout}}`) or embed the full HTML structure directly, and replicate that exact pattern.

    The template MUST accept these variables (matching what `handleBookingCancelled` passes in `templateData`):
    - `{{customer_name}}` — customer's full name
    - `{{service_name}}` — service name
    - `{{booking_date}}` — booking date (use `{{formatDate booking_date}}` helper — this helper already exists in template-renderer.ts)
    - `{{reason}}` — cancellation reason (optional — use `{{#if reason}}...{{/if}}` block)
    - `{{company_name}}` — company name (will be fetched from DB in Task 2)

    Content structure (all text in Czech):
    - Heading: "Rezervace zrušena"
    - Greeting: "Dobrý den {{customer_name}},"
    - Body paragraph: "Vaše rezervace byla zrušena."
    - Details box (same styling as other templates):
      - Služba: {{service_name}}
      - Datum: {{formatDate booking_date}}
      - Optional: {{#if reason}}<p><strong>Důvod zrušení:</strong> {{reason}}</p>{{/if}}
    - Closing paragraph: "Pokud si přejete rezervovat nový termín, navštivte prosím naše stránky."
    - Sign-off: "S pozdravem,<br>Tým {{company_name}}"

    Do NOT include a `{{cancel_url}}` in the cancellation template (the booking is already cancelled).
  </action>
  <verify>
    1. Read `layout.hbs` — confirm `{{unsubscribe_url}}` is gone, replaced with the static Czech note.
    2. Read `booking-cancellation.hbs` — confirm it contains all required Handlebars variables: `{{customer_name}}`, `{{service_name}}`, `{{formatDate booking_date}}`, `{{#if reason}}`, `{{company_name}}`.
    3. Confirm Czech text is present (look for "Rezervace zrušena" and "Dobrý den").
  </verify>
  <done>
    `layout.hbs` has no broken `{{unsubscribe_url}}` placeholder. `booking-cancellation.hbs` exists with correct Czech content, all required template variables, and matches the visual style of existing email templates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix hardcoded company name and wire cancellation template</name>
  <files>
    services/notification-worker/src/consumers/booking-consumer.ts
    services/notification-worker/src/schedulers/reminder-scheduler.ts
  </files>
  <action>
    **IMPORTANT — renderTemplateFile is synchronous:** Read `services/notification-worker/src/services/template-renderer.ts` first. The function signature is `export function renderTemplateFile(...): string` (synchronous, returns string). Do NOT use `await` when calling it. The existing code in reminder-scheduler.ts already calls it with `await` (harmless but unnecessary) — do not copy that pattern in new code. Write `html = renderTemplateFile(...)` without await.

    **booking-consumer.ts changes — handleBookingCreated function (line ~77):**

    1. Add `companies` to the existing import from `@schedulebox/database`:
    ```typescript
    import { db, bookings, companies } from '@schedulebox/database';
    ```
    2. Replace `const companyName = 'ScheduleBox'; // TODO: fetch from company settings` with:
    ```typescript
    const [company] = await db
      .select({ name: companies.name })
      .from(companies)
      .where(eq(companies.id, companyId))
      .limit(1);
    const companyName = company?.name || 'ScheduleBox';
    ```

    **booking-consumer.ts changes — handleBookingCancelled function (line ~303):**

    1. Add the same company name lookup after the booking fetch. `companyId` is already destructured from `event.data` at line 304 — use it directly:
    ```typescript
    const [company] = await db
      .select({ name: companies.name })
      .from(companies)
      .where(eq(companies.id, companyId))
      .limit(1);
    const companyName = company?.name || 'ScheduleBox';
    ```
    2. Add `company_name: companyName` to the `templateData` object (currently missing from cancellation templateData).
    3. Replace the inline HTML fallback in the `else` branch:
    ```typescript
    // BEFORE:
    html = `<p>Dobrý den ${customerName},</p><p>Vaše rezervace služby ${serviceName} byla zrušena.</p><p>Důvod: ${reason || 'Nebyl uveden'}</p>`;

    // AFTER:
    html = renderTemplateFile('booking-cancellation', 'email', templateData);
    ```
    Note: renderTemplateFile is synchronous — no `await` needed.

    **reminder-scheduler.ts changes (line ~179):**

    1. Verify `companyId` is available in the booking object: the `scanWindow` DB query already includes `companyId: bookings.companyId` in its select list (confirmed at line 113 of the file). No query change needed.
    2. Add `companies` to the existing import from `@schedulebox/database`. Check the current import — if it already includes companies, do NOT add a duplicate.
    3. Add `eq` to drizzle-orm imports if not already present (it is already imported at line 8 — do NOT duplicate).
    4. Replace `company_name: 'ScheduleBox', // TODO: fetch from company settings` with:
    ```typescript
    const [company] = await db
      .select({ name: companies.name })
      .from(companies)
      .where(eq(companies.id, booking.companyId))
      .limit(1);
    const companyName = company?.name || 'ScheduleBox';
    ```
    Then use `company_name: companyName` in templateData instead of the hardcoded string.

    IMPORTANT: Do NOT restructure the functions. Make minimal targeted changes — only replace the TODO lines and add the DB lookup + import.
  </action>
  <verify>
    1. Run TypeScript check: `cd /d/Project/ScheduleBox/services/notification-worker && npx tsc --noEmit 2>&1 | head -20`.
    2. Grep for TODO comments: `grep -n "TODO: fetch from company" services/notification-worker/src/consumers/booking-consumer.ts services/notification-worker/src/schedulers/reminder-scheduler.ts` — should return no results.
    3. Grep for cancellation template reference: `grep "booking-cancellation" services/notification-worker/src/consumers/booking-consumer.ts` — should show `renderTemplateFile('booking-cancellation'`.
    4. Confirm `booking.companyId` usage in reminder-scheduler.ts: `grep "booking.companyId" services/notification-worker/src/schedulers/reminder-scheduler.ts` — should appear in the new company lookup.
  </verify>
  <done>
    Both TODO comments are removed. Both files fetch company name from DB. Cancellation handler uses `renderTemplateFile('booking-cancellation')` (synchronous, no await) instead of inline HTML. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
1. `services/notification-worker/src/templates/email/booking-cancellation.hbs` exists
2. `grep "unsubscribe_url" services/notification-worker/src/templates/email/layout.hbs` returns empty (placeholder removed)
3. `grep "TODO: fetch from company" services/notification-worker/src/consumers/booking-consumer.ts` returns empty
4. `grep "TODO: fetch from company" services/notification-worker/src/schedulers/reminder-scheduler.ts` returns empty
5. `grep "booking-cancellation" services/notification-worker/src/consumers/booking-consumer.ts` shows renderTemplateFile call
6. TypeScript compiles clean for notification-worker
</verification>

<success_criteria>
- Booking cancellation emails use the proper `booking-cancellation.hbs` template (not inline HTML string)
- All booking-related emails (confirmation, cancellation, reminder) display the tenant's actual company name
- No hardcoded 'ScheduleBox' in template data — always fetched from DB with 'ScheduleBox' as fallback only
- layout.hbs has no broken {{unsubscribe_url}} placeholder — replaced with a static transactional note
- TypeScript compiles clean for notification-worker
</success_criteria>

<output>
After completion, create `.planning/phases/19-email-delivery/19-02-SUMMARY.md` following the summary template.
</output>
