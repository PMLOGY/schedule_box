---
phase: 36-marketing-glass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx
  - apps/web/app/[locale]/(marketing)/_components/social-proof.tsx
  - apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx
  - apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx
  - apps/web/app/[locale]/(marketing)/pricing/page.tsx
  - apps/web/app/[locale]/(marketing)/privacy/page.tsx
  - apps/web/app/[locale]/(marketing)/terms/page.tsx
autonomous: true

must_haves:
  truths:
    - "Feature grid cards use Card variant=glass on all 6 feature cards"
    - "Featured pricing tier (Pro) has glass-surface with ring-2 ring-primary/70"
    - "Non-featured pricing tiers (Free, Business) have glass-surface-subtle"
    - "All pricing CTA buttons remain solid with no glass treatment"
    - "Testimonial cards use Card variant=glass"
    - "Footer uses glass-surface-subtle replacing bg-muted"
    - "Privacy page article content is wrapped in a GlassPanel"
    - "Terms page article content is wrapped in a GlassPanel"
    - "Pricing page h1 uses gradient text (blue-to-indigo) for visual consistency"
  artifacts:
    - path: "apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx"
      provides: "Glass feature cards"
      contains: "variant=\"glass\""
    - path: "apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx"
      provides: "Glass pricing tiers with differentiated intensity"
      contains: "glass-surface"
    - path: "apps/web/app/[locale]/(marketing)/_components/social-proof.tsx"
      provides: "Glass testimonial cards"
      contains: "variant=\"glass\""
    - path: "apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx"
      provides: "Glass footer panel"
      contains: "glass-surface-subtle"
    - path: "apps/web/app/[locale]/(marketing)/privacy/page.tsx"
      provides: "Glass-wrapped privacy article"
      contains: "GlassPanel"
    - path: "apps/web/app/[locale]/(marketing)/terms/page.tsx"
      provides: "Glass-wrapped terms article"
      contains: "GlassPanel"
    - path: "apps/web/app/[locale]/(marketing)/pricing/page.tsx"
      provides: "Gradient text on pricing h1"
      contains: "bg-gradient-to-r"
  key_links:
    - from: "feature-grid.tsx"
      to: "Card component"
      via: "variant prop"
      pattern: "variant=\"glass\""
    - from: "pricing-table.tsx"
      to: "glass-plugin utilities"
      via: "className glass-surface"
      pattern: "glass-surface"
    - from: "privacy/page.tsx"
      to: "GlassPanel component"
      via: "import from @/components/glass/glass-panel"
      pattern: "GlassPanel"
    - from: "terms/page.tsx"
      to: "GlassPanel component"
      via: "import from @/components/glass/glass-panel"
      pattern: "GlassPanel"
---

<objective>
Apply glass treatment to all remaining marketing page components: feature grid cards, pricing tier cards with glass intensity differentiation, testimonial cards, footer panel, pricing page heading, and privacy/terms page content wrappers.

Purpose: Complete the marketing glass system by converting every card-level and panel-level surface to glass, creating the premium "frosted" look that justifies 2,990 CZK/month product pricing. Pricing tiers use glass intensity to visually differentiate featured vs non-featured plans.

Output: All marketing component surfaces use glass treatment from Phase 33/34 token and component system.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

@.planning/phases/34-component-glass-variants/34-01-SUMMARY.md
@.planning/phases/36-marketing-glass/36-RESEARCH.md

@apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx
@apps/web/app/[locale]/(marketing)/_components/social-proof.tsx
@apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx
@apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx
@apps/web/app/[locale]/(marketing)/pricing/page.tsx
@apps/web/app/[locale]/(marketing)/privacy/page.tsx
@apps/web/app/[locale]/(marketing)/terms/page.tsx
@apps/web/components/ui/card.tsx
@apps/web/components/glass/glass-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply glass to feature grid, testimonials, pricing cards, and pricing page heading</name>
  <files>
    apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx
    apps/web/app/[locale]/(marketing)/_components/social-proof.tsx
    apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx
    apps/web/app/[locale]/(marketing)/pricing/page.tsx
  </files>
  <action>
**feature-grid.tsx — Glass feature cards:**

Change each `<Card className="h-full">` to `<Card variant="glass" className="h-full">`. There are 6 feature cards rendered in a `.map()` loop. Only the Card component gets the variant prop — the `motion.div` wrappers remain unchanged (they animate opacity and y, not backdrop-filter). The `CardContent` className stays unchanged. The section's `bg-muted/50` background class should be removed since the gradient mesh (from layout) provides the background — glass cards on top of `bg-muted/50` would look dull. Change the section className from `overflow-hidden py-16 md:py-24 bg-muted/50` to `overflow-hidden py-16 md:py-24`. The heading text is fine as-is (no gradient text needed on feature section heading).

**social-proof.tsx — Glass testimonial cards:**

Change each `<Card key={testimonial.name}>` to `<Card variant="glass" key={testimonial.name}>`. There are 3 testimonial cards in a `.map()` loop. The `CardContent`, avatar circle (`bg-primary/10`), and star icons stay unchanged. Component remains a server component.

**pricing-table.tsx — Glass pricing tiers with intensity differentiation:**

Import `cn` from `@/lib/utils` (already imported? check — if not, add it).

Change the `<Card>` render for each pricing plan. The current className is:
```tsx
className={`relative flex flex-col ${plan.featured ? 'ring-2 ring-primary' : ''}`}
```

Replace with glass-differentiated classes:
```tsx
className={cn(
  'relative flex flex-col',
  plan.featured
    ? 'glass-surface ring-2 ring-primary/70'
    : 'glass-surface-subtle',
)}
```

This applies `glass-surface` (medium 16px blur) to the featured Pro tier with a ring for emphasis, and `glass-surface-subtle` (8px blur, lower opacity) to Free and Business tiers. The ring color changes from `ring-primary` to `ring-primary/70` for a slightly translucent ring that fits the glass aesthetic.

CRITICAL: Do NOT add `variant="glass"` to the Card — use className-based glass utilities directly instead. The `variant="glass"` CVA variant applies `glass-surface` unconditionally, and we need per-card differentiation (glass-surface vs glass-surface-subtle). The Card's default variant provides `bg-card border-border shadow-sm` which gets overridden by the glass utility classes via CSS cascade (glass plugin utilities come after Tailwind utilities in stylesheet order).

The CTA buttons MUST remain solid per locked Decision 16:
```tsx
<Button asChild variant={plan.featured ? 'default' : 'outline'} className="w-full">
```
This line stays EXACTLY as-is. Do NOT add glass variants to these buttons.

The featured badge `<Badge className="absolute -top-3 left-1/2 -translate-x-1/2">` stays unchanged — it uses the default badge variant (solid), which is correct for a pricing callout.

**pricing/page.tsx — Gradient text on pricing heading:**

Change the `<h1>` className from:
`text-center text-3xl font-bold tracking-tight md:text-4xl`
to:
`text-center text-3xl font-bold tracking-tight md:text-4xl bg-gradient-to-r from-blue-600 to-indigo-600 bg-clip-text text-transparent`

This adds the same gradient text treatment as the hero h1 for visual consistency across marketing pages.
  </action>
  <verify>
Run `npx tsc --noEmit` from `apps/web/` — zero type errors.
Verify `feature-grid.tsx` contains `variant="glass"` on Card.
Verify `feature-grid.tsx` does NOT contain `bg-muted/50` on the section.
Verify `social-proof.tsx` contains `variant="glass"` on Card.
Verify `pricing-table.tsx` contains both `glass-surface ring-2 ring-primary/70` (featured) and `glass-surface-subtle` (non-featured).
Verify `pricing-table.tsx` button variants are unchanged (`plan.featured ? 'default' : 'outline'`).
Verify `pricing/page.tsx` h1 contains `bg-gradient-to-r from-blue-600 to-indigo-600 bg-clip-text text-transparent`.
  </verify>
  <done>
All 6 feature cards use Card variant="glass". All 3 testimonial cards use Card variant="glass". Featured pricing tier (Pro) uses glass-surface with ring-2 ring-primary/70. Non-featured tiers (Free, Business) use glass-surface-subtle. All pricing CTA buttons remain solid. Feature section bg-muted/50 removed (gradient mesh provides background). Pricing page h1 uses gradient text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply glass to footer and wrap privacy/terms pages in GlassPanel</name>
  <files>
    apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx
    apps/web/app/[locale]/(marketing)/privacy/page.tsx
    apps/web/app/[locale]/(marketing)/terms/page.tsx
  </files>
  <action>
**marketing-footer.tsx — Glass footer panel:**

Change the `<footer>` className from:
`border-t bg-muted`
to:
`border-t border-glass glass-surface-subtle`

This replaces the solid `bg-muted` background with a subtle glass treatment. The footer has no interactive overlays (no dropdowns, popovers, or modals), so a glass stacking context is completely safe. The `border-glass` class replaces the default border with the glass-appropriate border token from Phase 33.

All content inside the footer stays unchanged — the grid layout, company info, links, and copyright section remain as-is.

**privacy/page.tsx — Glass wrapper for privacy article:**

Import `GlassPanel` from `@/components/glass/glass-panel`.

Wrap the article content in a GlassPanel. Change the structure from:
```tsx
<article className="mx-auto max-w-3xl px-4 py-16">
  <h1 ...>...</h1>
  <p ...>...</p>
  <div className="mt-8 space-y-8 text-base leading-7">...</div>
</article>
```

To:
```tsx
<div className="mx-auto max-w-3xl px-4 py-16">
  <GlassPanel intensity="subtle" className="p-8">
    <article>
      <h1 className="text-3xl font-bold tracking-tight">{t('title')}</h1>
      <p className="mt-2 text-sm text-muted-foreground">{t('lastUpdated')}: 1. 1. 2026</p>
      <div className="mt-8 space-y-8 text-base leading-7">
        {/* ... existing SECTIONS.map() loop unchanged ... */}
      </div>
    </article>
  </GlassPanel>
</div>
```

The `<article>` element moves inside the GlassPanel. The layout classes (`mx-auto max-w-3xl px-4 py-16`) move to a wrapping `<div>` so they control sizing outside the glass panel. The GlassPanel gets `intensity="subtle"` (8px blur) and `className="p-8"` for padding. All article content (h1, date text, sections loop) stays completely unchanged.

**terms/page.tsx — Glass wrapper for terms article:**

Same pattern as privacy. Import `GlassPanel` from `@/components/glass/glass-panel`.

Change from:
```tsx
<article className="mx-auto max-w-3xl px-4 py-16">
  <h1 ...>...</h1>
  <p ...>...</p>
  <div className="mt-8 space-y-8 text-base leading-7">...</div>
</article>
```

To:
```tsx
<div className="mx-auto max-w-3xl px-4 py-16">
  <GlassPanel intensity="subtle" className="p-8">
    <article>
      <h1 className="text-3xl font-bold tracking-tight">{t('title')}</h1>
      <p className="mt-2 text-sm text-muted-foreground">{t('lastUpdated')}: 1. 1. 2026</p>
      <div className="mt-8 space-y-8 text-base leading-7">
        {/* ... existing SECTIONS.map() loop unchanged, including the dataProtection Link ... */}
      </div>
    </article>
  </GlassPanel>
</div>
```

Same structure — outer div for layout spacing, GlassPanel for glass effect, article for semantic content. The existing `Link` in the dataProtection section stays unchanged.
  </action>
  <verify>
Run `npx tsc --noEmit` from `apps/web/` — zero type errors.
Verify `marketing-footer.tsx` contains `glass-surface-subtle` and does NOT contain `bg-muted`.
Verify `marketing-footer.tsx` contains `border-glass`.
Verify `privacy/page.tsx` imports `GlassPanel` and contains `intensity="subtle"`.
Verify `terms/page.tsx` imports `GlassPanel` and contains `intensity="subtle"`.
  </verify>
  <done>
Footer uses glass-surface-subtle replacing solid bg-muted background. Privacy page article wrapped in GlassPanel with subtle intensity. Terms page article wrapped in GlassPanel with subtle intensity. All content inside footer and legal pages unchanged. Glass stacking context safe (no overlays in footer or legal pages).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` in apps/web/ passes with zero errors
2. Feature grid Cards have `variant="glass"` and section lacks `bg-muted/50`
3. Testimonial Cards have `variant="glass"`
4. Featured pricing Card has `glass-surface ring-2 ring-primary/70` in className
5. Non-featured pricing Cards have `glass-surface-subtle` in className
6. Pricing CTA buttons remain `variant={plan.featured ? 'default' : 'outline'}` (solid, no glass)
7. Pricing page h1 has gradient text classes
8. Footer has `glass-surface-subtle` and `border-glass`, no `bg-muted`
9. Privacy page imports GlassPanel and wraps article content with `intensity="subtle"`
10. Terms page imports GlassPanel and wraps article content with `intensity="subtle"`
</verification>

<success_criteria>
- All 6 feature cards display as glass cards (variant="glass")
- All 3 testimonial cards display as glass cards (variant="glass")
- Featured pricing tier visually differentiated with glass-surface (medium blur) + ring
- Non-featured pricing tiers use glass-surface-subtle (subtle blur)
- All pricing CTA buttons are solid (no glass) per locked decision
- Footer is a subtle glass panel replacing the old solid bg-muted
- Privacy and terms pages show article content inside subtle glass panels
- Pricing page heading uses gradient text (blue-to-indigo)
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/36-marketing-glass/36-02-SUMMARY.md`
</output>
