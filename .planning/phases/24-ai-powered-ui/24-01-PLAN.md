---
phase: 24-ai-powered-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/ai/NoShowRiskBadge.tsx
  - apps/web/components/ai/NoShowRiskDetail.tsx
  - apps/web/app/[locale]/(dashboard)/bookings/page.tsx
  - apps/web/components/booking/BookingDetailPanel.tsx
  - apps/web/messages/cs.json
  - apps/web/messages/en.json
  - apps/web/messages/sk.json
autonomous: true
must_haves:
  truths:
    - "Every booking row in the management list shows a color-coded no-show risk badge (red >50%, yellow 30-50%, green <30%)"
    - "Booking detail page shows the no-show probability with an actionable label, not a raw decimal"
    - "When noShowProbability is null, the badge shows a neutral gray state, not an error"
    - "Risk badges use the stored noShowProbability from the Booking object, not individual AI service calls"
  artifacts:
    - path: "apps/web/components/ai/NoShowRiskBadge.tsx"
      provides: "Color-coded risk badge component for booking list rows"
      exports: ["NoShowRiskBadge"]
    - path: "apps/web/components/ai/NoShowRiskDetail.tsx"
      provides: "Risk detail section with probability, confidence, and actionable label"
      exports: ["NoShowRiskDetail"]
  key_links:
    - from: "apps/web/app/[locale]/(dashboard)/bookings/page.tsx"
      to: "apps/web/components/ai/NoShowRiskBadge.tsx"
      via: "import and render in table row"
      pattern: "NoShowRiskBadge"
    - from: "apps/web/components/booking/BookingDetailPanel.tsx"
      to: "apps/web/components/ai/NoShowRiskDetail.tsx"
      via: "import and render between metadata and actions"
      pattern: "NoShowRiskDetail"
    - from: "apps/web/components/ai/NoShowRiskBadge.tsx"
      to: "packages/shared/src/types/booking.ts"
      via: "uses Booking.noShowProbability field"
      pattern: "noShowProbability"
---

<objective>
Add no-show risk badges to the booking management list and probability display to the booking detail panel. Every booking row shows a color-coded badge (red/yellow/green) based on the stored noShowProbability, and the detail panel shows an actionable human-readable label instead of a raw decimal.

Purpose: Make the AI no-show prediction visible and actionable for business owners. This is the primary P1 feature that differentiates ScheduleBox from every Czech/Slovak competitor (Reservio, Bookio, Calendly -- none offer ML-based no-show predictions).

Output: 2 new components (NoShowRiskBadge, NoShowRiskDetail), 2 modified pages (bookings list, detail panel), i18n keys in all 3 locales
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ai-powered-ui/24-RESEARCH.md

@apps/web/app/[locale]/(dashboard)/bookings/page.tsx
@apps/web/components/booking/BookingDetailPanel.tsx
@apps/web/components/booking/BookingStatusBadge.tsx
@apps/web/components/ui/badge.tsx
@apps/web/components/ui/tooltip.tsx
@apps/web/lib/ai/types.ts
@packages/shared/src/types/booking.ts
@apps/web/hooks/use-bookings-query.ts
@apps/web/messages/cs.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NoShowRiskBadge and NoShowRiskDetail components</name>
  <files>
    apps/web/components/ai/NoShowRiskBadge.tsx
    apps/web/components/ai/NoShowRiskDetail.tsx
    apps/web/messages/cs.json
    apps/web/messages/en.json
    apps/web/messages/sk.json
  </files>
  <action>
    Create `apps/web/components/ai/NoShowRiskBadge.tsx`:
    - 'use client' component
    - Accept props: `{ probability: number | null }` -- the `noShowProbability` from the Booking object
    - Import `Badge` from `@/components/ui/badge` and `Tooltip`/`TooltipTrigger`/`TooltipContent`/`TooltipProvider` from `@/components/ui/tooltip`
    - Import `useTranslations` from `next-intl`
    - Compute risk level from probability:
      - `null` -> 'unknown' (gray badge, text "-")
      - `>= 0.50` -> 'high' (bg-red-100 text-red-800)
      - `>= 0.30` -> 'medium' (bg-amber-100 text-amber-800)
      - `< 0.30` -> 'low' (bg-green-100 text-green-800)
    - Wrap Badge in Tooltip showing exact percentage on hover: "Riziko: 47%"
    - Follow the exact same styling pattern as `BookingStatusBadge.tsx`: use `Badge` with `variant="outline"` and custom `bg-*` / `text-*` / `border-transparent` classes
    - Export as named export `NoShowRiskBadge`
    - Keep the component compact -- it renders inline in a table cell

    Create `apps/web/components/ai/NoShowRiskDetail.tsx`:
    - 'use client' component
    - Accept props: `{ probability: number | null; confidence?: number; fallback?: boolean }`
    - Import `useTranslations` from `next-intl`, `ShieldAlert` icon from `lucide-react`, `Separator` from `@/components/ui/separator`
    - Display a section (matching the existing BookingDetailPanel section pattern with icon + label + indented content):
      - Icon: `ShieldAlert` (lucide-react)
      - Title: "AI predikce nedostaveni" (from i18n `ai.riskDetail.title`)
      - If probability is null: show "Tato rezervace jeste nebyla vyhodnocena AI." in muted text
      - If probability is not null:
        - Show probability as percentage: "Pravdepodobnost: 47%" (not raw 0.47)
        - Show risk level with color indicator (small colored dot + text label matching NoShowRiskBadge colors)
        - Show actionable label based on risk level:
          - high (>= 0.50): "Vysoke riziko -- zvazte zaslani SMS pripominky" (bold, red-700 text)
          - medium (0.30-0.49): "Stredni riziko -- doporucujeme potvrdit ucast" (amber-700 text)
          - low (< 0.30): "Nizke riziko -- zadna akce neni potreba" (green-700 text)
        - If confidence is provided, show "Spolehlivost AI: 82%" in muted text
        - If `fallback === true`, show a subtle warning: "AI sluzba je docasne nedostupna. Zobrazena je vychozi hodnota." in muted text
    - Export as named export `NoShowRiskDetail`

    Add i18n keys to `apps/web/messages/cs.json`:
    - Under a new `"ai"` top-level key, add the following nested structure:
      ```
      "ai": {
        "riskBadge": {
          "high": "Vysoke riziko",
          "medium": "Stredni riziko",
          "low": "Nizke riziko",
          "unknown": "Neohodnoceno",
          "tooltipPrefix": "Riziko"
        },
        "riskDetail": {
          "title": "AI predikce nedostaveni",
          "probability": "Pravdepodobnost nedostaveni",
          "confidence": "Spolehlivost AI",
          "highAction": "Vysoke riziko -- zvazte zaslani SMS pripominky",
          "mediumAction": "Stredni riziko -- doporucujeme potvrdit ucast",
          "lowAction": "Nizke riziko -- zadna akce neni potreba",
          "fallbackNote": "AI sluzba je docasne nedostupna. Zobrazena je vychozi hodnota.",
          "notScored": "Tato rezervace jeste nebyla vyhodnocena AI."
        }
      }
      ```
    - Add equivalent English keys to `apps/web/messages/en.json` under `"ai"`:
      ```
      "ai": {
        "riskBadge": {
          "high": "High risk",
          "medium": "Medium risk",
          "low": "Low risk",
          "unknown": "Not scored",
          "tooltipPrefix": "Risk"
        },
        "riskDetail": {
          "title": "AI no-show prediction",
          "probability": "No-show probability",
          "confidence": "AI confidence",
          "highAction": "High risk -- consider sending an SMS reminder",
          "mediumAction": "Medium risk -- we recommend confirming attendance",
          "lowAction": "Low risk -- no action needed",
          "fallbackNote": "AI service is temporarily unavailable. Default value shown.",
          "notScored": "This booking has not been scored by AI yet."
        }
      }
      ```
    - Add equivalent Slovak keys to `apps/web/messages/sk.json` under `"ai"`:
      ```
      "ai": {
        "riskBadge": {
          "high": "Vysoke riziko",
          "medium": "Stredne riziko",
          "low": "Nizke riziko",
          "unknown": "Nehodnotene",
          "tooltipPrefix": "Riziko"
        },
        "riskDetail": {
          "title": "AI predikcia nedostavenia",
          "probability": "Pravdepodobnost nedostavenia",
          "confidence": "Spolahlivost AI",
          "highAction": "Vysoke riziko -- zvazite zaslanie SMS pripomienky",
          "mediumAction": "Stredne riziko -- odporucame potvrdit ucast",
          "lowAction": "Nizke riziko -- ziadna akcia nie je potrebna",
          "fallbackNote": "AI sluzba je docasne nedostupna. Zobrazena je predvolena hodnota.",
          "notScored": "Tato rezervacia este nebola vyhodnotena AI."
        }
      }
      ```
    - IMPORTANT: Read the existing JSON files first, parse them, add the new "ai" key, and write back. Do NOT overwrite existing content. The `"ai"` key should be added at the same level as `"common"`, `"auth"`, `"booking"`, `"dashboard"`, etc.
  </action>
  <verify>
    - `npx tsc --noEmit` in `apps/web` passes (TypeScript compiles)
    - `NoShowRiskBadge.tsx` exists and exports `NoShowRiskBadge`
    - `NoShowRiskDetail.tsx` exists and exports `NoShowRiskDetail`
    - All 3 message JSON files are valid JSON (no syntax errors)
    - All 3 message JSON files have the `ai.riskBadge` and `ai.riskDetail` keys
  </verify>
  <done>
    Two AI risk display components created. NoShowRiskBadge renders color-coded badges (red/yellow/green) based on probability thresholds. NoShowRiskDetail renders probability as percentage with actionable human-readable label. i18n keys added in Czech, English, and Slovak.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate NoShowRiskBadge into booking list and NoShowRiskDetail into booking detail panel</name>
  <files>
    apps/web/app/[locale]/(dashboard)/bookings/page.tsx
    apps/web/components/booking/BookingDetailPanel.tsx
  </files>
  <action>
    Modify `apps/web/app/[locale]/(dashboard)/bookings/page.tsx`:
    - Add import: `import { NoShowRiskBadge } from '@/components/ai/NoShowRiskBadge';`
    - Add a new column header after the "Status" column and before the "Price" column:
      ```tsx
      <TableHead>{t('columns.risk')}</TableHead>
      ```
    - In the table row map, add a new `<TableCell>` after the status badge cell and before the price cell:
      ```tsx
      <TableCell>
        <NoShowRiskBadge probability={booking.noShowProbability} />
      </TableCell>
      ```
    - Update the `colSpan` on the loading and empty state `<TableCell>` from `6` to `7` (since we added a column)
    - Add the i18n key `"columns.risk"` to the booking list translations. Add to all 3 locale files:
      - cs.json: `"risk": "Riziko"` under `booking.list.columns`
      - en.json: `"risk": "Risk"` under `booking.list.columns`
      - sk.json: `"risk": "Riziko"` under `booking.list.columns`
    - IMPORTANT: The `Booking` type from `@schedulebox/shared/types` already has `noShowProbability: number | null`. No type changes needed. Access it as `booking.noShowProbability` (it's already camelCase from the API response).

    Modify `apps/web/components/booking/BookingDetailPanel.tsx`:
    - Add import: `import { NoShowRiskDetail } from '@/components/ai/NoShowRiskDetail';`
    - Add a new section between the "Metadata" section and the "Action Buttons" section
    - The section should be:
      ```tsx
      <Separator />

      {/* AI No-Show Prediction */}
      <NoShowRiskDetail probability={booking.noShowProbability} />
      ```
    - Place it right before the `{/* Action Buttons */}` comment and `<div className="pt-4 space-y-2">` block
    - The `BookingDetail` interface in this file already has `noShowProbability: number | null` -- no changes needed to the interface
    - Do NOT pass `confidence` or `fallback` props here since the booking record only stores the probability value, not the full AI response metadata. The detail component handles null confidence gracefully.
  </action>
  <verify>
    - `npx tsc --noEmit` in `apps/web` passes
    - The bookings page has 7 table columns (dateTime, customer, service, employee, status, risk, price)
    - The booking detail panel renders `NoShowRiskDetail` before the action buttons
    - `colSpan` on loading/empty rows is 7
    - Grep for `NoShowRiskBadge` in bookings/page.tsx returns a match
    - Grep for `NoShowRiskDetail` in BookingDetailPanel.tsx returns a match
  </verify>
  <done>
    Booking list shows a color-coded no-show risk badge in every row. Booking detail panel shows no-show probability with an actionable label (e.g., "High risk -- consider SMS reminder"). Both use the stored noShowProbability from the booking record, not individual AI service calls.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd apps/web && npx tsc --noEmit`
2. Booking list page renders 7 columns including the Risk column
3. NoShowRiskBadge shows correct colors: red for >= 0.50, amber for 0.30-0.49, green for < 0.30, gray for null
4. BookingDetailPanel shows NoShowRiskDetail section with probability percentage and actionable label
5. All i18n keys exist in cs.json, en.json, and sk.json
6. No new API calls are made per booking row (uses existing `noShowProbability` from Booking object)
</verification>

<success_criteria>
- Every booking row in the management list shows a color-coded no-show risk badge (red >50%, yellow 30-50%, green <30%)
- Booking detail page shows the no-show probability with an actionable label ("High risk -- consider SMS reminder"), not a raw decimal
- When noShowProbability is null, displays a neutral gray state (not broken/errored)
- i18n keys present in all 3 locales (cs, en, sk)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-ai-powered-ui/24-01-SUMMARY.md`
</output>
