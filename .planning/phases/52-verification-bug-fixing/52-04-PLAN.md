---
phase: 52-verification-bug-fixing
plan: 04
type: execute
wave: 3
depends_on: [52-02, 52-03]
files_modified:
  - apps/web/app/[locale]/(admin)/**
  - apps/web/app/api/v1/admin/**
  - apps/web/app/[locale]/(dashboard)/marketplace/**
  - apps/web/app/[locale]/(marketing)/page.tsx
  - apps/web/components/admin/**
autonomous: true
requirements: [VER-04, VER-05, VER-08]

must_haves:
  truths:
    - "Admin can log in and access the admin dashboard"
    - "Impersonation works with a visible red banner and audit log entry"
    - "Feature flags can be toggled and take effect"
    - "Company suspend/unsuspend works and blocks login for suspended companies"
    - "Marketplace search returns results and firm detail page loads"
    - "Book Now from marketplace navigates to the correct public booking wizard"
    - "All P1/P2 bugs found in plans 01-03 are fixed and re-verified"
  artifacts:
    - path: "apps/web/app/[locale]/(admin)"
      provides: "Admin panel pages"
    - path: "apps/web/app/api/v1/admin"
      provides: "Admin API endpoints"
    - path: "apps/web/app/[locale]/(dashboard)/marketplace"
      provides: "Marketplace search and listing pages"
  key_links:
    - from: "apps/web/app/[locale]/(admin)"
      to: "/api/v1/admin"
      via: "admin API calls"
      pattern: "fetch.*admin"
    - from: "marketplace search"
      to: "/api/v1/marketplace"
      via: "search query"
      pattern: "fetch.*marketplace"
    - from: "firm detail Book Now"
      to: "/[locale]/[company_slug]"
      via: "navigation link"
      pattern: "href.*company_slug|Link.*booking"
---

<objective>
Verify the admin panel (impersonation, feature flags, suspend, broadcast, maintenance, metrics, audit log), marketplace (search, firm detail, Book Now), and perform a final bug sweep across all flows. Fix every P1/P2 bug.

Purpose: Admin tools are critical for platform operations and trust. Marketplace is the customer acquisition channel. The final bug sweep ensures no known issues remain before deployment.
Output: Working admin panel, functional marketplace, and zero known P1/P2 bugs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-verification-bug-fixing/52-01-SUMMARY.md
@.planning/phases/52-verification-bug-fixing/52-02-SUMMARY.md
@.planning/phases/52-verification-bug-fixing/52-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify admin panel — impersonation, feature flags, suspend, broadcast, maintenance, metrics, audit log</name>
  <files>apps/web/app/[locale]/(admin)/**, apps/web/app/api/v1/admin/**, apps/web/components/admin/**</files>
  <action>
1. Login as admin:
   ```
   curl -X POST http://localhost:3000/api/v1/auth/login \
     -H "Content-Type: application/json" \
     -d '{"email":"admin@schedulebox.cz","password":"password123"}' -c admin-cookies.txt
   ```
   Verify admin lands on /cs/admin (not /cs/dashboard). If redirect is wrong, fix the role-based routing logic.

2. Test each admin feature via API (then verify UI pages load):

   **Impersonation:**
   ```
   curl -X POST http://localhost:3000/api/v1/admin/impersonate \
     -H "Content-Type: application/json" \
     -b admin-cookies.txt \
     -d '{"userId":"target-user-uuid"}'
   ```
   Expected: 200, session switches to target user. Verify:
   - Red impersonation banner appears (check the ImpersonationBanner component renders)
   - Audit log entry created for the impersonation action
   - Session limited to 15 minutes
   Fix: Missing endpoint, banner not rendering, audit log not written

   **Feature Flags:**
   ```
   curl -s http://localhost:3000/api/v1/admin/feature-flags -b admin-cookies.txt
   curl -X PUT http://localhost:3000/api/v1/admin/feature-flags/{flag-id} \
     -H "Content-Type: application/json" \
     -b admin-cookies.txt \
     -d '{"enabled":true}'
   ```
   Expected: List flags, toggle one. Verify the flag state propagates (check Redis cache).
   Fix: Missing endpoints, Redis connection for flag storage, UI not listing flags

   **Company Suspend/Unsuspend:**
   ```
   curl -X POST http://localhost:3000/api/v1/admin/companies/{id}/suspend \
     -H "Content-Type: application/json" \
     -b admin-cookies.txt \
     -d '{"reason":"Test suspension"}'
   ```
   Expected: 200, company marked suspended. Verify suspended company cannot login.
   Fix: Missing endpoint, suspension not checked during login, unsuspend not implemented

   **Broadcast:** Test the broadcast messaging endpoint exists and accepts a message.

   **Maintenance Mode:** Test enable/disable maintenance mode. Verify public pages show maintenance page when enabled.

   **Platform Metrics:** GET /api/v1/admin/metrics — should return company count, user count, booking count, revenue totals.

   **Audit Log:** GET /api/v1/admin/audit-log — should return recent admin actions.

3. For each feature that fails: read the route handler code, identify the issue, fix it. Common bugs:
   - Admin routes not protected by role check (anyone can access)
   - Admin API returns mock data instead of real DB queries
   - Impersonation banner component exists but is not included in the layout
   - Feature flag Redis key format mismatch between read and write
   - Metrics endpoint does SQL aggregation that fails on empty tables
  </action>
  <verify>
    <automated>curl -s -X POST http://localhost:3000/api/v1/auth/login -H "Content-Type: application/json" -d '{"email":"admin@schedulebox.cz","password":"password123"}' -w "\n%{http_code}" | tail -1</automated>
  </verify>
  <done>Admin login works and routes to admin dashboard. All 7 admin features (impersonation, feature flags, suspend/unsuspend, broadcast, maintenance mode, metrics, audit log) respond with real data. Admin UI pages load without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify marketplace search, firm detail page, and Book Now navigation</name>
  <files>apps/web/app/[locale]/(dashboard)/marketplace/**, apps/web/app/api/v1/marketplace/**, apps/web/app/[locale]/[company_slug]/**</files>
  <action>
1. Test marketplace search:
   ```
   curl -s "http://localhost:3000/api/v1/marketplace/search?q=salon"
   ```
   Or whatever the marketplace search endpoint is. Expected: Array of business listings with name, category, rating.
   If empty: Check marketplace_listings table has seed data. Check if companies are auto-listed or require explicit listing.
   Fix: Missing search endpoint, empty results, wrong table/query.

2. Test marketplace page loads:
   ```
   curl -s http://localhost:3000/cs/marketplace -o /dev/null -w "%{http_code}"
   ```
   If the marketplace is under (dashboard), it may require auth. Check if there is also a public marketplace route.
   Fix: Ensure marketplace is accessible publicly OR document that it requires login.

3. Test firm detail page:
   - Identify the firm detail URL pattern (likely /cs/marketplace/{slug} or /cs/{company_slug})
   ```
   curl -s http://localhost:3000/cs/{company-slug} -o /dev/null -w "%{http_code}"
   ```
   Expected: 200 with company details, services list, reviews.
   Fix: 404 (route not defined), 500 (SSR crash on missing data), empty content.

4. Verify "Book Now" navigation:
   - Read the firm detail page HTML and find the "Book Now" button/link
   - Verify it points to the correct public booking URL (the [company_slug] booking wizard)
   - The link should work for unauthenticated users
   Fix: Wrong href, link to non-existent page, link requires auth

5. Test search filtering:
   ```
   curl -s "http://localhost:3000/api/v1/marketplace/search?category=beauty&city=Praha"
   ```
   Expected: Filtered results. If filtering not implemented, note as P3 (not blocking).

6. Common bugs:
   - Marketplace page imports a component that crashes on server render
   - Search returns all companies regardless of query (missing WHERE clause)
   - Firm detail fetches company by ID but URL uses slug
   - Book Now href is relative and breaks depending on current route
   - Reviews section crashes when company has zero reviews
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/cs/marketplace -o /dev/null -w "%{http_code}"</automated>
  </verify>
  <done>Marketplace page loads and shows business listings. Search returns relevant results. Firm detail page loads with services and reviews. "Book Now" navigates to the correct public booking wizard URL.</done>
</task>

<task type="auto">
  <name>Task 3: Final bug sweep — re-verify all flows, fix remaining P1/P2 issues</name>
  <files>apps/web/**, packages/database/**</files>
  <action>
1. Create a verification checklist file at apps/web/VERIFICATION-LOG.md to track all bugs found and fixed across all plans. Compile bugs from plans 01-03 summaries.

2. Re-run the complete flow sequence to verify fixes did not introduce regressions:

   Flow A — Server boot:
   - `pnpm dev` starts clean (from Plan 01)
   - Homepage, login, register pages load

   Flow B — Owner setup:
   - Login as test@example.com / password123
   - Navigate to /cs/dashboard — should load with data
   - Navigate to /cs/dashboard/services — should list services
   - Navigate to /cs/dashboard/employees — should list employees

   Flow C — Customer booking:
   - Visit public booking URL /cs/{company-slug}
   - Verify services display
   - Verify slot availability returns times
   - Create a test booking via API

   Flow D — Admin:
   - Login as admin@schedulebox.cz / password123
   - Verify admin dashboard loads with metrics

   Flow E — Marketplace:
   - Visit /cs/marketplace
   - Verify listings display

   Flow F — Notifications:
   - Check notification log has entries from booking creation

3. For each flow step that fails:
   - Document the bug (route, error, expected vs actual)
   - Fix the root cause (not a workaround)
   - Re-test the specific step
   - Mark as fixed in VERIFICATION-LOG.md

4. Priority classification:
   - P1 (must fix): Flow completely blocked, data loss, security issue
   - P2 (must fix): Feature partially broken, bad UX, incorrect data
   - P3 (defer): Cosmetic, minor inconvenience, edge case
   Only P1 and P2 must be fixed. P3 can be documented for Phase 53 or later.

5. Final verification pass:
   - Run `pnpm build` to ensure no TypeScript compilation errors from fixes
   - Run `pnpm test` to ensure no existing tests broken
   - Run `pnpm lint` to catch any obvious code quality issues from fixes

6. Update VERIFICATION-LOG.md with final status: all P1/P2 resolved, any P3 items documented.
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 flows re-verified with no P1/P2 bugs remaining. pnpm build passes. pnpm test passes (or pre-existing failures documented). VERIFICATION-LOG.md documents all bugs found and their resolution status.</done>
</task>

</tasks>

<verification>
- Admin login routes correctly and dashboard shows real metrics
- All 7 admin features respond with expected behavior
- Marketplace search returns results
- Firm detail page loads with services and reviews
- Book Now navigates to correct booking wizard
- All flows from plans 01-03 re-verified without regressions
- pnpm build succeeds
- No P1 or P2 bugs remain open
</verification>

<success_criteria>
Admin panel fully functional with real data. Marketplace discoverable with working Book Now flow. All bugs from the entire verification phase are fixed and re-verified. The platform is safe to deploy with zero known P1/P2 issues.
</success_criteria>

<output>
After completion, create `.planning/phases/52-verification-bug-fixing/52-04-SUMMARY.md`
</output>
