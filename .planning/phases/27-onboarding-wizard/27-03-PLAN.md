---
phase: 27-onboarding-wizard
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - apps/web/app/api/v1/onboarding/demo-data/route.ts
  - apps/web/lib/onboarding/demo-data-seeder.ts
  - apps/web/components/onboarding/demo-data-card.tsx
  - apps/web/components/onboarding/driver-tour.tsx
  - apps/web/app/[locale]/(dashboard)/page.tsx
  - apps/web/messages/cs.json
  - apps/web/messages/en.json
  - apps/web/messages/sk.json
autonomous: true
must_haves:
  truths:
    - "Load demo data option seeds a realistic Czech business (Beauty Studio Praha, 3 services, 10 bookings, 5 customers) clearly labeled as demo data"
    - "Demo data can be removed with a single action"
    - "Driver.js contextual tooltips appear on first visit to each dashboard section and never repeat"
    - "Tooltips highlight key dashboard elements without being obtrusive"
  artifacts:
    - path: "apps/web/app/api/v1/onboarding/demo-data/route.ts"
      provides: "POST to seed demo data, DELETE to remove demo data"
      exports: ["POST", "DELETE"]
    - path: "apps/web/lib/onboarding/demo-data-seeder.ts"
      provides: "Demo data generation logic for Beauty Studio Praha"
      exports: ["seedDemoData", "removeDemoData"]
    - path: "apps/web/components/onboarding/demo-data-card.tsx"
      provides: "Dashboard card offering demo data load/remove"
      exports: ["DemoDataCard"]
    - path: "apps/web/components/onboarding/driver-tour.tsx"
      provides: "Driver.js tour wrapper for dashboard onboarding"
      exports: ["DashboardTour"]
  key_links:
    - from: "apps/web/app/api/v1/onboarding/demo-data/route.ts"
      to: "apps/web/lib/onboarding/demo-data-seeder.ts"
      via: "import seedDemoData, removeDemoData"
      pattern: "seedDemoData|removeDemoData"
    - from: "apps/web/components/onboarding/driver-tour.tsx"
      to: "driver.js"
      via: "import driver from driver.js"
      pattern: "import.*driver"
---

<objective>
Create the demo company data seeder and Driver.js contextual tooltips for first-time dashboard visitors. The demo data gives new owners a populated dashboard to explore, and tooltips highlight key features without requiring a full product tour.

Purpose: Demo data solves the "empty dashboard" problem — owners see what a working ScheduleBox looks like with realistic Czech business data. Contextual tooltips reduce the learning curve for each dashboard section independently.

Output: Demo data API endpoint + seeder logic + dashboard demo card + Driver.js tour component + i18n messages
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-onboarding-wizard/27-RESEARCH.md

@apps/web/app/api/v1/services/route.ts
@apps/web/app/api/v1/settings/company/route.ts
@apps/web/lib/middleware/route-handler.ts
@packages/database/src/schema/services.ts
@packages/database/src/schema/bookings.ts
@packages/database/src/schema/customers.ts
@packages/database/src/schema/auth.ts
@packages/database/src/seeds/development.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo data seeder and API endpoint</name>
  <files>
    apps/web/lib/onboarding/demo-data-seeder.ts
    apps/web/app/api/v1/onboarding/demo-data/route.ts
    apps/web/components/onboarding/demo-data-card.tsx
    apps/web/app/[locale]/(dashboard)/page.tsx
    apps/web/messages/cs.json
    apps/web/messages/en.json
    apps/web/messages/sk.json
  </files>
  <action>
    Create `apps/web/lib/onboarding/demo-data-seeder.ts`:
    - Export `seedDemoData(companyId: number): Promise<{ servicesCreated: number, customersCreated: number, bookingsCreated: number }>`
    - Export `removeDemoData(companyId: number): Promise<void>`
    - Export `hasDemoData(companyId: number): Promise<boolean>`

    **seedDemoData implementation:**
    - Import `db` from `@/lib/db/client` and schema tables from `@schedulebox/database`
    - All operations in a single transaction
    - Create 3 services for "Beauty Studio Praha" (use the beauty_salon template data):
      1. "Střih dámský" — 60 min, 500 CZK, buffer_after: 10 min, color: '#EC4899' (pink)
      2. "Barvení" — 120 min, 1200 CZK, buffer_after: 15 min, color: '#8B5CF6' (purple)
      3. "Manikúra" — 60 min, 400 CZK, buffer_after: 10 min, color: '#F59E0B' (amber)
    - All services: `companyId`, `onlineBookingEnabled: true`, `isActive: true`, `description` with Czech text
    - Create 5 customers with realistic Czech names:
      1. "Jana Novotná" — jana.novotna@email.cz, +420 601 234 567
      2. "Petra Svobodová" — petra.sv@email.cz, +420 602 345 678
      3. "Marie Dvořáková" — marie.dvorakova@email.cz, +420 603 456 789
      4. "Eva Černá" — eva.cerna@email.cz, +420 604 567 890
      5. "Lucie Procházková" — lucie.p@email.cz, +420 605 678 901
    - Create 10 bookings spread across the past 2 weeks and upcoming 1 week:
      - 5 completed bookings (in the past) — randomize across services and customers
      - 3 confirmed bookings (upcoming within next 7 days)
      - 1 cancelled booking (in the past)
      - 1 no-show booking (in the past)
    - Each booking: `companyId`, `serviceId` (from created services), `customerId` (from created customers), `startTime`, `endTime` (calculated from service duration), `status`, `totalPrice` (from service price)
    - Tag all demo records: update `company.settings` JSONB to include `{ demo_data: true, demo_data_ids: { service_ids: [...], customer_ids: [...], booking_ids: [...] } }`
    - Update the company `industryType` to 'beauty_salon' and `name` to include "(Demo)" suffix if it doesn't already

    **removeDemoData implementation:**
    - Read `company.settings.demo_data_ids` from the company record
    - Delete bookings by IDs from `demo_data_ids.booking_ids`
    - Delete customers by IDs from `demo_data_ids.customer_ids`
    - Delete services by IDs from `demo_data_ids.service_ids`
    - Clear `demo_data` and `demo_data_ids` from `company.settings` JSONB
    - All in a transaction

    **hasDemoData implementation:**
    - Read `company.settings` JSONB, return `settings.demo_data === true`

    Create `apps/web/app/api/v1/onboarding/demo-data/route.ts`:
    - Use `createRouteHandler` pattern (requires auth, owner permission)
    - **POST handler:** calls `seedDemoData(companyId)`, returns created counts
    - **DELETE handler:** calls `removeDemoData(companyId)`, returns success message
    - **GET handler:** calls `hasDemoData(companyId)`, returns `{ has_demo_data: boolean }`
    - All handlers: get companyId from `findCompanyId(user.sub)` — standard tenant scoping
    - POST should check if demo data already exists (via `hasDemoData`) and return 409 if so

    Create `apps/web/components/onboarding/demo-data-card.tsx`:
    - 'use client' directive
    - Card component shown on the dashboard (below checklist, above stats)
    - Two states:
      **State 1 — No demo data loaded:**
      - Heading: "Chcete vidět, jak ScheduleBox vypadá v akci?" / "Want to see ScheduleBox in action?"
      - Description: "Načtěte demo data s realistickým českým salonem krásy — 3 služby, 5 zákazníků, 10 rezervací" / "Load demo data with a realistic Czech beauty salon — 3 services, 5 customers, 10 bookings"
      - "Načíst demo data" button (primary) — calls POST /api/v1/onboarding/demo-data
      - Small info text: "Demo data jsou jasně označena a lze je kdykoliv odstranit" / "Demo data is clearly labeled and can be removed anytime"
      **State 2 — Demo data loaded:**
      - Info banner with yellow/amber background: "Demo data aktivní — Beauty Studio Praha" / "Demo data active — Beauty Studio Praha"
      - "Odstranit demo data" button (destructive variant) — calls DELETE /api/v1/onboarding/demo-data
      - Show confirmation dialog before deletion
    - Use React Query mutation for the POST/DELETE calls (optimistic update not needed — just invalidate queries on success)
    - On success of POST: invalidate all data queries (bookings, customers, services) to refresh dashboard
    - Use `useTranslations('onboarding.demoData')` for all text

    Update `apps/web/app/[locale]/(dashboard)/page.tsx`:
    - Import `DemoDataCard` component
    - Render it between OnboardingChecklist (from 27-02) and DashboardGrid
    - Only show when `onboarding_completed === true` (wizard is done) — new owners who just finished the wizard see it as a next option
    - Read the existing page first and add imports + conditional render without breaking existing layout

    Add i18n messages (append to locale files):
    - `onboarding.demoData.promptTitle`: "Chcete vidět ScheduleBox v akci?" / "Want to see ScheduleBox in action?" / "Chcete vidieť ScheduleBox v akcii?"
    - `onboarding.demoData.promptDescription`: "Načtěte demo data: Beauty Studio Praha — 3 služby, 5 zákazníků, 10 rezervací" / "Load demo data: Beauty Studio Praha — 3 services, 5 customers, 10 bookings" / "Načítajte demo dáta: Beauty Studio Praha — 3 služby, 5 zákazníkov, 10 rezervácií"
    - `onboarding.demoData.loadButton`: "Načíst demo data" / "Load demo data" / "Načítať demo dáta"
    - `onboarding.demoData.loadingButton`: "Načítání..." / "Loading..." / "Načítanie..."
    - `onboarding.demoData.info`: "Demo data jsou jasně označena a lze je kdykoliv odstranit" / "Demo data is clearly labeled and can be removed anytime" / "Demo dáta sú jasne označené a možno ich kedykoľvek odstrániť"
    - `onboarding.demoData.activeBanner`: "Demo data aktivní" / "Demo data active" / "Demo dáta aktívne"
    - `onboarding.demoData.activeCompany`: "Beauty Studio Praha" / "Beauty Studio Praha" / "Beauty Studio Praha"
    - `onboarding.demoData.removeButton`: "Odstranit demo data" / "Remove demo data" / "Odstrániť demo dáta"
    - `onboarding.demoData.removeConfirm`: "Opravdu chcete odstranit všechna demo data?" / "Are you sure you want to remove all demo data?" / "Naozaj chcete odstrániť všetky demo dáta?"
    - `onboarding.demoData.success`: "Demo data úspěšně načtena" / "Demo data loaded successfully" / "Demo dáta úspešne načítané"
    - `onboarding.demoData.removed`: "Demo data odstraněna" / "Demo data removed" / "Demo dáta odstránené"
  </action>
  <verify>
    - `cd apps/web && npx tsc --noEmit` passes
    - POST /api/v1/onboarding/demo-data creates 3 services, 5 customers, 10 bookings for the authenticated company
    - GET /api/v1/onboarding/demo-data returns `{ has_demo_data: true }` after seeding
    - DELETE /api/v1/onboarding/demo-data removes all demo records
    - Dashboard page shows DemoDataCard component
  </verify>
  <done>
    Demo data seeder creates Beauty Studio Praha with 3 services, 5 Czech customers, 10 bookings (mixed statuses) tagged as demo data. Dashboard card offers one-click load and removal. Demo data clearly labeled and removable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install Driver.js and create contextual tooltip tour</name>
  <files>
    apps/web/components/onboarding/driver-tour.tsx
    apps/web/app/[locale]/(dashboard)/layout.tsx
    apps/web/messages/cs.json
    apps/web/messages/en.json
    apps/web/messages/sk.json
  </files>
  <action>
    First, install driver.js:
    ```
    cd apps/web && pnpm add driver.js
    ```

    Create `apps/web/components/onboarding/driver-tour.tsx`:
    - 'use client' directive
    - Import `driver` from `driver.js` and `driver.js/dist/driver.css`
    - Export `DashboardTour` component
    - The tour is contextual: it runs ONCE on first visit to the dashboard after onboarding is complete
    - Detection: check localStorage key `sb_tour_completed_{companyUuid}`. If set, do not run.
    - On component mount (useEffect), check if tour should run:
      1. `onboarding_completed === true` (wizard done)
      2. `sb_tour_completed_*` not in localStorage
      3. User is on the main dashboard page
    - If all conditions met, initialize `driver()` with the following steps:
      Step 1: Highlight the sidebar navigation area
        - element: `nav[aria-label="Main navigation"]`
        - title: "Navigace" / "Navigation"
        - description: "Zde najdete všechny sekce — rezervace, zákazníky, služby, analýzu a nastavení" / "Find all sections here — bookings, customers, services, analytics, and settings"
        - position: 'right'
      Step 2: Highlight the stat cards area (DashboardGrid)
        - element: first `.grid` inside main content
        - title: "Přehled" / "Dashboard Overview"
        - description: "Vaše klíčové metriky — dnešní rezervace, měsíční tržby, noví zákazníci" / "Your key metrics — today's bookings, monthly revenue, new customers"
        - position: 'bottom'
      Step 3: Highlight the quick actions area
        - element: the QuickActions card
        - title: "Rychlé akce" / "Quick Actions"
        - description: "Rychle vytvořte rezervaci, přidejte zákazníka nebo otevřete kalendář" / "Quickly create a booking, add a customer, or open the calendar"
        - position: 'top'
    - Driver.js configuration:
      - `showProgress: true`
      - `showButtons: ['next', 'previous', 'close']`
      - `nextBtnText`: "Další" / "Next"
      - `prevBtnText`: "Zpět" / "Previous"
      - `doneBtnText`: "Hotovo" / "Done"
      - `closeBtnText`: "Zavřít" / "Close"
      - `animate: true`
      - `overlayColor`: 'rgba(0, 0, 0, 0.5)'
      - `popoverClass`: 'schedulebox-tour-popover'
    - On tour completion or close: set `sb_tour_completed_{companyUuid}` in localStorage
    - Add a small delay (500ms) before starting the tour to let the dashboard fully render
    - Use `useTranslations('onboarding.tour')` for all step text
    - The tour should use the current locale for button text

    Update `apps/web/app/[locale]/(dashboard)/layout.tsx`:
    - Read the existing file first
    - Import `DashboardTour` component
    - Add `<DashboardTour />` inside the layout, after the `<main>` content area (it renders nothing visually — just triggers the driver.js overlay)
    - Do NOT modify any existing layout structure — just add the tour component import and placement

    Add i18n messages (append to locale files):
    - `onboarding.tour.navigation.title`: "Navigace" / "Navigation" / "Navigácia"
    - `onboarding.tour.navigation.description`: "Zde najdete všechny sekce — rezervace, zákazníky, služby, analýzu a nastavení" / "Find all sections here — bookings, customers, services, analytics, and settings" / "Tu nájdete všetky sekcie — rezervácie, zákazníkov, služby, analýzu a nastavenia"
    - `onboarding.tour.overview.title`: "Přehled" / "Dashboard Overview" / "Prehľad"
    - `onboarding.tour.overview.description`: "Vaše klíčové metriky — dnešní rezervace, měsíční tržby, noví zákazníci" / "Your key metrics — today's bookings, monthly revenue, new customers" / "Vaše kľúčové metriky — dnešné rezervácie, mesačné tržby, noví zákazníci"
    - `onboarding.tour.quickActions.title`: "Rychlé akce" / "Quick Actions" / "Rýchle akcie"
    - `onboarding.tour.quickActions.description`: "Rychle vytvořte rezervaci, přidejte zákazníka nebo otevřete kalendář" / "Quickly create a booking, add a customer, or open the calendar" / "Rýchlo vytvorte rezerváciu, pridajte zákazníka alebo otvorte kalendár"
    - `onboarding.tour.nextBtn`: "Další" / "Next" / "Ďalej"
    - `onboarding.tour.prevBtn`: "Zpět" / "Previous" / "Späť"
    - `onboarding.tour.doneBtn`: "Hotovo" / "Done" / "Hotovo"
    - `onboarding.tour.closeBtn`: "Zavřít" / "Close" / "Zavrieť"
  </action>
  <verify>
    - `cd apps/web && npx tsc --noEmit` passes
    - `driver.js` is in apps/web/package.json dependencies
    - DashboardTour component exists and is imported in dashboard layout
    - Tour triggers once on first dashboard visit after onboarding, then never again
    - localStorage key `sb_tour_completed_*` is set after tour completes
  </verify>
  <done>
    Driver.js contextual tooltip tour highlights 3 key dashboard areas (navigation, stats overview, quick actions) on first visit after onboarding completion. Tour runs once and never repeats. All tooltip text in cs/en/sk.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd apps/web && npx tsc --noEmit`
2. POST /api/v1/onboarding/demo-data creates 3 services + 5 customers + 10 bookings
3. Dashboard shows demo data after loading; booking list, customer list, service list all populated
4. DELETE /api/v1/onboarding/demo-data removes all demo records
5. First visit to dashboard after onboarding triggers Driver.js tour (3 steps)
6. Second visit does not trigger tour (localStorage check)
7. All text displays in Czech when locale is 'cs'
</verification>

<success_criteria>
- "Load demo data" button on dashboard seeds Beauty Studio Praha with realistic Czech data
- Demo data is clearly labeled and removable with one click
- Driver.js tour runs exactly once on first dashboard visit post-onboarding
- Tour highlights navigation, stats, and quick actions with contextual descriptions
- All UI text available in cs/en/sk locales
</success_criteria>

<output>
After completion, create `.planning/phases/27-onboarding-wizard/27-03-SUMMARY.md`
</output>
