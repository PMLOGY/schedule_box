---
phase: 31-analytics-reporting
plan: 05
type: execute
wave: 2
depends_on: ["31-03"]
files_modified:
  - apps/web/app/[locale]/(dashboard)/analytics/admin/page.tsx
  - apps/web/app/[locale]/(dashboard)/analytics/organization/page.tsx
  - apps/web/components/analytics/admin-dashboard.tsx
  - apps/web/components/analytics/org-analytics-dashboard.tsx
  - apps/web/hooks/use-admin-analytics.ts
  - apps/web/hooks/use-org-analytics.ts
  - apps/web/app/api/v1/reports/customers/pdf/route.ts
  - apps/web/lib/export/pdf-templates/customer-report.tsx
  - apps/web/components/analytics/export-toolbar.tsx
  - apps/web/lib/navigation.ts
autonomous: true

must_haves:
  truths:
    - "Platform admin can view admin dashboard at /analytics/admin with MRR chart, churn rate, plan distribution, active companies, signup trends"
    - "Admin dashboard page is not accessible to regular business owners"
    - "Franchise owner can view organization analytics at /analytics/organization with per-location drill-down"
    - "Owner can export customer retention report as PDF or CSV from the analytics page"
    - "Navigation includes admin analytics link for admin users"
  artifacts:
    - path: "apps/web/app/[locale]/(dashboard)/analytics/admin/page.tsx"
      provides: "Platform admin SaaS health dashboard page"
      min_lines: 30
    - path: "apps/web/app/[locale]/(dashboard)/analytics/organization/page.tsx"
      provides: "Cross-location organization analytics page"
      min_lines: 30
    - path: "apps/web/components/analytics/admin-dashboard.tsx"
      provides: "Admin dashboard component with MRR, churn, plan distribution charts"
      min_lines: 50
    - path: "apps/web/components/analytics/org-analytics-dashboard.tsx"
      provides: "Organization analytics component with per-location breakdown"
      min_lines: 50
    - path: "apps/web/app/api/v1/reports/customers/pdf/route.ts"
      provides: "Customer retention report PDF export"
      exports: ["GET"]
    - path: "apps/web/components/analytics/export-toolbar.tsx"
      provides: "Extended export toolbar with customer report buttons"
  key_links:
    - from: "apps/web/hooks/use-admin-analytics.ts"
      to: "/api/v1/admin/analytics"
      via: "apiClient.get() to admin analytics endpoint"
      pattern: "apiClient.*admin/analytics"
    - from: "apps/web/hooks/use-org-analytics.ts"
      to: "/api/v1/analytics/organization"
      via: "apiClient.get() to organization analytics endpoint"
      pattern: "apiClient.*analytics/organization"
---

<objective>
Build the platform admin dashboard page, organization analytics page, and extend the export system with customer report PDF/CSV.

Purpose: Delivers ANLYT-05 (cross-location org analytics UI), ANLYT-06 (platform admin dashboard UI), and ANLYT-08 (customer report export). These are the final UI pieces that make all analytics data accessible to the three user roles: owner, franchise owner, and platform admin.

Output: Two new analytics sub-pages (admin + organization), two dashboard components, customer report PDF template, and extended export toolbar.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-analytics-reporting/31-RESEARCH.md

@apps/web/app/[locale]/(dashboard)/analytics/page.tsx
@apps/web/components/analytics/export-toolbar.tsx
@apps/web/lib/export/pdf-templates/revenue-report.tsx
@apps/web/lib/export/pdf-templates/pdf-config.ts
@apps/web/app/api/v1/reports/revenue/pdf/route.ts
@apps/web/lib/navigation.ts
@apps/web/hooks/use-analytics-query.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Platform admin dashboard and organization analytics pages</name>
  <files>
    apps/web/app/[locale]/(dashboard)/analytics/admin/page.tsx
    apps/web/app/[locale]/(dashboard)/analytics/organization/page.tsx
    apps/web/components/analytics/admin-dashboard.tsx
    apps/web/components/analytics/org-analytics-dashboard.tsx
    apps/web/hooks/use-admin-analytics.ts
    apps/web/hooks/use-org-analytics.ts
    apps/web/lib/navigation.ts
  </files>
  <action>
**1. Create use-admin-analytics.ts:**
- Hook: `useAdminAnalytics(days: number)` -> GET /admin/analytics?days=X
- useQuery with queryKey ['admin', 'analytics', days], staleTime 5 min
- TypeScript interface matching the admin API response (mrr, arr, churnRate, activeCompanies, totalCompanies, planDistribution, signupTrend, mrrByPlan)

**2. Create use-org-analytics.ts:**
- Hook: `useOrgAnalytics(days: number)` -> GET /analytics/organization?days=X
- useQuery with queryKey ['analytics', 'organization', days], staleTime 5 min
- TypeScript interface matching the org API response

**3. Create admin-dashboard.tsx (component):**
- 'use client' component
- Props: `data` from useAdminAnalytics hook
- Layout:
  - Top row: 4 KPI cards (MRR, ARR, Active Companies, Churn Rate) using Card/CardContent
  - MRR formatted as CZK with thousands separator
  - Churn rate as percentage with color (green < 5%, yellow 5-10%, red > 10%)
  - Second row: Plan Distribution — Recharts PieChart showing plan counts (Free=gray, Essential=blue, Growth=green, AI-Powered=purple)
  - Third row: Two-column grid:
    - Left: Signup Trend — Recharts LineChart with daily new signups
    - Right: MRR by Plan — Recharts BarChart showing MRR contribution per plan
- Use dynamic imports for charts (ssr: false)

**4. Create analytics/admin/page.tsx:**
- 'use client' page
- Guard: Check user role from auth store. If role !== 'admin', show an "Access Denied" card with message and redirect link. Import `useAuthStore` from '@/stores/auth.store'.
- If admin: render PageHeader("Platform Admin Dashboard") + PeriodSelector + AdminDashboard component
- Pass days state to useAdminAnalytics hook

**5. Create org-analytics-dashboard.tsx (component):**
- 'use client' component
- Props: `data` from useOrgAnalytics hook
- Layout:
  - Header: Organization name
  - Top row: 4 KPI cards (Total Revenue, Total Bookings, Completed, No-Shows) across all locations
  - Per-location breakdown: Table or card grid showing each location with its metrics
  - Each location card: company name, revenue, bookings, completed count, occupancy approximation (as progress bar)
- Use Tailwind grid layout (lg:grid-cols-2 or lg:grid-cols-3 for location cards)

**6. Create analytics/organization/page.tsx:**
- 'use client' page
- Uses useOrgAnalytics hook
- If error (403), show "Organization access required" message — this naturally happens for non-franchise users
- If loading, show skeleton grid
- If data, render OrgAnalyticsDashboard component

**7. Update lib/navigation.ts:**
- Add a new nav item for admin analytics (only visible to admin):
```typescript
{
  key: 'adminAnalytics',
  href: '/analytics/admin',
  icon: BarChart3,  // reuse existing icon
  roles: ['admin'],
}
```
- Place it after the existing 'analytics' entry
- NOTE: The organization analytics page is accessed from within the analytics page or via URL — it does not need its own top-level nav item. Franchise owners discover it through a link/tab on the analytics page.
  </action>
  <verify>
Run `npx tsc --noEmit` from apps/web — no type errors. Verify admin page guards against non-admin access. Verify navigation.ts includes adminAnalytics entry.
  </verify>
  <done>
Admin dashboard page renders MRR, churn, plan distribution, signup trends — guarded to admin-only. Organization analytics page renders cross-location breakdown for franchise owners. Navigation updated with admin analytics link.
  </done>
</task>

<task type="auto">
  <name>Task 2: Customer report PDF export and extended export toolbar</name>
  <files>
    apps/web/app/api/v1/reports/customers/pdf/route.ts
    apps/web/lib/export/pdf-templates/customer-report.tsx
    apps/web/components/analytics/export-toolbar.tsx
  </files>
  <action>
**1. Create customer-report.tsx PDF template:**
- Follow the exact pattern of revenue-report.tsx and booking-report.tsx
- Use @react-pdf/renderer Document, Page, View, Text, StyleSheet
- Props: `{ data: { repeatRate, totalCustomers, repeatCustomers, churned, atRisk, active, clvDistribution }, period, locale }`
- Sections:
  - Header: "Zprava o zakaznicich" (Customer Report) with period
  - Summary table: repeat booking rate, total customers, repeat customers
  - Churn breakdown: active / at risk / churned counts
  - CLV Distribution: simple text table with range and count columns
- Use registerFonts() pattern for Czech diacritics

**2. Create GET /api/v1/reports/customers/pdf:**
- Follow the exact pattern of reports/revenue/pdf/route.ts
- requiresAuth: true, requiredPermissions: [PERMISSIONS.CUSTOMERS_READ]
- Zod query: days (1-365, default 30), locale (cs/sk/en, default cs)
- Query customer retention data (same logic as analytics/customer-retention endpoint):
  - Repeat rate: customers with totalBookings > 1
  - Churn: lastVisitAt thresholds (90d at_risk, 180d churned)
  - CLV distribution: bucket clvPredicted values
- Call registerFonts(), renderToBuffer(CustomerReport({...}))
- Return PDF with Content-Type: application/pdf

**3. Update export-toolbar.tsx:**
- Add two new buttons to the existing toolbar:
  - "Zakaznici CSV" — downloads customer retention data as CSV
  - "Zakaznici PDF" — calls /api/v1/reports/customers/pdf
- For CSV: import `useCustomerRetention` from use-extended-analytics (or pass data as prop)
- Actually, to keep the component clean, add a new prop `customerRetentionData` to ExportToolbarProps and handle it like the existing revenueData/bookingData props
- CSV columns: Metrika (metric name), Hodnota (value) — simple key-value export of retention stats
- PDF: same fetchPdfWithAuth pattern as existing PDF buttons
- Add loading state for customer PDF download (isExportingCustomerPdf)
- Place new buttons at the end of the flex row
  </action>
  <verify>
Run `npx tsc --noEmit` from apps/web — no type errors. Verify customer-report.tsx uses @react-pdf/renderer. Verify export-toolbar.tsx has 6 buttons total (revenue CSV/PDF, bookings CSV/PDF, customer CSV/PDF).
  </verify>
  <done>
Customer report available as both PDF and CSV export from the analytics page. Export toolbar has 6 buttons covering revenue, bookings, and customer reports in both formats.
  </done>
</task>

</tasks>

<verification>
- /analytics/admin page shows MRR, churn, plan distribution, signup trends (admin only)
- /analytics/organization page shows cross-location breakdown (franchise owner only)
- Customer PDF export downloads correctly formatted Czech PDF
- Customer CSV export includes retention metrics
- Export toolbar shows all 6 export buttons
- Navigation shows admin analytics link for admin users only
- TypeScript compiles cleanly
</verification>

<success_criteria>
- Platform admin sees SaaS health metrics at /analytics/admin
- Non-admin users see "Access Denied" on admin page
- Franchise owners see per-location breakdown at /analytics/organization
- Revenue, bookings, and customer reports are all exportable as PDF and CSV
- Navigation correctly shows/hides admin analytics based on role
</success_criteria>

<output>
After completion, create `.planning/phases/31-analytics-reporting/31-05-SUMMARY.md`
</output>
