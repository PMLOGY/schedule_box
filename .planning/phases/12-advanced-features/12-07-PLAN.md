---
phase: 12-advanced-features
plan: 07
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - apps/web/public/widget/embed.js
  - apps/web/app/embed/[company_slug]/page.tsx
  - apps/web/app/embed/[company_slug]/layout.tsx
  - apps/web/app/api/v1/public/widget/config/[slug]/route.ts
  - apps/web/middleware.ts
autonomous: true
must_haves:
  truths:
    - "External website can embed ScheduleBox widget with a single script tag"
    - "Widget loads in sandboxed iframe for security isolation"
    - "Widget shows company services and redirects to full public booking page for booking completion"
    - "PostMessage API communicates service selection and resize events to parent page"
    - "Widget respects company branding (colors)"
    - "/embed routes are excluded from next-intl locale middleware"
  artifacts:
    - path: "apps/web/public/widget/embed.js"
      provides: "Widget loader script (Web Component + iframe)"
      min_lines: 40
    - path: "apps/web/app/embed/[company_slug]/page.tsx"
      provides: "Embedded booking widget React page"
      exports: ["default"]
    - path: "apps/web/app/embed/[company_slug]/layout.tsx"
      provides: "Minimal layout for embedded widget (no navigation)"
      exports: ["default"]
    - path: "apps/web/app/api/v1/public/widget/config/[slug]/route.ts"
      provides: "Widget configuration API (company branding, services)"
      exports: ["GET"]
  key_links:
    - from: "apps/web/public/widget/embed.js"
      to: "apps/web/app/embed/[company_slug]/page.tsx"
      via: "iframe src points to /embed/{company_slug}"
      pattern: "iframe.*embed"
    - from: "apps/web/app/embed/[company_slug]/page.tsx"
      to: "apps/web/app/api/v1/public/widget/config/[slug]/route.ts"
      via: "Fetch widget config for rendering"
      pattern: "widget/config"
    - from: "apps/web/app/embed/[company_slug]/page.tsx"
      to: "apps/web/app/[locale]/[company_slug]/page.tsx"
      via: "Service 'Book' button links to public booking page with service pre-selected"
      pattern: "company_slug.*serviceUuid"
    - from: "apps/web/middleware.ts"
      to: "apps/web/app/embed/[company_slug]/page.tsx"
      via: "Matcher excludes /embed from locale processing"
      pattern: "embed"
---

<objective>
Build the embeddable JavaScript booking widget that external websites can add with a single script tag. Uses Web Component wrapper with sandboxed iframe for security. Widget shows company services and links to the full public booking page for completing the booking flow.

Purpose: Enable businesses to embed a service catalog widget on their existing websites. Customers browse services in-widget and click through to the full public booking page for the complete booking experience.
Output: 1 static JS loader, 2 Next.js page files for the iframe content, 1 API route for widget config, 1 middleware update.
</objective>

<execution_context>
@./.claude/agents/gsd-executor.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-advanced-features/12-06-SUMMARY.md
@packages/database/src/schema/marketplace.ts
@apps/web/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widget loader script and configuration API</name>
  <files>
    apps/web/public/widget/embed.js
    apps/web/app/api/v1/public/widget/config/[slug]/route.ts
  </files>
  <action>
Create the widget loader script and configuration endpoint.

**apps/web/public/widget/embed.js:**
Plain JavaScript file (no build step, no React, no TypeScript - must work on any website). Served as static file from /widget/embed.js.

```javascript
// ScheduleBox Booking Widget Loader
// Usage: <script src="https://app.schedulebox.cz/widget/embed.js"></script>
//        <schedulebox-widget data-company="my-salon" data-theme="light"></schedulebox-widget>
```

- Define `ScheduleBoxWidget` as a custom element extending HTMLElement
- `connectedCallback()`:
  1. Read attributes: `data-company` (required), `data-theme` (optional, default 'light'), `data-locale` (optional, default 'cs'), `data-width` (optional, default '100%'), `data-height` (optional, default '600px')
  2. If `data-company` missing: show error message in element
  3. Create shadow DOM for style isolation
  4. Create iframe element:
     - src: `${window.location.protocol}//app.schedulebox.cz/embed/${companySlug}?theme=${theme}&locale=${locale}`
     - For development: use relative URL `/embed/${companySlug}?theme=${theme}&locale=${locale}` (detect via script src or data-base-url attribute)
     - style: width from data-width, min-height from data-height, border none, border-radius 8px
     - sandbox: 'allow-scripts allow-same-origin allow-forms allow-popups'
     - allow: 'payment' (for potential payment flow)
  5. Add PostMessage listener (origin-validated):
     - `SERVICE_SELECTED`: dispatch CustomEvent 'service-selected' with service detail
     - `RESIZE`: update iframe height to match content
     - `ERROR`: dispatch CustomEvent 'widget-error' with error detail
     - Validate event.origin matches expected app URL
  6. Append iframe to shadow DOM
- `disconnectedCallback()`: remove PostMessage listener
- Register: `customElements.define('schedulebox-widget', ScheduleBoxWidget)`
- Include inline CSS in shadow DOM for default styling (loading spinner while iframe loads)

Note: The widget does NOT handle booking completion in-iframe. When a user clicks "Book" on a service, the widget page opens the full public booking page (/{locale}/{company_slug}?service={uuid}) in a new tab. The PostMessage events are for service selection and resize coordination only.

**GET /api/v1/public/widget/config/[slug]:**
- No auth required (public endpoint)
- Find company by slug
- If not found: return 404
- Query marketplace listing for branding (colors, logo)
- Query active services (name, duration, price)
- Return config object: companyName, companySlug, logo, primaryColor, services (minimal: name, uuid, duration, price), locale defaults
- Set CORS headers: `Access-Control-Allow-Origin: *` (widget can be embedded anywhere)
- Set cache headers: `Cache-Control: public, max-age=300` (5 min cache for config)
  </action>
  <verify>Verify embed.js is valid JavaScript: `cd D:/Project/ScheduleBox && node -c apps/web/public/widget/embed.js`. Verify API route compiles: `npx tsc --noEmit -p apps/web/tsconfig.json`</verify>
  <done>Widget loader registers custom element, creates sandboxed iframe, handles PostMessage for resize and service selection. Config API returns company branding and services.</done>
</task>

<task type="auto">
  <name>Task 2: Embedded widget page and middleware exclusion</name>
  <files>
    apps/web/app/embed/[company_slug]/page.tsx
    apps/web/app/embed/[company_slug]/layout.tsx
    apps/web/middleware.ts
  </files>
  <action>
Create the React page that renders inside the widget iframe, and update middleware to exclude /embed routes from locale processing.

**CRITICAL: Update apps/web/middleware.ts FIRST.**
The current middleware matcher is `'/((?!api|_next|.*\\..*).*)' ` which WILL match `/embed/[company_slug]` and cause next-intl to try processing it, breaking the widget iframe. Add `embed` to the exclusion list in the matcher regex:

Update the matcher config to: `'/((?!api|_next|embed|.*\\..*).*)' `

This ensures /embed routes bypass the locale middleware entirely. The locale is passed as a query param to the widget page instead.

**layout.tsx:**
- Minimal layout with NO navigation, NO header, NO footer
- Just `<html><body>{children}</body></html>` with Tailwind CSS
- Include base styles and font loading
- Meta viewport for mobile responsiveness
- CSP meta tag: `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; frame-ancestors *;`
- Read `theme` from searchParams (light/dark) and apply appropriate Tailwind classes

**page.tsx:**
- Server component that fetches company data via direct Drizzle queries (same DB access as server component)
- Accept `company_slug` from params, `theme` and `locale` from searchParams
- If company not found: render "Company not found" message
- Render compact booking widget UI:
  1. **Company header**: Logo (if available), company name, average rating
  2. **Service list**: Scrollable list of active services with name, duration, price, "Book" button
  3. **"Book" button behavior**: Each service's "Book" button opens the full public booking page in a new tab at `/{locale}/{company_slug}?service={serviceUuid}`. Use `window.open()` in a client component. This is NOT in-widget booking - it redirects to the full booking page where the booking wizard handles the complete flow.
  4. PostMessage to parent: `window.parent.postMessage({ type: 'SERVICE_SELECTED', service: { uuid, name, price } }, '*')` when user clicks Book (before opening new tab)
- PostMessage to parent on key events:
  - `window.parent.postMessage({ type: 'RESIZE', height: document.body.scrollHeight }, '*')` after render
- Use client components where interactivity needed (service selection, PostMessage)
- Style with Tailwind CSS, compact spacing (this is an embedded widget, not a full page)
- Support `theme=dark` by toggling Tailwind dark mode classes

The widget page lives OUTSIDE the [locale] routing to avoid middleware interference. It's at `/embed/[company_slug]` not `/[locale]/embed/[company_slug]`. The locale is passed as a query param instead.
  </action>
  <verify>Run `cd D:/Project/ScheduleBox && npx tsc --noEmit -p apps/web/tsconfig.json` - no type errors. Verify middleware matcher includes 'embed' exclusion by reading the file: `grep -n "embed" apps/web/middleware.ts`</verify>
  <done>Middleware excludes /embed from locale processing. Widget page renders service list with "Book" buttons that open the full public booking page in a new tab. PostMessage communicates resize and service selection events.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p apps/web/tsconfig.json` passes
- embed.js is valid JavaScript (node -c check)
- Widget creates sandboxed iframe with correct src
- PostMessage listener validates origin
- Widget page renders without admin navigation
- CORS headers set on config API
- /embed path excluded from locale middleware (grep confirms 'embed' in matcher)
- "Book" button on services opens full public booking page at /{locale}/{company_slug}?service={uuid}
</verification>

<success_criteria>
- External website can embed widget with single script tag + custom element
- Widget shows company services in sandboxed iframe
- Middleware matcher excludes /embed routes: `'/((?!api|_next|embed|.*\\..*).*)' `
- "Book" button redirects to full public booking page for booking completion
- PostMessage API works for resize and service selection events
- Security: sandbox, CSP, origin validation all in place
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-features/12-07-SUMMARY.md`
</output>
