---
phase: 12-advanced-features
plan: 06
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - apps/web/app/[locale]/[company_slug]/page.tsx
  - apps/web/app/[locale]/[company_slug]/layout.tsx
  - apps/web/app/api/v1/public/company/[slug]/route.ts
  - apps/web/app/api/v1/public/company/[slug]/services/route.ts
  - apps/web/app/api/v1/public/company/[slug]/reviews/route.ts
autonomous: true
must_haves:
  truths:
    - "Public booking page renders for any company by slug without authentication"
    - "Page shows company branding (name, description, logo)"
    - "Page displays company services with prices"
    - "Page shows published reviews with average rating"
    - "SEO metadata is unique per company (title, description, Open Graph)"
    - "JSON-LD structured data (LocalBusiness) is embedded"
  artifacts:
    - path: "apps/web/app/[locale]/[company_slug]/page.tsx"
      provides: "Public booking page with SEO metadata"
      exports: ["default", "generateMetadata"]
    - path: "apps/web/app/[locale]/[company_slug]/layout.tsx"
      provides: "Public page layout (no admin sidebar)"
      exports: ["default"]
    - path: "apps/web/app/api/v1/public/company/[slug]/route.ts"
      provides: "Public company info by slug"
      exports: ["GET"]
    - path: "apps/web/app/api/v1/public/company/[slug]/services/route.ts"
      provides: "Public company services list"
      exports: ["GET"]
    - path: "apps/web/app/api/v1/public/company/[slug]/reviews/route.ts"
      provides: "Public company reviews"
      exports: ["GET"]
  key_links:
    - from: "apps/web/app/[locale]/[company_slug]/page.tsx"
      to: "apps/web/app/api/v1/public/company/[slug]/route.ts"
      via: "Server-side fetch for company data"
      pattern: "fetch.*public/company"
    - from: "apps/web/app/[locale]/[company_slug]/page.tsx"
      to: "packages/database/src/schema/marketplace.ts"
      via: "Company data for SEO and rendering"
      pattern: "generateMetadata"
---

<objective>
Build public booking pages with company branding, service listings, reviews, and SEO optimization. Each company gets a unique page at /{locale}/{company_slug}.

Purpose: Enable customers to discover and book from a company's branded page. SEO-optimized pages drive organic traffic. This is the primary customer-facing booking entry point.
Output: 2 Next.js page files + 3 public API routes.
</objective>

<execution_context>
@./.claude/agents/gsd-executor.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-advanced-features/12-02-SUMMARY.md
@.planning/phases/12-advanced-features/12-03-SUMMARY.md
@packages/database/src/schema/marketplace.ts
@packages/database/src/schema/reviews.ts
@packages/database/src/schema/auth.ts
@apps/web/app/api/v1/coupons/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public API endpoints for company data, services, and reviews</name>
  <files>
    apps/web/app/api/v1/public/company/[slug]/route.ts
    apps/web/app/api/v1/public/company/[slug]/services/route.ts
    apps/web/app/api/v1/public/company/[slug]/reviews/route.ts
  </files>
  <action>
Create 3 public (no auth) API endpoints for the public booking page data.

**GET /api/v1/public/company/[slug]:**
- No auth required (public endpoint)
- Find company by slug from `companies` table WHERE `slug = params.slug`
- If not found: return 404
- Find marketplace listing for this company (LEFT JOIN or separate query)
- Return company info: name, slug, description (from marketplace listing), address (from marketplace listing), averageRating, reviewCount, images, category, priceRange
- Do NOT expose companyId (SERIAL). Return company UUID if needed.
- Include settings.logoUrl, settings.primaryColor if available from company settings

**GET /api/v1/public/company/[slug]/services:**
- No auth required
- Find company by slug
- If not found: return 404
- Query services WHERE companyId matches AND isActive=true AND deletedAt IS NULL
- Return services: uuid, name, description, durationMinutes, price, currency, category (from service_categories), isOnline
- Order by category name, then service name

**GET /api/v1/public/company/[slug]/reviews:**
- No auth required
- Find company by slug
- If not found: return 404
- Query reviews WHERE companyId matches AND isPublished=true AND deletedAt IS NULL
- JOIN customers for first name (anonymize: show first name + last initial or "Verified customer")
- Return: uuid, rating, comment, reply, repliedAt, createdAt, customerName (anonymized)
- Order by createdAt DESC
- Pagination: page and limit query params
- Also return aggregate: averageRating, reviewCount, rating distribution (count per star 1-5)

All endpoints use `NextResponse.json()` directly (not createRouteHandler since these are public with no auth). Or use createRouteHandler with `requiresAuth: false` for consistency. Either approach works - prefer createRouteHandler for consistency with the codebase pattern.

Import `companies` from `@schedulebox/database` (from auth.ts schema). Import `services`, `reviews`, `marketplaceListings` as needed.
  </action>
  <verify>Run `cd D:/Project/ScheduleBox && npx tsc --noEmit -p apps/web/tsconfig.json` - no type errors</verify>
  <done>3 public API endpoints return company info, services list, and published reviews by company slug</done>
</task>

<task type="auto">
  <name>Task 2: Public booking page with SEO and company branding</name>
  <files>
    apps/web/app/[locale]/[company_slug]/page.tsx
    apps/web/app/[locale]/[company_slug]/layout.tsx
  </files>
  <action>
Create the public-facing company booking page with proper SEO metadata.

**layout.tsx:**
- Simple layout WITHOUT the admin sidebar/navigation
- Include basic header with ScheduleBox logo (links to /) and language switcher
- Children rendered in a clean, centered container (max-w-4xl mx-auto)
- Include footer with "Powered by ScheduleBox" link

**page.tsx:**
- Server component that fetches company data
- `generateMetadata` function for dynamic SEO:
  - title: `${company.name} - Online Rezervace | ${company.city || 'ScheduleBox'}`
  - description: `Rezervujte si termin online u ${company.name}. ${services.map(s => s.name).slice(0, 5).join(', ')}.`
  - openGraph: title, description, images (company logo or default), url
  - alternates: canonical URL `https://schedulebox.cz/${params.company_slug}`
- JSON-LD structured data embedded in page:
  ```json
  {
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "name": company.name,
    "address": { "@type": "PostalAddress", "streetAddress": ..., "addressLocality": ... },
    "aggregateRating": { "@type": "AggregateRating", "ratingValue": ..., "reviewCount": ... }
  }
  ```
- Page sections:
  1. **Hero**: Company name, description, average rating (star display), review count
  2. **Services**: Grid of service cards showing name, duration, price, "Book" button (links to booking wizard or placeholder)
  3. **Reviews**: Recent published reviews with star ratings, comments, and owner replies
  4. **Contact/Location**: Address from marketplace listing (if available)
- Fetch data via internal API calls or direct Drizzle queries (server component can use either)
  - Prefer direct Drizzle queries for server components (no HTTP overhead)
  - Import db from @schedulebox/database
- If company not found by slug: return notFound() from next/navigation
- Use Tailwind CSS classes consistent with design system (shadcn/ui components where applicable)
- Star rating display: use filled/empty star SVG icons or Unicode stars (no external dependency needed)

Handle the [locale] param: use next-intl for translated strings where applicable (button labels like "Rezervovat" / "Book"). The page content (company name, descriptions, reviews) is not translated - it's user-generated content.

IMPORTANT: Check existing [locale] route structure. The app may already have a `[locale]` layout. The new `[company_slug]` should be a dynamic segment WITHIN the existing [locale] structure. Verify there's no conflict with existing routes by checking if any existing pages might match the [company_slug] pattern.
  </action>
  <verify>Run `cd D:/Project/ScheduleBox && npx tsc --noEmit -p apps/web/tsconfig.json` - no type errors. Visually verify by checking page structure renders server-side.</verify>
  <done>Public booking page renders with company branding, services, reviews, and SEO metadata (unique title, description, JSON-LD per company)</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p apps/web/tsconfig.json` passes
- Public API endpoints return data without auth
- SEO metadata is unique per company (not generic ScheduleBox title)
- JSON-LD structured data is valid (LocalBusiness schema)
- Page shows 404 for non-existent company slugs
- Reviews are filtered to published only (no pending/rejected)
- Services are filtered to active only
</verification>

<success_criteria>
- 5 files created (3 API routes + 2 page files)
- Each company has unique SEO metadata
- Page renders company name, services, reviews without auth
- No SERIAL IDs exposed in public pages
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-features/12-06-SUMMARY.md`
</output>
