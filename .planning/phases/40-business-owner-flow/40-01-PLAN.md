---
phase: 40-business-owner-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/dashboard/booking-link-card.tsx
  - apps/web/components/dashboard/quick-actions.tsx
  - apps/web/app/[locale]/(dashboard)/dashboard/page.tsx
  - apps/web/hooks/use-services-query.ts
  - apps/web/app/[locale]/(dashboard)/services/page.tsx
  - apps/web/messages/en.json
  - apps/web/messages/cs.json
  - apps/web/messages/sk.json
autonomous: true
requirements:
  - OWNER-01
  - OWNER-02
  - OWNER-03

must_haves:
  truths:
    - "Owner can see and copy their public booking URL from the dashboard in one click"
    - "Owner can delete a service from the services page"
    - "Owner can create, edit, and deactivate employees with service assignments persisting"
  artifacts:
    - path: "apps/web/components/dashboard/booking-link-card.tsx"
      provides: "Public booking URL display and copy-to-clipboard component"
      min_lines: 40
    - path: "apps/web/hooks/use-services-query.ts"
      provides: "useDeleteService mutation hook"
      exports: ["useDeleteService"]
  key_links:
    - from: "apps/web/components/dashboard/booking-link-card.tsx"
      to: "apps/web/hooks/use-settings-query.ts"
      via: "useCompanySettingsQuery for slug"
      pattern: "useCompanySettingsQuery"
    - from: "apps/web/app/[locale]/(dashboard)/services/page.tsx"
      to: "apps/web/hooks/use-services-query.ts"
      via: "useDeleteService mutation"
      pattern: "useDeleteService"
---

<objective>
Add the public booking URL card to the dashboard and add delete functionality to services, completing the owner's ability to share their booking link and fully manage services.

Purpose: OWNER-01 (booking URL), OWNER-02 (service delete), OWNER-03 (employee CRUD — already works, needs verification)
Output: Booking link card component, delete service hook/UI, verified employee CRUD
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/hooks/use-settings-query.ts
@apps/web/hooks/use-services-query.ts
@apps/web/app/[locale]/(dashboard)/dashboard/page.tsx
@apps/web/app/[locale]/(dashboard)/services/page.tsx
@apps/web/app/[locale]/(dashboard)/employees/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/web/hooks/use-settings-query.ts:
```typescript
export interface CompanySettings {
  uuid: string;
  name: string;
  slug: string;  // <-- This is the company slug for the public booking URL
  // ... other fields
}
export function useCompanySettingsQuery(): UseQueryResult<CompanySettings>;
```

From apps/web/hooks/use-services-query.ts:
```typescript
export interface Service {
  id: number;
  uuid: string;
  name: string;
  // ... other fields
}
export function useServicesQuery(params?): UseQueryResult<Service[]>;
export function useCreateService(): UseMutationResult;
export function useUpdateService(): UseMutationResult;
// Missing: useDeleteService — needs to be added
```

API route exists: DELETE /api/v1/services/[id] (soft delete via deletedAt)
Public booking URL pattern: /{locale}/{company_slug}/booking
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public booking URL card and copy-to-clipboard on dashboard</name>
  <files>apps/web/components/dashboard/booking-link-card.tsx, apps/web/app/[locale]/(dashboard)/dashboard/page.tsx, apps/web/messages/en.json, apps/web/messages/cs.json, apps/web/messages/sk.json</files>
  <action>
Create a new BookingLinkCard component at apps/web/components/dashboard/booking-link-card.tsx:
- Use useCompanySettingsQuery() from @/hooks/use-settings-query to get the company slug
- Construct the public booking URL: `${window.location.origin}/${locale}/${slug}/booking` (use useLocale() from next-intl for locale)
- Display the URL in a read-only Input with a "Copy" Button next to it
- On copy click, use navigator.clipboard.writeText() and show a toast via sonner ("Link copied!")
- Toggle the button icon between Copy and Check (lucide-react) for 2 seconds after copy
- Use the existing Card component with variant="glass" for the container
- Add a heading like "Public Booking Link" with ExternalLink icon
- Show a loading skeleton while settings load, and a fallback message if slug is unavailable
- Style consistent with the existing dashboard cards (glass-surface, same padding/spacing)

Add the BookingLinkCard to the dashboard page (apps/web/app/[locale]/(dashboard)/dashboard/page.tsx):
- Import BookingLinkCard
- Place it right after the QuickActions row and before OnboardingChecklist, so it's prominent and visible
- Only show for non-employee users (the isEmployee check already exists in the page)

Add i18n translations to all three message files (en.json, cs.json, sk.json):
- dashboard.bookingLink.title: "Public Booking Link" / "Verejny odkaz na rezervaci" / "Verejny odkaz na rezervaciu"
- dashboard.bookingLink.description: "Share this link with your customers" / "Sdílejte tento odkaz se svými zákazníky" / "Zdieľajte tento odkaz so svojimi zákazníkmi"
- dashboard.bookingLink.copied: "Link copied!" / "Odkaz zkopírován!" / "Odkaz skopírovany!"
- dashboard.bookingLink.copy: "Copy" / "Kopírovat" / "Kopírovať"
- dashboard.bookingLink.noSlug: "Set up your company profile first" / "Nejprve nastavte profil firmy" / "Najprv nastavte profil firmy"

IMPORTANT: Do NOT use the Copy/Check icon swap from lucide-react via conditional state — use a simple useState with setTimeout pattern that's already used elsewhere in the codebase (see settings/page.tsx API key copy pattern for reference).
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>BookingLinkCard renders on dashboard with real company slug, copy button copies URL to clipboard, translations in all 3 languages</done>
</task>

<task type="auto">
  <name>Task 2: Add delete service hook and delete button to services page</name>
  <files>apps/web/hooks/use-services-query.ts, apps/web/app/[locale]/(dashboard)/services/page.tsx, apps/web/messages/en.json, apps/web/messages/cs.json, apps/web/messages/sk.json</files>
  <action>
Add useDeleteService hook to apps/web/hooks/use-services-query.ts:
```typescript
export function useDeleteService() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (uuid: string) => {
      return apiClient.delete(`/services/${uuid}`);
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['services'] });
    },
  });
}
```

Add a delete button to the edit service dialog in apps/web/app/[locale]/(dashboard)/services/page.tsx:
- Import useDeleteService, add deleteMutation = useDeleteService()
- Add a confirmation step: use a [deleteConfirmOpen, setDeleteConfirmOpen] state
- In the edit dialog footer (DialogFooter), add a Trash2 icon button on the left side (mr-auto pattern, same as employee edit dialog)
- Clicking the delete button opens a confirmation Dialog asking "Are you sure you want to delete this service?"
- On confirm, call deleteMutation.mutateAsync(editingService.uuid), close both dialogs on success, show toast
- The API does a soft delete (sets deletedAt), so the service disappears from the active list

Add i18n translations:
- services.deleteConfirmTitle: "Delete Service" / "Smazat službu" / "Zmazať službu"
- services.deleteConfirmMessage: "Are you sure? This action cannot be undone." / "Jste si jisti? Tuto akci nelze vrátit." / "Ste si istí? Túto akciu nemožno vrátiť."
- services.deleteSuccess: "Service deleted" / "Služba smazána" / "Služba zmazaná"
- services.deleteError: "Failed to delete service" / "Nepodařilo se smazat službu" / "Nepodarilo sa zmazať službu"

Also verify the existing employee CRUD (OWNER-03): The employees page already has create, edit with service assignments, deactivate (is_active toggle), and invite functionality. Confirm the hooks (useEmployeesQuery, useCreateEmployee, useUpdateEmployee, useAssignEmployeeServices, useInviteEmployee) are all properly wired to real API endpoints by checking that each calls the correct apiClient method. No code changes needed for OWNER-03 if everything is wired correctly.
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Delete service button appears in edit dialog, confirmation dialog works, service is soft-deleted via API, list refreshes after delete. Employee CRUD verified as functional.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with no errors
- BookingLinkCard appears on dashboard page for owner role
- Copy button copies the correct URL pattern (/{locale}/{slug}/booking)
- Delete button appears in service edit dialog with confirmation
- All translations present in en.json, cs.json, sk.json
</verification>

<success_criteria>
- Owner sees their public booking URL on the dashboard and can copy it in one click
- Owner can delete a service from the edit dialog with confirmation
- Employee CRUD remains functional (create, edit, deactivate, service assignments, invite)
</success_criteria>

<output>
After completion, create `.planning/phases/40-business-owner-flow/40-01-SUMMARY.md`
</output>
