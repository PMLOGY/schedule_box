---
phase: 40-business-owner-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/booking/BookingDetailPanel.tsx
  - apps/web/components/booking/BookingCalendar.tsx
  - apps/web/components/dashboard/dashboard-grid.tsx
  - apps/web/components/dashboard/recent-bookings.tsx
  - apps/web/components/dashboard/revenue-mini-chart.tsx
autonomous: true
requirements:
  - OWNER-04
  - OWNER-05
  - OWNER-06

must_haves:
  truths:
    - "Owner can see incoming bookings and use confirm, cancel, complete, and no-show actions"
    - "Dashboard calendar displays real bookings at correct times with correct employee names"
    - "All dashboard sub-pages load real data rather than empty states or mock data"
  artifacts:
    - path: "apps/web/components/booking/BookingDetailPanel.tsx"
      provides: "Booking action buttons (confirm, cancel, complete, no-show)"
      min_lines: 100
    - path: "apps/web/components/booking/BookingCalendar.tsx"
      provides: "Calendar with real bookings and employee names"
      min_lines: 80
  key_links:
    - from: "apps/web/components/booking/BookingDetailPanel.tsx"
      to: "/api/v1/bookings/[id]/confirm"
      via: "apiClient.post in actionMutation"
      pattern: "apiClient\\.post.*bookings.*confirm|cancel|complete|no-show"
    - from: "apps/web/components/booking/BookingCalendar.tsx"
      to: "/api/v1/bookings"
      via: "apiClient.get for date range"
      pattern: "apiClient\\.get.*bookings"
    - from: "apps/web/components/dashboard/dashboard-grid.tsx"
      to: "/api/v1/analytics"
      via: "useAnalyticsQuery hook"
      pattern: "useAnalyticsQuery"
---

<objective>
Verify and fix the booking management flow, calendar display, and dashboard data pages so that all owner-facing features show real data and action buttons work end-to-end.

Purpose: OWNER-04 (booking actions), OWNER-05 (calendar with real bookings), OWNER-06 (all dashboard pages use real data)
Output: Verified and fixed booking actions, calendar, and dashboard data
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/components/booking/BookingDetailPanel.tsx
@apps/web/components/booking/BookingCalendar.tsx
@apps/web/app/[locale]/(dashboard)/bookings/page.tsx
@apps/web/app/[locale]/(dashboard)/calendar/page.tsx
@apps/web/hooks/use-bookings-query.ts

<interfaces>
<!-- Key booking-related types -->

From @schedulebox/shared/types:
```typescript
type BookingStatus = 'pending' | 'confirmed' | 'completed' | 'cancelled' | 'no_show';

interface Booking {
  id: number;
  uuid: string;
  startTime: string;
  endTime: string;
  status: BookingStatus;
  customer: { id: string; name: string; email: string | null; phone: string | null };
  service: { id: string; name: string; durationMinutes: number; price: string };
  employee: { id: string; name: string } | null;
  price: string;
  currency: string;
  noShowProbability: number | null;
  // ... other fields
}
```

API action endpoints (all POST, all exist):
- /api/v1/bookings/[id]/confirm  (pending -> confirmed)
- /api/v1/bookings/[id]/cancel   (pending|confirmed -> cancelled)
- /api/v1/bookings/[id]/complete (confirmed -> completed)
- /api/v1/bookings/[id]/no-show  (confirmed -> no_show)

BookingDetailPanel already has action buttons wired via actionMutation.
BookingCalendar already fetches real bookings from the API.
Dashboard components (DashboardGrid, RevenueMiniChart, RecentBookings) use real analytics hooks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix booking actions and calendar data flow</name>
  <files>apps/web/components/booking/BookingDetailPanel.tsx, apps/web/components/booking/BookingCalendar.tsx, apps/web/hooks/use-bookings-query.ts</files>
  <action>
AUDIT PHASE - Read and verify each component's data flow:

1. BookingDetailPanel.tsx:
   - The actionMutation posts to `/bookings/${bookingId}/${action}` — verify the bookingId is the UUID (not numeric id). The useBookingDetail hook takes a string bookingId. Check if the bookingId passed from the bookings list page is booking.id (numeric) or booking.uuid. Looking at bookings/page.tsx line 170: `handleRowClick(String(booking.id))` — this passes the NUMERIC id as a string, but the API expects a UUID. This is a BUG. Fix: change `String(booking.id)` to `booking.uuid` in bookings/page.tsx.
   - Similarly check the BookingCalendar — it sets `id: String(booking.uuid)` in the event, and passes that to BookingDetailPanel. This is correct.
   - Verify the useBookingDetail hook calls `/bookings/${bookingId}` — this needs a UUID for the route handler's paramsSchema.

2. BookingCalendar.tsx:
   - Verify it fetches bookings for the displayed date range
   - Verify event titles show customer name and service name
   - Verify employee names appear (check the event rendering — the `resource` field is set to employee.id but the calendar may not display it in the title)
   - If employee name is not visible in the calendar event title, update the title format to include it: `${booking.customer?.name ?? ''} - ${booking.service?.name ?? ''} (${booking.employee?.name ?? ''})` in the useBookingsForCalendar hook

3. useBookingsQuery.ts:
   - The useBookingsForCalendar hook constructs events. Verify the title includes employee name
   - Check that the limit: 100 is sufficient for most use cases

FIX: In apps/web/app/[locale]/(dashboard)/bookings/page.tsx, change line ~170:
  `onClick={() => handleRowClick(String(booking.id))}`
  to
  `onClick={() => handleRowClick(booking.uuid)}`

Also check if the BookingDetailPanel uses bookingId to call useBookingDetail — if useBookingDetail calls `/bookings/${bookingId}`, it needs a UUID. The hook on line 109 does: `apiClient.get<Booking>(`/bookings/${bookingId}`)`. The API route handler at /api/v1/bookings/[id]/route.ts uses bookingIdParamSchema which expects a UUID. So passing a numeric ID would cause a 404 or validation error. This confirms the bug.

If the calendar event title does not include the employee name, update useBookingsForCalendar in use-bookings-query.ts to include it:
```typescript
title: `${booking.customer?.name ?? ''} - ${booking.service?.name ?? ''}${booking.employee?.name ? ` (${booking.employee.name})` : ''}`,
```
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Booking detail panel opens correctly when clicking a booking row (uses UUID not numeric ID), action buttons (confirm, cancel, complete, no-show) work end-to-end, calendar events show employee names</done>
</task>

<task type="auto">
  <name>Task 2: Verify all dashboard sub-pages load real data</name>
  <files>apps/web/components/dashboard/dashboard-grid.tsx, apps/web/components/dashboard/recent-bookings.tsx, apps/web/components/dashboard/revenue-mini-chart.tsx</files>
  <action>
AUDIT each dashboard sub-page to confirm it loads real data (not mock/hardcoded). For each page, verify the React Query hook calls a real API endpoint:

1. Dashboard main (apps/web/app/[locale]/(dashboard)/dashboard/page.tsx):
   - DashboardGrid: uses useAnalyticsQuery(30) — verify this hook calls a real API
   - RevenueMiniChart: check if it uses a real data hook or mock data
   - RecentBookings: check if it fetches from /bookings API or uses hardcoded data
   - QuickActions: static links, OK

2. Settings page: Uses useCompanySettingsQuery + useWorkingHoursQuery — both call real APIs. OK.

3. Customers page: Uses useCustomersQuery — calls /customers. OK.

4. Reviews page: Uses useReviewsQuery — calls /reviews. OK.

5. Payments page: Uses usePaymentsQuery — calls real API. OK.

6. Loyalty page: Uses useLoyaltyProgram, useTiers — calls real APIs. OK.

7. Analytics page: Uses useRevenueAnalytics, useBookingAnalytics, etc. — all real hooks. OK.

For any component that uses mock/hardcoded data:
- Replace with a real API call using the existing apiClient
- Create a simple React Query hook if one doesn't exist
- Wire the component to display the fetched data

Specifically check RecentBookings component — read it to see if it fetches real bookings or displays mock data. If mock, wire it to useBookingsQuery with limit: 5, sorted by most recent.

Also check RevenueMiniChart — read it to see if it uses real analytics data or is a placeholder chart with fake numbers.

If any component has empty/mock data issues, fix them. If all components already use real API hooks, document this finding and note that OWNER-06 is already satisfied.
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>All dashboard sub-pages (Settings, Payments, Customers, Reviews, Loyalty, Analytics) confirmed to load real data from API endpoints, no mock/hardcoded data remaining</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with no errors
- Booking detail panel correctly receives UUID for API calls
- Booking action buttons (confirm, cancel, complete, no-show) call correct API endpoints
- Calendar events include employee names in event titles
- All dashboard sub-pages use real React Query hooks that call actual API endpoints
</verification>

<success_criteria>
- Clicking a booking in the list opens the detail panel with correct data (not a 404)
- Each booking action (confirm, cancel, complete, no-show) changes the status immediately
- Calendar displays real bookings at correct times with employee names visible
- Every dashboard sub-page (Settings, Payments, Customers, Reviews, Loyalty, Analytics) shows real data from the database
</success_criteria>

<output>
After completion, create `.planning/phases/40-business-owner-flow/40-02-SUMMARY.md`
</output>
