---
phase: 07-notifications-automation
plan: 07
type: execute
wave: 4
depends_on: ["07-05", "07-06"]
files_modified:
  - docker/docker-compose.yml
  - .env.example
autonomous: false
must_haves:
  truths:
    - "Notification worker starts via Docker Compose and connects to RabbitMQ and Redis"
    - "Docker Compose includes all env vars needed for notification worker"
    - ".env.example documents all new notification-related environment variables"
    - "Booking creation event is consumed and confirmation email is enqueued"
  artifacts:
    - path: "docker/docker-compose.yml"
      provides: "notification-worker service definition in Docker Compose"
      contains: "notification-worker"
    - path: ".env.example"
      provides: "Documentation of all SMTP, Twilio, VAPID env vars"
      contains: "SMTP_HOST"
  key_links:
    - from: "docker/docker-compose.yml"
      to: "services/notification-worker/src/index.ts"
      via: "Docker Compose service running worker entrypoint"
      pattern: "notification-worker"
---

<objective>
Add notification worker to Docker Compose, update environment variable documentation, and verify the full notification pipeline end-to-end.

Purpose: The notification worker needs to run as a separate process from the Next.js app. Docker Compose integration ensures it starts alongside PostgreSQL, Redis, and RabbitMQ. The verification checkpoint confirms the entire pipeline works: event published -> consumer receives -> template rendered -> job enqueued -> email sent.

Output: Updated Docker Compose with notification-worker service, updated .env.example with all new env vars, and integration verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-notifications-automation/07-02-SUMMARY.md
@.planning/phases/07-notifications-automation/07-03-SUMMARY.md
@.planning/phases/07-notifications-automation/07-05-SUMMARY.md
@docker/docker-compose.yml
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker Compose integration and environment variable documentation</name>
  <files>
    docker/docker-compose.yml
    .env.example
  </files>
  <action>
**Update `docker/docker-compose.yml`:**

Add `notification-worker` service after the `app` service:

```yaml
  notification-worker:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: development
    container_name: schedulebox-notification-worker
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://schedulebox:schedulebox@postgres:5432/schedulebox
      - REDIS_URL=redis://redis:6379
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
      - SMTP_HOST=${SMTP_HOST:-localhost}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USER=${SMTP_USER:-}
      - SMTP_PASS=${SMTP_PASS:-}
      - SMTP_FROM=${SMTP_FROM:-noreply@schedulebox.cz}
      - TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID:-}
      - TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN:-}
      - TWILIO_FROM_NUMBER=${TWILIO_FROM_NUMBER:-}
      - VAPID_PUBLIC_KEY=${VAPID_PUBLIC_KEY:-}
      - VAPID_PRIVATE_KEY=${VAPID_PRIVATE_KEY:-}
      - NEXT_PUBLIC_APP_URL=http://localhost:3000
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    volumes:
      - ..:/app
      - /app/node_modules
    command: pnpm --filter @schedulebox/notification-worker start
    restart: unless-stopped
```

Key points:
- Same Dockerfile as app (development target has all workspace packages)
- Depends on all three infrastructure services (postgres, redis, rabbitmq)
- Uses `pnpm --filter @schedulebox/notification-worker start` which runs `tsx src/index.ts`
- All notification env vars use ${VAR:-default} syntax for optional development defaults
- SMTP/Twilio/VAPID vars default to empty (worker logs warnings when not configured per Plan 07-02)

**Update `.env.example`:**

Add a new section after existing env vars:

```bash
# ============================================================================
# Notifications (Phase 7)
# ============================================================================

# SMTP Email (required for email notifications)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=your-sendgrid-api-key
SMTP_FROM=noreply@schedulebox.cz

# Twilio SMS (required for SMS notifications)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_FROM_NUMBER=+420123456789

# Web Push VAPID Keys (required for push notifications)
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
```
  </action>
  <verify>
Run `docker compose -f docker/docker-compose.yml config` (dry-run) to validate YAML syntax. Verify notification-worker service is defined with correct depends_on. Verify .env.example contains all new env vars.
  </verify>
  <done>
Notification worker defined in Docker Compose with correct service dependencies. All new environment variables documented in .env.example with descriptions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete notification and automation system:
1. RabbitMQ event consumers (booking, payment, review)
2. BullMQ job queues for email/SMS/push delivery
3. Nodemailer email sender, Twilio SMS sender, web-push sender
4. Handlebars template system with Czech templates
5. 24h and 2h appointment reminder scheduler
6. Automation rule engine with delay support
7. API routes for templates, notifications, automation rules
8. Visual automation builder UI with React Flow
9. Email open/click tracking
10. Docker Compose integration
  </what-built>
  <how-to-verify>
1. Start services: `docker compose -f docker/docker-compose.yml up -d`
2. Verify notification-worker container is running: `docker ps | grep notification-worker`
3. Check worker logs: `docker logs schedulebox-notification-worker` - should show "Notification worker fully started"
4. Visit http://localhost:3000/notifications - notification history page loads
5. Visit http://localhost:3000/templates - template list page loads
6. Visit http://localhost:3000/automation - automation rules page loads
7. Visit http://localhost:3000/automation/builder - React Flow canvas renders with drag-drop nodes
8. Create a test automation rule via the builder (trigger: booking_completed -> delay: 0 -> action: send_email)
9. Verify type-check passes: `pnpm type-check` across all packages
10. (Optional if SMTP configured) Create a booking via API, check worker logs for "Enqueued confirmation email"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Docker Compose YAML is valid
- Notification worker starts and connects to RabbitMQ + Redis
- All frontend pages load without errors
- React Flow builder is interactive
- Type-check passes across all packages
</verification>

<success_criteria>
- Notification worker runs as Docker Compose service alongside the app
- All environment variables documented
- Full notification pipeline verified: events -> consumers -> queues -> delivery
- Frontend pages render correctly for notification management
</success_criteria>

<output>
After completion, create `.planning/phases/07-notifications-automation/07-07-SUMMARY.md`
</output>
