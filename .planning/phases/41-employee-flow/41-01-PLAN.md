---
phase: 41-employee-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/api/v1/employees/me/bookings/route.ts
  - apps/web/hooks/use-my-bookings.ts
  - apps/web/app/[locale]/(dashboard)/bookings/page.tsx
  - apps/web/messages/en.json
  - apps/web/messages/cs.json
  - apps/web/messages/sk.json
autonomous: true
requirements:
  - EMP-03
  - EMP-04

must_haves:
  truths:
    - "Employee sees only their assigned bookings on the bookings page"
    - "Employee can confirm, complete, or mark no-show on their bookings"
    - "Owner still sees all bookings (no regression)"
  artifacts:
    - path: "apps/web/app/api/v1/employees/me/bookings/route.ts"
      provides: "GET endpoint returning only bookings assigned to the authenticated employee"
      exports: ["GET"]
    - path: "apps/web/hooks/use-my-bookings.ts"
      provides: "TanStack Query hook for fetching employee's own bookings"
      exports: ["useMyBookings"]
  key_links:
    - from: "apps/web/app/[locale]/(dashboard)/bookings/page.tsx"
      to: "hooks/use-my-bookings.ts OR hooks/use-bookings-query.ts"
      via: "conditional hook based on user.role"
      pattern: "role.*employee.*useMyBookings|useBookingsQuery"
    - from: "apps/web/app/api/v1/employees/me/bookings/route.ts"
      to: "lib/booking/booking-service.ts"
      via: "listBookings with employee_id filter"
      pattern: "listBookings.*employee"
---

<objective>
Employee-scoped booking list and status actions

Purpose: Employees must see only their own bookings (not other employees') and be able to change booking status (confirm, complete, no-show). The bookings page currently shows ALL company bookings regardless of role.

Output: A new /api/v1/employees/me/bookings endpoint that resolves the employee's internal ID from their JWT and filters bookings. The bookings page conditionally uses this endpoint when the logged-in user is an employee.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/app/api/v1/employees/me/working-hours/route.ts (pattern for resolveEmployee helper)
@apps/web/app/api/v1/bookings/route.ts (existing bookings list endpoint)
@apps/web/lib/booking/booking-service.ts (listBookings with employee_id filter)
@apps/web/hooks/use-bookings-query.ts (existing bookings query hooks)
@apps/web/hooks/use-my-schedule.ts (pattern for employee /me/ hooks)
@apps/web/app/[locale]/(dashboard)/bookings/page.tsx (bookings list UI)
@apps/web/components/booking/BookingDetailPanel.tsx (status action buttons)
@apps/web/stores/auth.store.ts (user.role for conditional logic)

<interfaces>
From apps/web/lib/booking/booking-service.ts:
```typescript
export async function listBookings(
  query: BookingListQuery,
  companyId: number,
): Promise<{ data: BookingWithRelations[]; meta: PaginationMeta }>
// Supports employee_id filter in query
```

From apps/web/app/api/v1/employees/me/working-hours/route.ts:
```typescript
// Pattern for resolving employee from JWT:
async function resolveEmployee(userUuid: string, companyId: number) {
  // Looks up users.id from users.uuid, then employees.id from employees.userId + companyId
  return { id: number, uuid: string };
}
```

From apps/web/hooks/use-bookings-query.ts:
```typescript
export function useBookingsQuery(params: BookingListQuery): UseQueryResult
```

From apps/web/stores/auth.store.ts:
```typescript
interface User { role: string; /* 'owner' | 'employee' | 'manager' | 'admin' */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Employee bookings API endpoint and frontend hook</name>
  <files>
    apps/web/app/api/v1/employees/me/bookings/route.ts,
    apps/web/hooks/use-my-bookings.ts
  </files>
  <action>
    1. Create `apps/web/app/api/v1/employees/me/bookings/route.ts`:
       - GET handler using `createRouteHandler({ requiresAuth: true })`
       - Copy the `resolveEmployee` helper pattern from the working-hours route (resolve user UUID to employee internal ID via users + employees tables)
       - Parse query params using `validateQuery` with the existing `bookingListQuerySchema`
       - Call `listBookings(query, companyId)` but inject the resolved `employee.id` into the query as `employee_id` — this ensures the employee ONLY sees their own bookings regardless of what query params the client sends
       - Return `paginatedResponse(data, meta)`
       - Do NOT require any specific permission beyond auth — any authenticated employee should see their own bookings

    2. Create `apps/web/hooks/use-my-bookings.ts`:
       - Export `useMyBookings(params)` hook that calls `apiClient.get('/employees/me/bookings', params)`
       - Accept same params shape as `useBookingsQuery` (page, limit, status)
       - Use query key `['me', 'bookings', params]` to keep it separate from owner's bookings cache
       - staleTime: 30_000 (same as regular bookings)
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - GET /api/v1/employees/me/bookings returns only bookings assigned to the authenticated employee
    - useMyBookings hook exists and compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Bookings page employee-aware filtering + translations</name>
  <files>
    apps/web/app/[locale]/(dashboard)/bookings/page.tsx,
    apps/web/messages/en.json,
    apps/web/messages/cs.json,
    apps/web/messages/sk.json
  </files>
  <action>
    1. Modify `apps/web/app/[locale]/(dashboard)/bookings/page.tsx`:
       - Import `useAuthStore` from `@/stores/auth.store`
       - Import `useMyBookings` from `@/hooks/use-my-bookings`
       - At the top of the component, get `const { user } = useAuthStore()`
       - Determine `const isEmployee = user?.role === 'employee'`
       - Use conditional data fetching:
         - If isEmployee: `useMyBookings({ page, limit: 20, status })`
         - If NOT employee: `useBookingsQuery({ page, limit: 20, status })` (existing behavior)
       - Both hooks return the same shape `{ data: Booking[], meta: PaginationMeta }` so the rest of the component works unchanged
       - For employees, hide the "New Booking" button (employees don't create bookings, owners do). Wrap the Button/Link with `{!isEmployee && (...)}`
       - The employee column in the table can be hidden for employees since all bookings are theirs. Use `{!isEmployee && <TableHead>...</TableHead>}` and matching cell
       - BookingDetailPanel already has confirm/complete/no-show action buttons — these work via POST to `/bookings/:uuid/confirm` etc. which uses BOOKINGS_UPDATE permission. Employees should already have this permission from seed data. No changes needed to the detail panel.

    2. Add/verify translation keys in all 3 locale files:
       - en.json: `"booking.list.myBookings": "My Bookings"` (for employee page title variant)
       - cs.json: `"booking.list.myBookings": "Moje rezervace"`
       - sk.json: `"booking.list.myBookings": "Moje rezervacie"`
       - Use `isEmployee ? t('myBookings') : t('title')` for the PageHeader title
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Bookings page shows "My Bookings" title for employees and only their bookings
    - Owners still see all bookings with full UI (no regression)
    - Employee column hidden for employees
    - "New Booking" button hidden for employees
    - Clicking a booking opens detail panel with confirm/complete/no-show actions
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` passes
2. Employee bookings API: logged in as employee, GET /api/v1/employees/me/bookings returns only their bookings
3. Owner bookings: logged in as owner, bookings page still shows all company bookings
4. Status actions: employee clicks booking -> detail panel -> confirm/complete/no-show buttons work
</verification>

<success_criteria>
- Employee sees only bookings assigned to them on /bookings page
- Employee can change booking status via detail panel (confirm, complete, no-show)
- Owner sees all company bookings (no regression)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/41-employee-flow/41-01-SUMMARY.md`
</output>
