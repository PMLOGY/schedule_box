---
phase: 32-frontend-polish
plan: 03
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - apps/web/app/[locale]/(dashboard)/dashboard/page.tsx
  - apps/web/components/dashboard/dashboard-grid.tsx
  - apps/web/components/dashboard/quick-actions.tsx
  - apps/web/components/dashboard/stat-card.tsx
  - apps/web/components/dashboard/recent-bookings.tsx
  - apps/web/components/dashboard/revenue-mini-chart.tsx
  - apps/web/app/[locale]/(marketing)/page.tsx
  - apps/web/app/[locale]/(marketing)/_components/hero-section.tsx
  - apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx
  - apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx
autonomous: false

must_haves:
  truths:
    - "Dashboard shows a KPI summary row (revenue, bookings, customers, no-show rate) visible without scrolling on 1280px desktop"
    - "Dashboard has data visualization cards (mini revenue chart or booking trend) below the KPI row"
    - "Quick action buttons are visible without scrolling on 1280px desktop screen"
    - "Landing page includes the testimonials/social-proof section"
    - "All dashboard and marketing pages have no horizontal scroll at 375px, 768px, and 1280px"
  artifacts:
    - path: "apps/web/components/dashboard/recent-bookings.tsx"
      provides: "Recent bookings card for dashboard"
      min_lines: 30
    - path: "apps/web/components/dashboard/revenue-mini-chart.tsx"
      provides: "Small revenue trend chart for dashboard"
      min_lines: 25
    - path: "apps/web/app/[locale]/(dashboard)/dashboard/page.tsx"
      provides: "Redesigned dashboard with grid layout"
      contains: "grid"
  key_links:
    - from: "apps/web/app/[locale]/(dashboard)/dashboard/page.tsx"
      to: "apps/web/components/dashboard/revenue-mini-chart.tsx"
      via: "RevenueMiniChart import"
      pattern: "RevenueMiniChart"
    - from: "apps/web/app/[locale]/(dashboard)/dashboard/page.tsx"
      to: "apps/web/components/dashboard/recent-bookings.tsx"
      via: "RecentBookings import"
      pattern: "RecentBookings"
    - from: "apps/web/app/[locale]/(marketing)/page.tsx"
      to: "apps/web/app/[locale]/(marketing)/_components/social-proof.tsx"
      via: "SocialProof import"
      pattern: "SocialProof"
---

<objective>
Redesign the dashboard page with a professional grid layout (KPI row + visualization cards + quick actions visible above the fold), add social proof to the landing page, and verify responsive design across all breakpoints.

Purpose: Deliver the professional dashboard feel (UI-01), complete landing page (UI-03), and responsive audit (UI-06). This plan depends on dark mode (Plan 01) and loading states (Plan 02) being in place.
Output: Redesigned dashboard page, 2 new dashboard components, landing page with testimonials, responsive fixes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-frontend-polish/32-RESEARCH.md

@apps/web/app/[locale]/(dashboard)/dashboard/page.tsx
@apps/web/components/dashboard/dashboard-grid.tsx
@apps/web/components/dashboard/quick-actions.tsx
@apps/web/components/dashboard/stat-card.tsx
@apps/web/components/ai/AiInsightsPanel.tsx
@apps/web/app/[locale]/(marketing)/page.tsx
@apps/web/app/[locale]/(marketing)/_components/hero-section.tsx
@apps/web/app/[locale]/(marketing)/_components/social-proof.tsx
@apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign dashboard with professional grid layout and new components</name>
  <files>
    apps/web/app/[locale]/(dashboard)/dashboard/page.tsx
    apps/web/components/dashboard/dashboard-grid.tsx
    apps/web/components/dashboard/quick-actions.tsx
    apps/web/components/dashboard/stat-card.tsx
    apps/web/components/dashboard/recent-bookings.tsx
    apps/web/components/dashboard/revenue-mini-chart.tsx
  </files>
  <action>
1. Create `apps/web/components/dashboard/revenue-mini-chart.tsx`:
   - Client component using recharts `AreaChart` (already installed)
   - Import `useAnalyticsQuery` from `@/hooks/use-analytics-query` for revenue data
   - If analytics hook provides daily revenue breakdown, render a compact `AreaChart` (height 180px) with:
     - Gradient fill area (blue/primary), no grid lines, minimal axis labels
     - `<ResponsiveContainer width="100%" height={180}>`
     - `<Area type="monotone" dataKey="revenue" stroke="hsl(217, 91%, 60%)" fill="url(#revenueGradient)" />`
     - `<defs>` with linearGradient from primary at 0.3 opacity to transparent
   - If no data available, show a subtle placeholder with `text-muted-foreground`: "Revenue trend will appear after first bookings"
   - Wrap in `Card` with `CardHeader` title "Revenue Trend" (use translations key `dashboard.revenueTrend`)
   - Show total revenue value as large number in card header right side

2. Create `apps/web/components/dashboard/recent-bookings.tsx`:
   - Client component fetching latest 5 bookings via `useBookingsQuery({ page: 1, limit: 5 })`
   - Render in a `Card` with `CardHeader` "Recent Bookings" (use translations key `dashboard.recentBookings`)
   - Each booking row: flex row with customer name (font-medium), service name (text-muted-foreground text-sm), time (text-muted-foreground text-xs), and `BookingStatusBadge`
   - If loading: 5 skeleton rows (`<Skeleton className="h-12 w-full" />`)
   - If empty: show BookingsEmptyState (compact variant — use the shared EmptyState with smaller padding)
   - "View all" link at bottom of card: `<Link href="/bookings" className="text-sm text-primary hover:underline">`
   - Import `BookingStatusBadge` from `@/components/booking/BookingStatusBadge`
   - Import `BookingsEmptyState` from `@/components/onboarding/empty-states/bookings-empty`

3. Update `apps/web/components/dashboard/stat-card.tsx`:
   - Add optional `className` prop to allow grid positioning
   - Ensure the card uses `shadow-sm hover:shadow transition-shadow` for subtle hover effect
   - Keep existing functionality unchanged

4. Update `apps/web/components/dashboard/quick-actions.tsx`:
   - Change from a Card-based layout to inline button row
   - Remove the Card wrapper, render buttons directly in a `div className="flex flex-wrap gap-3"`
   - Add 2 more quick actions: "View Analytics" (BarChart3 icon, href="/analytics") and "Manage Services" (Settings icon, href="/services")
   - All buttons: `variant="outline" size="sm"` with icons

5. Update `apps/web/components/dashboard/dashboard-grid.tsx`:
   - Keep the 4 KPI stat cards in `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`
   - Update the 4th card to show "No-show Rate" instead of "Average Rating" (use `data?.comparison?.noShowChange` if available, otherwise show "N/A"). Use `AlertTriangle` icon. This matches the success criteria requirement for no-show rate KPI.

6. Redesign `apps/web/app/[locale]/(dashboard)/dashboard/page.tsx`:
   New layout structure (top to bottom, all above fold on 1280px):
   ```
   [PageHeader] ............... [QuickActions (inline buttons)]
   [KPI Card 1] [KPI Card 2] [KPI Card 3] [KPI Card 4]
   [Revenue Mini Chart  (2/3)] [Recent Bookings (1/3)]
   [AI Insights Panel (full width)]
   [Onboarding Checklist (conditional)]
   [Demo Data Card (conditional)]
   ```

   Implementation:
   ```tsx
   <div className="space-y-6">
     {/* Header row with quick actions */}
     <div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
       <PageHeader title={t('title')} />
       <QuickActions />
     </div>

     {/* Onboarding (conditional, above KPIs for new users) */}
     <OnboardingChecklist />
     <DemoDataCard />

     {/* KPI Summary Row */}
     <DashboardGrid />

     {/* Data visualization row: 2-column grid on lg */}
     <div className="grid grid-cols-1 gap-6 lg:grid-cols-3">
       <div className="lg:col-span-2">
         <RevenueMiniChart />
       </div>
       <div className="lg:col-span-1">
         <RecentBookings />
       </div>
     </div>

     {/* AI Insights */}
     <AiInsightsPanel />
   </div>
   ```

   Keep all existing imports. Add imports for RevenueMiniChart and RecentBookings.
   Keep the onboarding redirect logic and welcome banner unchanged.
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/web build` to confirm all imports resolve and no TypeScript errors.
    Verify dashboard page imports RevenueMiniChart and RecentBookings.
    Verify quick-actions no longer wrapped in Card component.
    Verify stat-card has hover shadow classes.
  </verify>
  <done>
    Dashboard has professional grid layout: KPI summary row (revenue, bookings, customers, no-show rate) at top, revenue trend chart and recent bookings side by side below, AI insights full width, quick actions in header. All visible without scrolling on 1280px desktop.
  </done>
</task>

<task type="auto">
  <name>Task 2: Landing page upgrade with testimonials and responsive audit fixes</name>
  <files>
    apps/web/app/[locale]/(marketing)/page.tsx
    apps/web/app/[locale]/(marketing)/_components/hero-section.tsx
    apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx
    apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx
  </files>
  <action>
1. Update `apps/web/app/[locale]/(marketing)/page.tsx`:
   - Add the `SocialProof` component (already exists but not rendered)
   - Import `SocialProof` from `./_components/social-proof`
   - New page order: HeroSection, FeatureGrid, SocialProof, TrustBadges
   - This adds the testimonials section to the landing page

2. Update `apps/web/app/[locale]/(marketing)/_components/hero-section.tsx`:
   - Add `overflow-hidden` to the section to prevent any horizontal scroll on mobile from animations
   - Ensure the grid has `gap-8 lg:gap-12` (reduce gap on smaller screens)
   - Add `max-w-full overflow-hidden` to the LiveWidgetPreview container div to prevent it from causing horizontal scroll on mobile (375px)
   - Ensure heading text uses `text-3xl md:text-4xl lg:text-5xl xl:text-6xl` for better mobile scaling (currently starts at text-4xl which can be large on 375px)

3. Update `apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx`:
   - Add `overflow-hidden` to the section to prevent motion animations from causing horizontal scroll
   - Ensure heading uses `text-2xl md:text-3xl lg:text-4xl` for better mobile scaling

4. Update `apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx`:
   - Keep existing responsive behavior (nav hidden on mobile, CTA always visible — acceptable since there are only 2 links and CTA is always visible)
   - Ensure no horizontal scroll by verifying the header has `overflow-hidden` or max-width constraints

5. Responsive audit — check for overflow issues:
   - Add `overflow-x-hidden` to the marketing layout's main wrapper if the hero LiveWidgetPreview causes horizontal scroll at 375px
   - Ensure all dashboard pages use the existing `max-w-7xl mx-auto` constraint from the layout (they already do via layout.tsx)
   - All grid patterns in dashboard pages already use responsive breakpoints (grid-cols-1 -> sm:grid-cols-2 -> lg:grid-cols-4)
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/web build` to confirm imports resolve.
    Verify SocialProof is imported and rendered in marketing page.tsx.
    Verify hero-section has overflow-hidden.
    Verify feature-grid heading scales properly for mobile.
  </verify>
  <done>
    Landing page includes testimonials/social-proof section. Hero section heading scales properly at all breakpoints. Feature grid and hero have overflow-hidden to prevent horizontal scroll. Marketing pages responsive at 375px, 768px, and 1280px without horizontal scroll.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of dark mode, dashboard, and responsive design</name>
  <files>none — verification only</files>
  <action>
    Human visually verifies the combined output of Plans 01, 02, and 03 (Tasks 1-2).
    No code changes — this task is a human verification checkpoint.

    Start dev server: `pnpm dev` and visit http://localhost:3000

    **Dark mode verification:**
    - On landing page, click the theme toggle (sun/moon icon in navbar) — page should switch to dark theme
    - All text, backgrounds, cards should be readable in dark mode (no white backgrounds, no invisible text)
    - Navigate to /dashboard — dark mode should persist
    - Verify sidebar, header, all cards use dark theme colors
    - Toggle back to light mode — everything should return to normal

    **Dashboard verification (1280px desktop):**
    - Dashboard should show: KPI row (4 cards: revenue, bookings, customers, no-show rate) at top
    - Below KPIs: revenue trend chart (left 2/3) and recent bookings (right 1/3)
    - Quick action buttons should be in the header row (not a separate card)
    - AI insights panel below the charts
    - All above content should be visible without scrolling on 1280px viewport

    **Loading states verification:**
    - Hard refresh /dashboard — should see skeleton loaders (gray animated blocks), NOT a spinner
    - Navigate to /bookings — should see table skeleton during load
    - Navigate to /analytics — should see dashboard skeleton during load
    - Navigate to /customers — should see table skeleton during load

    **Landing page verification:**
    - Visit http://localhost:3000 (marketing page)
    - Scroll down — should see: Hero, Features, Testimonials (3 review cards with stars), Trust badges
    - Testimonials section should show 3 cards with star ratings, quotes, and author names

    **Responsive verification (use browser DevTools device toolbar):**
    - At 375px (iPhone SE): Landing page — no horizontal scroll, text readable, hero stacks vertically
    - At 375px: Dashboard — sidebar hidden, content fills width, KPI cards stack to 1 column
    - At 768px (iPad): Landing page — features in 2 columns, hero still stacked
    - At 768px: Dashboard — KPI cards in 2 columns, chart and bookings stack
    - At 1280px: Everything in full desktop layout

    **Error boundary verification:**
    - If possible, trigger an error — should see error card with "Try again" button instead of white screen
  </action>
  <verify>Human confirms all visual checks pass at all breakpoints and in both themes.</verify>
  <done>Human approves the visual quality of dark mode, dashboard layout, loading states, landing page testimonials, and responsive design across all breakpoints.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @schedulebox/web build` succeeds
2. Dashboard page renders KPI summary row with 4 metrics including no-show rate
3. Dashboard has RevenueMiniChart and RecentBookings components
4. Landing page renders SocialProof section with testimonials
5. No horizontal scroll at 375px on marketing and dashboard pages
6. Human verification confirms visual quality at all breakpoints and in both themes
</verification>

<success_criteria>
- Dashboard has professional grid layout with KPI row (revenue, bookings, customers, no-show rate), data visualization, and quick actions — all visible without scrolling at 1280px
- Landing page includes testimonials/social-proof section
- All pages pass responsive design at 375px, 768px, 1280px without horizontal scroll
- Human visual approval obtained for dark mode, dashboard layout, loading states, and responsive design
</success_criteria>

<output>
After completion, create `.planning/phases/32-frontend-polish/32-03-SUMMARY.md`
</output>
