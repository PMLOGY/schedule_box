---
phase: 32-frontend-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/[locale]/(dashboard)/dashboard/loading.tsx
  - apps/web/app/[locale]/(dashboard)/bookings/loading.tsx
  - apps/web/app/[locale]/(dashboard)/customers/loading.tsx
  - apps/web/app/[locale]/(dashboard)/employees/loading.tsx
  - apps/web/app/[locale]/(dashboard)/services/loading.tsx
  - apps/web/app/[locale]/(dashboard)/calendar/loading.tsx
  - apps/web/app/[locale]/(dashboard)/analytics/loading.tsx
  - apps/web/app/[locale]/(dashboard)/settings/loading.tsx
  - apps/web/app/[locale]/(dashboard)/settings/billing/loading.tsx
  - apps/web/app/[locale]/(dashboard)/loyalty/loading.tsx
  - apps/web/app/[locale]/(dashboard)/automation/loading.tsx
  - apps/web/app/[locale]/(dashboard)/notifications/loading.tsx
  - apps/web/app/[locale]/(dashboard)/marketing/loading.tsx
  - apps/web/app/[locale]/(dashboard)/templates/loading.tsx
  - apps/web/app/[locale]/(dashboard)/ai/loading.tsx
  - apps/web/app/[locale]/(dashboard)/profile/loading.tsx
  - apps/web/app/[locale]/(dashboard)/error.tsx
  - apps/web/components/shared/page-skeleton.tsx
  - apps/web/components/shared/table-skeleton.tsx
autonomous: true

must_haves:
  truths:
    - "Every data-fetching dashboard page shows a skeleton loader (not a spinner or blank) while loading"
    - "Every data list page shows a descriptive empty state with action CTA when no data exists"
    - "A route-level error boundary catches unhandled errors in any dashboard page and shows a retry action"
  artifacts:
    - path: "apps/web/components/shared/page-skeleton.tsx"
      provides: "Reusable page skeleton with header + content area variants"
      min_lines: 20
    - path: "apps/web/components/shared/table-skeleton.tsx"
      provides: "Reusable table skeleton with configurable rows and columns"
      min_lines: 15
    - path: "apps/web/app/[locale]/(dashboard)/error.tsx"
      provides: "Dashboard-wide error boundary"
      min_lines: 20
    - path: "apps/web/app/[locale]/(dashboard)/bookings/loading.tsx"
      provides: "Bookings page skeleton loader"
      min_lines: 5
    - path: "apps/web/app/[locale]/(dashboard)/analytics/loading.tsx"
      provides: "Analytics page skeleton loader"
      min_lines: 5
  key_links:
    - from: "apps/web/app/[locale]/(dashboard)/bookings/loading.tsx"
      to: "apps/web/components/shared/table-skeleton.tsx"
      via: "TableSkeleton import"
      pattern: "TableSkeleton"
    - from: "apps/web/app/[locale]/(dashboard)/error.tsx"
      to: "ErrorBoundary pattern"
      via: "Next.js error.tsx convention"
      pattern: "use client.*error.*reset"
---

<objective>
Create consistent skeleton loading states for all dashboard pages, add route-level error boundary, and ensure every list page shows appropriate empty states.

Purpose: Eliminate blank/spinner loading states (UI-05). Every page should feel responsive with skeleton content hints while data loads, and gracefully handle errors.
Output: 16 loading.tsx files (one per dashboard route group), 1 error.tsx, 2 shared skeleton components.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-frontend-polish/32-RESEARCH.md

@apps/web/app/[locale]/(dashboard)/loading.tsx
@apps/web/components/ui/skeleton.tsx
@apps/web/components/shared/error-boundary.tsx
@apps/web/components/shared/empty-state.tsx
@apps/web/components/shared/page-header.tsx
@apps/web/components/onboarding/empty-states/bookings-empty.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared skeleton components and dashboard error boundary</name>
  <files>
    apps/web/components/shared/page-skeleton.tsx
    apps/web/components/shared/table-skeleton.tsx
    apps/web/app/[locale]/(dashboard)/error.tsx
    apps/web/app/[locale]/(dashboard)/loading.tsx
  </files>
  <action>
1. Create `apps/web/components/shared/page-skeleton.tsx`:
   Export `PageSkeleton` component with props: `{ variant: 'dashboard' | 'table' | 'cards' | 'form' | 'detail' }`.
   - All variants start with a header skeleton: `<div className="space-y-2"><Skeleton className="h-9 w-48" /><Skeleton className="h-4 w-72" /></div>`
   - `dashboard`: Header skeleton + 4 stat card skeletons in grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`, each `<Skeleton className="h-[120px] rounded-xl" />`), then 2 chart area skeletons (`<Skeleton className="h-[300px] rounded-xl" />`)
   - `table`: Header skeleton + filter bar skeleton (2 inputs) + table skeleton (calls TableSkeleton with rows=8, cols=5)
   - `cards`: Header skeleton + grid of 6 card skeletons (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`, each `<Skeleton className="h-[180px] rounded-xl" />`)
   - `form`: Header skeleton + 6 form field skeletons (label + input pairs)
   - `detail`: Header skeleton + sidebar-detail two-column layout skeleton
   Import `Skeleton` from `@/components/ui/skeleton`.

2. Create `apps/web/components/shared/table-skeleton.tsx`:
   Export `TableSkeleton` with props `{ rows?: number; cols?: number }` (defaults: rows=8, cols=5).
   Render a `<div className="rounded-lg border">` containing:
   - Header row: `cols` number of `<Skeleton className="h-4 w-20" />` in a flex row with `px-4 py-3 border-b bg-muted/50`
   - Body rows: `rows` number of rows, each with `cols` skeleton cells: `<Skeleton className="h-4 w-full" />` in alternating `even:bg-muted/20` for visual rhythm
   Use `<Table>`, `<TableHeader>`, `<TableBody>`, `<TableRow>`, `<TableHead>`, `<TableCell>` from ui/table for semantic markup.

3. Create `apps/web/app/[locale]/(dashboard)/error.tsx`:
   Must be a client component (`'use client'`).
   Receives `{ error, reset }` props (Next.js error boundary convention).
   Renders centered card with:
   - `AlertTriangle` icon from lucide-react (h-12 w-12 text-destructive)
   - Heading: "Something went wrong" (hardcoded EN since this is a fallback)
   - Error message in dev mode: `{process.env.NODE_ENV === 'development' && error.message}`
   - Two buttons: "Try again" (calls `reset()`) and "Go to dashboard" (links to /dashboard)
   Use `Card`, `CardContent` from ui/card, `Button` from ui/button.

4. Update existing `apps/web/app/[locale]/(dashboard)/loading.tsx`:
   Replace the current Loader2 spinner implementation with:
   ```tsx
   import { PageSkeleton } from '@/components/shared/page-skeleton';
   export default function DashboardLoading() {
     return <PageSkeleton variant="dashboard" />;
   }
   ```
  </action>
  <verify>
    Verify `apps/web/components/shared/page-skeleton.tsx` exists and exports PageSkeleton.
    Verify `apps/web/components/shared/table-skeleton.tsx` exists and exports TableSkeleton.
    Verify `apps/web/app/[locale]/(dashboard)/error.tsx` exists with 'use client' directive.
    Run `pnpm --filter @schedulebox/web build` to confirm no import errors.
  </verify>
  <done>
    Shared PageSkeleton (5 variants) and TableSkeleton components created. Dashboard-wide error.tsx catches unhandled errors. Default loading.tsx uses PageSkeleton instead of spinner.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add route-specific loading.tsx files for all dashboard pages</name>
  <files>
    apps/web/app/[locale]/(dashboard)/bookings/loading.tsx
    apps/web/app/[locale]/(dashboard)/customers/loading.tsx
    apps/web/app/[locale]/(dashboard)/employees/loading.tsx
    apps/web/app/[locale]/(dashboard)/services/loading.tsx
    apps/web/app/[locale]/(dashboard)/calendar/loading.tsx
    apps/web/app/[locale]/(dashboard)/analytics/loading.tsx
    apps/web/app/[locale]/(dashboard)/settings/loading.tsx
    apps/web/app/[locale]/(dashboard)/settings/billing/loading.tsx
    apps/web/app/[locale]/(dashboard)/loyalty/loading.tsx
    apps/web/app/[locale]/(dashboard)/automation/loading.tsx
    apps/web/app/[locale]/(dashboard)/notifications/loading.tsx
    apps/web/app/[locale]/(dashboard)/marketing/loading.tsx
    apps/web/app/[locale]/(dashboard)/templates/loading.tsx
    apps/web/app/[locale]/(dashboard)/ai/loading.tsx
    apps/web/app/[locale]/(dashboard)/profile/loading.tsx
  </files>
  <action>
Create a `loading.tsx` file in each dashboard route directory. Each file imports `PageSkeleton` from `@/components/shared/page-skeleton` and uses the appropriate variant:

**Table-based pages** (variant="table"):
- `bookings/loading.tsx` — booking list is a table
- `customers/loading.tsx` — customer list is a table
- `employees/loading.tsx` — employee list is a table
- `notifications/loading.tsx` — notification list is a table
- `automation/loading.tsx` — automation rules are a table/list

**Card-based pages** (variant="cards"):
- `services/loading.tsx` — services shown as cards
- `loyalty/loading.tsx` — loyalty programs shown as cards
- `templates/loading.tsx` — templates shown as cards
- `marketing/loading.tsx` — campaigns shown as cards

**Dashboard/chart pages** (variant="dashboard"):
- `analytics/loading.tsx` — KPI cards + charts
- `ai/loading.tsx` — AI insights with stat cards + charts

**Form/detail pages** (variant="form"):
- `settings/loading.tsx` — settings form
- `settings/billing/loading.tsx` — billing uses "detail" variant (subscription card + plan grid + invoice table — use variant="dashboard" since it has mixed content)
- `profile/loading.tsx` — profile form

**Special pages:**
- `calendar/loading.tsx` — Custom skeleton: header skeleton + `<Skeleton className="h-[600px] rounded-xl" />` for the calendar area (full-height placeholder)

Each file follows this template:
```tsx
import { PageSkeleton } from '@/components/shared/page-skeleton';

export default function XxxLoading() {
  return <PageSkeleton variant="table" />;
}
```

For calendar, create a custom one:
```tsx
import { Skeleton } from '@/components/ui/skeleton';

export default function CalendarLoading() {
  return (
    <div className="space-y-6">
      <div className="space-y-2">
        <Skeleton className="h-9 w-48" />
      </div>
      <div className="flex items-center justify-between">
        <Skeleton className="h-10 w-32" />
        <div className="flex gap-2">
          <Skeleton className="h-10 w-24" />
          <Skeleton className="h-10 w-24" />
          <Skeleton className="h-10 w-24" />
        </div>
      </div>
      <Skeleton className="h-[600px] rounded-xl" />
    </div>
  );
}
```
  </action>
  <verify>
    Count loading.tsx files under `(dashboard)/`: should be 16 (1 existing updated + 15 new).
    Run `pnpm --filter @schedulebox/web build` to confirm all imports resolve.
    Grep for `Loader2` in loading.tsx files — should return zero (replaced with skeletons).
  </verify>
  <done>
    Every dashboard route directory has a loading.tsx showing content-appropriate skeleton loaders. No page shows a blank white area or spinner-only state during loading. The bookings, customers, employees pages show table skeletons; analytics/ai show dashboard skeletons; services/loyalty show card skeletons; settings/profile show form skeletons; calendar shows a full-height calendar placeholder.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @schedulebox/web build` succeeds
2. `find apps/web/app/\[locale\]/\(dashboard\) -name "loading.tsx" | wc -l` returns 16
3. `apps/web/app/[locale]/(dashboard)/error.tsx` exists with reset button
4. PageSkeleton and TableSkeleton exported from shared components
5. No remaining Loader2 spinner in any loading.tsx file
</verification>

<success_criteria>
- Every dashboard route group has its own loading.tsx with skeleton appropriate to its content type
- Route-level error.tsx catches unhandled errors with retry and go-home actions
- Shared PageSkeleton component supports 5 variants (dashboard, table, cards, form, detail)
- Shared TableSkeleton component renders semantic table with configurable rows/cols
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-frontend-polish/32-02-SUMMARY.md`
</output>
