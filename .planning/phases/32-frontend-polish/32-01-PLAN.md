---
phase: 32-frontend-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/app/providers.tsx
  - apps/web/app/globals.css
  - apps/web/components/ui/theme-toggle.tsx
  - apps/web/components/layout/header.tsx
  - apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx
  - apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx
  - apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx
  - apps/web/components/dashboard/stat-card.tsx
  - apps/web/components/onboarding/demo-data-card.tsx
  - apps/web/components/loyalty/WalletButtons.tsx
autonomous: true

must_haves:
  truths:
    - "Dark mode toggle is visible in the dashboard header and marketing navbar"
    - "System color scheme preference is respected on first load (prefers-color-scheme)"
    - "All dashboard pages render correctly in dark mode without hardcoded white/gray backgrounds"
    - "All marketing pages render correctly in dark mode without hardcoded white/gray backgrounds"
    - "Design tokens are consistent — spacing, shadows, and border-radius follow the single CSS variable set"
  artifacts:
    - path: "apps/web/components/ui/theme-toggle.tsx"
      provides: "Dark mode toggle button component"
      min_lines: 15
    - path: "apps/web/app/providers.tsx"
      provides: "ThemeProvider wrapping the app"
      contains: "ThemeProvider"
    - path: "apps/web/app/globals.css"
      provides: "Extended design tokens (shadow, spacing utilities)"
      contains: "--shadow"
  key_links:
    - from: "apps/web/app/providers.tsx"
      to: "next-themes"
      via: "ThemeProvider import"
      pattern: "ThemeProvider.*next-themes"
    - from: "apps/web/components/layout/header.tsx"
      to: "apps/web/components/ui/theme-toggle.tsx"
      via: "ThemeToggle import"
      pattern: "ThemeToggle"
    - from: "apps/web/components/layout/header.tsx"
      to: "bg-background"
      via: "semantic color class"
      pattern: "bg-background"
---

<objective>
Install next-themes, create ThemeProvider and toggle component, fix all hardcoded light-only colors across dashboard and marketing pages, and extend design system tokens for shadows and spacing.

Purpose: Enable dark mode (UI-04) and harmonize design tokens (UI-02) so all pages use the single CSS variable set. This is foundational — Plans 02 and 03 build on top of dark-mode-ready components.
Output: Working dark mode toggle, ThemeProvider, all hardcoded colors replaced with semantic tokens, extended shadow/spacing tokens in globals.css.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-frontend-polish/32-RESEARCH.md

@apps/web/app/providers.tsx
@apps/web/app/globals.css
@apps/web/tailwind.config.ts
@apps/web/app/layout.tsx
@apps/web/components/layout/header.tsx
@apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx
@apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx
@apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx
@apps/web/components/dashboard/stat-card.tsx
@apps/web/components/onboarding/demo-data-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install next-themes, create ThemeProvider + toggle, extend design tokens</name>
  <files>
    apps/web/package.json
    apps/web/app/providers.tsx
    apps/web/app/globals.css
    apps/web/components/ui/theme-toggle.tsx
    apps/web/tailwind.config.ts
  </files>
  <action>
1. Install next-themes:
   `pnpm --filter @schedulebox/web add next-themes`

2. Create `apps/web/components/ui/theme-toggle.tsx`:
   - Import `useTheme` from `next-themes`, `Moon` and `Sun` from `lucide-react`, `Button` from `./button`
   - Export `ThemeToggle` component that renders a ghost Button (size="icon") with Sun icon (visible in light, hidden in dark via `dark:hidden`) and Moon icon (hidden in light, visible in dark via `hidden dark:block`)
   - On click, toggle between "light" and "dark" using `setTheme(theme === 'dark' ? 'light' : 'dark')`
   - Add `suppressHydrationWarning` on the button
   - Use `mounted` state pattern (useState + useEffect) to avoid hydration mismatch — render null or a placeholder icon until mounted

3. Update `apps/web/app/providers.tsx`:
   - Import `ThemeProvider` from `next-themes`
   - Wrap children with `<ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>`
   - Keep existing QueryClientProvider wrapping inside ThemeProvider

4. Extend `apps/web/app/globals.css` with additional design tokens inside `:root` and `.dark`:
   ```css
   /* Shadow system */
   --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
   --shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
   --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
   --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
   ```
   For `.dark`:
   ```css
   --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.3);
   --shadow: 0 1px 3px 0 rgb(0 0 0 / 0.4), 0 1px 2px -1px rgb(0 0 0 / 0.3);
   --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.4), 0 2px 4px -2px rgb(0 0 0 / 0.3);
   --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.4), 0 4px 6px -4px rgb(0 0 0 / 0.3);
   ```

   Also add a success color token for consistent green usage:
   ```css
   :root {
     --success: 142 71% 45%;
     --success-foreground: 0 0% 100%;
     --warning: 38 92% 50%;
     --warning-foreground: 0 0% 100%;
   }
   .dark {
     --success: 142 71% 45%;
     --success-foreground: 0 0% 100%;
     --warning: 38 92% 50%;
     --warning-foreground: 0 0% 100%;
   }
   ```

5. Update `apps/web/tailwind.config.ts` to add the new color tokens:
   ```ts
   success: {
     DEFAULT: 'hsl(var(--success))',
     foreground: 'hsl(var(--success-foreground))',
   },
   warning: {
     DEFAULT: 'hsl(var(--warning))',
     foreground: 'hsl(var(--warning-foreground))',
   },
   ```
   Also add boxShadow entries referencing the CSS variables:
   ```ts
   boxShadow: {
     sm: 'var(--shadow-sm)',
     DEFAULT: 'var(--shadow)',
     md: 'var(--shadow-md)',
     lg: 'var(--shadow-lg)',
   },
   ```
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/web build` to confirm no TypeScript errors.
    Verify `node_modules/next-themes` exists after install.
    Verify `apps/web/components/ui/theme-toggle.tsx` exports ThemeToggle.
    Verify `apps/web/app/providers.tsx` contains ThemeProvider import.
  </verify>
  <done>
    next-themes installed, ThemeProvider wraps the app, ThemeToggle component created, design tokens extended with shadow system and success/warning semantic colors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix hardcoded colors and add theme toggle to header/navbar</name>
  <files>
    apps/web/components/layout/header.tsx
    apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx
    apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx
    apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx
    apps/web/components/dashboard/stat-card.tsx
    apps/web/components/onboarding/demo-data-card.tsx
    apps/web/components/loyalty/WalletButtons.tsx
  </files>
  <action>
1. **Header** (`components/layout/header.tsx`):
   - Replace `bg-white` with `bg-background`
   - Add `<ThemeToggle />` between LocaleSwitcher and UserMenu in the header actions div
   - Import ThemeToggle from `@/components/ui/theme-toggle`

2. **Marketing navbar** (`marketing-navbar.tsx`):
   - Replace `bg-white/95 backdrop-blur supports-[backdrop-filter]:bg-white/60` with `bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60`
   - Add `<ThemeToggle />` next to the LocaleSwitcher in the button group (before the CTA button). Since this is a server component, convert the nav actions div to include a client island wrapper, OR move the ThemeToggle to a separate client component embedded inline. Simplest approach: make the entire component async server component and embed `<ThemeToggle />` directly since it's a client component imported into an RSC — this works fine in Next.js 14.
   - Import ThemeToggle from `@/components/ui/theme-toggle`

3. **Marketing footer** (`marketing-footer.tsx`):
   - Replace `bg-gray-50` with `bg-muted`

4. **Feature grid** (`feature-grid.tsx`):
   - Replace `bg-gray-50/50` on the section with `bg-muted/50`

5. **Stat card** (`stat-card.tsx`):
   - Replace hardcoded `text-green-600` with `text-success` (or `text-green-600 dark:text-green-400`)
   - Replace hardcoded `text-red-600` with `text-destructive`
   - This ensures trend arrows are visible in dark mode

6. **Demo data card** (`demo-data-card.tsx`):
   - Replace `bg-gray-50/50` with `bg-muted/50`
   - Replace `border-gray-200` with `border-border`

7. **WalletButtons** (`components/loyalty/WalletButtons.tsx`):
   - Replace `bg-white text-gray-800 border border-gray-300 hover:bg-gray-50` with `bg-background text-foreground border border-border hover:bg-accent`

Note: Do NOT change `dark:` prefixed classes that already exist in analytics charts and billing page — those are already correct dual-mode styles.
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/web build` to confirm no TypeScript or import errors.
    Grep for remaining `bg-white` usages that are NOT inside embed/ or loyalty card decorative elements — should be zero outside those contexts.
    Grep for `bg-gray-50` in main layout/page files — should be zero (only allowed in embed widget).
  </verify>
  <done>
    Dashboard header and marketing navbar show the ThemeToggle button. All hardcoded bg-white, bg-gray-50 replaced with semantic tokens (bg-background, bg-muted). Trend colors in stat cards visible in both themes. Dark mode is fully functional across all pages.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @schedulebox/web build` succeeds with zero errors
2. ThemeToggle is rendered in both dashboard header and marketing navbar
3. globals.css contains --shadow-sm, --shadow, --shadow-md, --shadow-lg in both :root and .dark
4. globals.css contains --success and --warning tokens
5. No remaining `bg-white` in header.tsx or marketing-navbar.tsx
6. No remaining `bg-gray-50` in marketing-footer.tsx or feature-grid.tsx
</verification>

<success_criteria>
- next-themes installed and ThemeProvider wraps the entire app
- Dark mode toggle visible and functional in dashboard header and marketing navbar
- System preference (prefers-color-scheme) respected on first load via defaultTheme="system"
- All hardcoded light-only colors (bg-white, bg-gray-50, text-green-600 without dark variant) replaced with semantic tokens
- Design system extended with shadow variables and success/warning color tokens
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-frontend-polish/32-01-SUMMARY.md`
</output>
