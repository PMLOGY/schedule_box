---
phase: 03-auth-core-services
plan: 07
type: execute
wave: 3
depends_on: ['03-03']
files_modified:
  - apps/web/validations/service.ts
  - apps/web/app/api/v1/services/route.ts
  - apps/web/app/api/v1/services/[id]/route.ts
  - apps/web/app/api/v1/service-categories/route.ts
  - apps/web/app/api/v1/service-categories/[id]/route.ts
  - apps/web/validations/employee.ts
  - apps/web/app/api/v1/employees/route.ts
  - apps/web/app/api/v1/employees/[id]/route.ts
  - apps/web/app/api/v1/employees/[id]/services/route.ts
  - apps/web/app/api/v1/employees/[id]/working-hours/route.ts
  - apps/web/app/api/v1/employees/[id]/schedule-overrides/route.ts
autonomous: true

must_haves:
  truths:
    - 'Service CRUD works with category filtering and soft delete'
    - 'Service categories can be created, updated, and deleted'
    - 'Employee CRUD works with service assignment and soft delete'
    - 'Working hours can be read and bulk-updated for an employee'
    - 'Schedule overrides can be created for specific dates'
    - 'All endpoints enforce tenant isolation via company_id'
  artifacts:
    - path: 'apps/web/validations/service.ts'
      provides: 'Zod schemas for service and category endpoints'
      exports: ['serviceCreateSchema', 'serviceUpdateSchema', 'serviceCategoryCreateSchema']
      min_lines: 30
    - path: 'apps/web/app/api/v1/services/route.ts'
      provides: 'Service list and create'
      exports: ['GET', 'POST']
      min_lines: 40
    - path: 'apps/web/app/api/v1/services/[id]/route.ts'
      provides: 'Service detail, update, delete'
      exports: ['GET', 'PUT', 'DELETE']
    - path: 'apps/web/validations/employee.ts'
      provides: 'Zod schemas for employee endpoints'
      exports: ['employeeCreateSchema', 'employeeUpdateSchema', 'workingHoursSchema', 'scheduleOverrideSchema']
      min_lines: 30
    - path: 'apps/web/app/api/v1/employees/route.ts'
      provides: 'Employee list and create'
      exports: ['GET', 'POST']
    - path: 'apps/web/app/api/v1/employees/[id]/route.ts'
      provides: 'Employee detail, update, delete'
      exports: ['GET', 'PUT', 'DELETE']
  key_links:
    - from: 'apps/web/app/api/v1/services/route.ts'
      to: '@schedulebox/database services schema'
      via: 'Drizzle query with company_id filter'
      pattern: "eq.*companyId"
    - from: 'apps/web/app/api/v1/employees/route.ts'
      to: '@schedulebox/database employees schema'
      via: 'Drizzle query with company_id filter'
      pattern: "eq.*companyId"
    - from: 'apps/web/app/api/v1/employees/[id]/working-hours/route.ts'
      to: '@schedulebox/database working hours schema'
      via: 'Drizzle query for working hours'
      pattern: "workingHours"
---

<objective>
Implement Service CRUD with categories, and Employee CRUD with service assignment, working hours, and schedule overrides.

Purpose: Services define what a business offers (with pricing, duration, capacity). Employees are assigned to services and have working hours that determine availability. These are the two primary entity types needed before the booking engine can work.

Output: Service and employee route files with validation schemas
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth-core-services/03-03-SUMMARY.md
@packages/database/src/schema/services.ts
@packages/database/src/schema/employees.ts
@schedulebox_complete_documentation.md (lines 2865-3103 for service/employee endpoints, lines 4635-4772 for Service/Employee/WorkingHours schemas)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create service and service category CRUD endpoints</name>
  <files>
    apps/web/validations/service.ts
    apps/web/app/api/v1/services/route.ts
    apps/web/app/api/v1/services/[id]/route.ts
    apps/web/app/api/v1/service-categories/route.ts
    apps/web/app/api/v1/service-categories/[id]/route.ts
  </files>
  <action>
    Create `apps/web/validations/service.ts`:
    - serviceCreateSchema matching OpenAPI ServiceCreate (lines 4660-4680):
      name: z.string().min(1).max(255),
      description: z.string().optional(),
      category_id: z.number().int().optional(),
      duration_minutes: z.number().int().min(5),
      buffer_before_minutes: z.number().int().min(0).optional().default(0),
      buffer_after_minutes: z.number().int().min(0).optional().default(0),
      price: z.number().min(0),
      dynamic_pricing_enabled: z.boolean().optional().default(false),
      price_min: z.number().optional(),
      price_max: z.number().optional(),
      max_capacity: z.number().int().min(1).optional().default(1),
      online_booking_enabled: z.boolean().optional().default(true),
      requires_payment: z.boolean().optional().default(false),
      is_online: z.boolean().optional().default(false),
      video_provider: z.enum(['zoom', 'google_meet', 'ms_teams']).optional(),
      color: z.string().regex(/^#[0-9A-Fa-f]{6}$/).optional(),
      resource_ids: z.array(z.number().int()).optional()

    - serviceUpdateSchema matching OpenAPI ServiceUpdate (lines 4682-4690):
      All optional: name, description, category_id, duration_minutes, price, is_active

    - serviceCategoryCreateSchema:
      name: z.string().min(1).max(100),
      description: z.string().optional(),
      sort_order: z.number().int().optional()

    - serviceQuerySchema:
      category_id: z.coerce.number().int().optional(),
      is_active: z.enum(['true', 'false']).optional()

    Create `apps/web/app/api/v1/services/route.ts`:
    Export GET = createRouteHandler:
      requiresAuth: true, requiredPermissions: ['services.read']
      - Parse query with serviceQuerySchema
      - Query services WHERE companyId AND deletedAt IS NULL
      - If category_id filter, add WHERE categoryId
      - If is_active filter, add WHERE isActive
      - Include category via join with service_categories
      - Return { data: services } (using UUIDs)

    Export POST = createRouteHandler:
      bodySchema: serviceCreateSchema, requiresAuth: true, requiredPermissions: ['services.create']
      - Insert service with companyId
      - If resource_ids, insert into service_resources junction table
      - Return createdResponse with service data

    Create `apps/web/app/api/v1/services/[id]/route.ts`:
    - [id] is UUID
    Export GET: fetch by UUID and companyId, include category, return detail
    Export PUT: update by UUID and companyId with serviceUpdateSchema
    Export DELETE: soft delete (SET deletedAt = NOW())

    Create `apps/web/app/api/v1/service-categories/route.ts`:
    Export GET: list categories WHERE companyId, ORDER BY sort_order
    Export POST: create category with companyId, requiredPermissions: ['services.create']

    Create `apps/web/app/api/v1/service-categories/[id]/route.ts`:
    Export PUT: update category by id (SERIAL, not UUID — categories are simple entities)
    Export DELETE: delete category (hard delete — categories don't have soft delete)
    Both require 'services.update'/'services.delete' permissions, scoped to companyId
  </action>
  <verify>
    Run `cd apps/web && pnpm tsc --noEmit` — compiles without errors.
    Verify services support category and is_active filtering.
    Verify service delete is soft delete (deletedAt).
    Verify category CRUD is scoped to company.
  </verify>
  <done>
    Service CRUD with category filtering, soft delete, and resource assignment. Service category CRUD for organization. All tenant-isolated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create employee CRUD, service assignment, working hours, and schedule override endpoints</name>
  <files>
    apps/web/validations/employee.ts
    apps/web/app/api/v1/employees/route.ts
    apps/web/app/api/v1/employees/[id]/route.ts
    apps/web/app/api/v1/employees/[id]/services/route.ts
    apps/web/app/api/v1/employees/[id]/working-hours/route.ts
    apps/web/app/api/v1/employees/[id]/schedule-overrides/route.ts
  </files>
  <action>
    Create `apps/web/validations/employee.ts`:
    - employeeCreateSchema matching OpenAPI EmployeeCreate (lines 4724-4734):
      name: z.string().min(1).max(255),
      email: z.string().email().optional(),
      phone: z.string().optional(),
      title: z.string().optional(),
      bio: z.string().optional(),
      color: z.string().regex(/^#[0-9A-Fa-f]{6}$/).optional(),
      service_ids: z.array(z.number().int()).optional()

    - employeeUpdateSchema matching OpenAPI EmployeeUpdate (lines 4736-4744):
      All optional: name, email, phone, title, bio, is_active

    - employeeServicesSchema:
      service_ids: z.array(z.number().int())

    - workingHoursCreateSchema matching OpenAPI WorkingHoursCreate (lines 4755-4762):
      z.array(z.object({
        day_of_week: z.number().int().min(0).max(6),
        start_time: z.string().regex(/^\d{2}:\d{2}$/), // HH:MM format
        end_time: z.string().regex(/^\d{2}:\d{2}$/),
        is_active: z.boolean().optional().default(true)
      }))

    - scheduleOverrideSchema matching OpenAPI WorkingHoursOverride (lines 4764-4772):
      date: z.string().date(), // YYYY-MM-DD
      start_time: z.string().regex(/^\d{2}:\d{2}$/).optional(),
      end_time: z.string().regex(/^\d{2}:\d{2}$/).optional(),
      is_day_off: z.boolean().optional().default(false),
      reason: z.string().optional()

    Create `apps/web/app/api/v1/employees/route.ts`:
    Export GET = createRouteHandler:
      requiresAuth: true, requiredPermissions: ['employees.manage']
      - Query employees WHERE companyId AND deletedAt IS NULL
      - Include assigned services via join
      - Return { data: employees } with UUIDs

    Export POST = createRouteHandler:
      bodySchema: employeeCreateSchema, requiresAuth: true, requiredPermissions: ['employees.manage']
      - Insert employee with companyId
      - If service_ids, insert into employee_services junction table
      - Return createdResponse

    Create `apps/web/app/api/v1/employees/[id]/route.ts`:
    - [id] is UUID
    Export GET: fetch by UUID and companyId, include services
    Export PUT: update by UUID and companyId
    Export DELETE: soft delete (SET deletedAt = NOW())

    Create `apps/web/app/api/v1/employees/[id]/services/route.ts`:
    Export PUT = createRouteHandler:
      bodySchema: employeeServicesSchema, requiresAuth: true, requiredPermissions: ['employees.manage']
      - Find employee by UUID and companyId (verify ownership)
      - Delete existing employee_services for employee
      - Insert new service_ids
      - Return { message: 'Services assigned' }

    Create `apps/web/app/api/v1/employees/[id]/working-hours/route.ts`:
    Export GET = createRouteHandler:
      requiresAuth: true, requiredPermissions: ['employees.manage']
      - Find employee by UUID and companyId
      - Query working_hours WHERE employeeId = employee.id
      - Return { data: workingHours }

    Export PUT = createRouteHandler:
      bodySchema: workingHoursCreateSchema, requiresAuth: true, requiredPermissions: ['employees.manage']
      - Find employee by UUID and companyId
      - Delete existing working_hours for employeeId
      - Insert new working hours entries with companyId and employeeId
      - Return { message: 'Working hours updated' }

    Create `apps/web/app/api/v1/employees/[id]/schedule-overrides/route.ts`:
    Export POST = createRouteHandler:
      bodySchema: scheduleOverrideSchema, requiresAuth: true, requiredPermissions: ['employees.manage']
      - Find employee by UUID and companyId
      - Insert into working_hours_overrides: employeeId, date, startTime, endTime, isDayOff, reason, companyId
      - Return createdResponse with override data
  </action>
  <verify>
    Run `cd apps/web && pnpm tsc --noEmit` — compiles without errors.
    Verify employee list includes assigned services.
    Verify service assignment replaces all services atomically (delete + insert).
    Verify working hours PUT replaces all hours for employee.
    Verify schedule override creates per-date exceptions.
    Verify all operations scoped to companyId.
  </verify>
  <done>
    Employee CRUD with service assignment, working hours management, and schedule overrides. All operations enforce tenant isolation and use UUIDs for public-facing IDs.
  </done>
</task>

</tasks>

<verification>
1. `pnpm tsc --noEmit` passes in apps/web
2. Service CRUD with category filtering, soft delete, resource assignment
3. Service category CRUD with sort ordering
4. Employee CRUD with service assignment and soft delete
5. Working hours bulk update (replaces all for employee)
6. Schedule overrides for date-specific exceptions
7. All endpoints check proper permissions
8. All responses use UUIDs, never SERIAL IDs
9. All queries scoped to company_id
</verification>

<success_criteria>
- Service CRUD: list (with filters), create, read, update, soft delete
- Service categories: list, create, update, delete
- Employee CRUD: list (with services), create, read, update, soft delete
- Employee services: assign services
- Working hours: read, bulk update
- Schedule overrides: create
- Tenant isolation on all operations
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-core-services/03-07-SUMMARY.md`
</output>
