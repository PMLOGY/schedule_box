---
phase: 09-loyalty-program
plan: 08
type: execute
wave: 4
depends_on: [04, 05, 06]
files_modified:
  - apps/web/components/loyalty/LoyaltyCardDisplay.tsx
  - apps/web/components/loyalty/TierProgressBar.tsx
  - apps/web/components/loyalty/RewardsCatalog.tsx
  - apps/web/components/loyalty/TransactionHistory.tsx
  - apps/web/components/loyalty/WalletButtons.tsx
autonomous: true

must_haves:
  truths:
    - "Loyalty card display shows points balance, tier name, and card number"
    - "Tier progress bar visually shows progress toward next tier with percentage"
    - "Rewards catalog shows available rewards with points cost and redeem button"
    - "Wallet buttons link to Apple/Google pass download endpoints"
    - "Transaction history shows earn/redeem timeline with running balance"
  artifacts:
    - path: "apps/web/components/loyalty/LoyaltyCardDisplay.tsx"
      provides: "Card component showing points, tier, card number with branded styling"
      exports: ["LoyaltyCardDisplay"]
    - path: "apps/web/components/loyalty/TierProgressBar.tsx"
      provides: "Progress bar component for tier advancement visualization"
      exports: ["TierProgressBar"]
    - path: "apps/web/components/loyalty/RewardsCatalog.tsx"
      provides: "Grid of available rewards with redeem buttons"
      exports: ["RewardsCatalog"]
    - path: "apps/web/components/loyalty/TransactionHistory.tsx"
      provides: "Timeline of points earn/redeem transactions"
      exports: ["TransactionHistory"]
    - path: "apps/web/components/loyalty/WalletButtons.tsx"
      provides: "Add to Apple Wallet / Add to Google Wallet CTA buttons"
      exports: ["WalletButtons"]
  key_links:
    - from: "apps/web/components/loyalty/LoyaltyCardDisplay.tsx"
      to: "packages/shared/src/types/loyalty.ts"
      via: "LoyaltyCard type"
      pattern: "import.*LoyaltyCard.*from"
    - from: "apps/web/components/loyalty/WalletButtons.tsx"
      to: "apps/web/app/api/v1/loyalty/cards/[id]/apple-pass/route.ts"
      via: "link to API endpoint"
      pattern: "api/v1/loyalty/cards"
    - from: "apps/web/components/loyalty/RewardsCatalog.tsx"
      to: "apps/web/hooks/use-loyalty-queries.ts"
      via: "useRedeemReward mutation hook"
      pattern: "useRedeemReward"
---

<objective>
Create customer-facing loyalty UI components for displaying card info, tier progress, rewards, and wallet integration.

Purpose: These components provide the customer view of the loyalty program. They can be used in the customer-facing booking portal, embedded widgets, or the admin dashboard's card detail view. The components fulfill the visual requirements for LOYAL-01 through LOYAL-07.

Output: 5 reusable React components for the loyalty UI.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-loyalty-program/09-04-SUMMARY.md
@.planning/phases/09-loyalty-program/09-06-SUMMARY.md

@packages/shared/src/types/loyalty.ts
@apps/web/hooks/use-loyalty-queries.ts
@apps/web/components/booking/BookingStatusBadge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Loyalty Card Display + Tier Progress Bar</name>
  <files>
    apps/web/components/loyalty/LoyaltyCardDisplay.tsx
    apps/web/components/loyalty/TierProgressBar.tsx
  </files>
  <action>
**LoyaltyCardDisplay** (`apps/web/components/loyalty/LoyaltyCardDisplay.tsx`):

Props: `{ card: LoyaltyCard; showWalletButtons?: boolean; className?: string }`

Create a card-shaped component (styled like a physical loyalty card) showing:
- Card background color based on tier color (use tier.color or default blue #3B82F6)
- Program name (top left)
- Points balance (large, centered): e.g., "1,250 bodů" (Czech locale)
- Stamps balance (if program type is 'stamps'): show filled/empty stamps row
- Current tier name + badge (e.g., "Silver" with tier color)
- Card number (bottom, monospace font): e.g., "SB-A3F2-B8C1-4D5E"
- Customer name (bottom right)
- If showWalletButtons is true, render WalletButtons below the card

Use Tailwind CSS for styling:
- Rounded corners (rounded-2xl)
- Shadow (shadow-lg)
- White text on colored background
- Gradient effect for premium feel
- Responsive: full width on mobile, max-w-md on desktop

**TierProgressBar** (`apps/web/components/loyalty/TierProgressBar.tsx`):

Props: `{ currentTier: LoyaltyTier | null; nextTier: { name: string; minPoints: number; pointsNeeded: number } | null; currentPoints: number; className?: string }`

Create a progress bar showing advancement toward next tier:
- If no nextTier (already at max tier): show "Highest tier reached!" message
- Otherwise:
  - Label: "[currentTier.name] → [nextTier.name]"
  - Progress bar: filled portion = (currentPoints - currentTier.minPoints) / (nextTier.minPoints - currentTier.minPoints) * 100
  - Below bar: "[pointsNeeded] bodů do [nextTier.name]"
  - Tier milestones as dots on the progress bar (if multiple tiers exist)

Use shadcn/ui Progress component or Tailwind CSS custom progress bar.
- Color: current tier color for filled portion
- Height: h-3 or h-4
- Animated fill transition
  </action>
  <verify>
Run `cd D:/Project/ScheduleBox && pnpm exec tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20` to check for TypeScript errors.
  </verify>
  <done>LoyaltyCardDisplay renders as a branded card with points/tier info. TierProgressBar shows visual advancement progress.</done>
</task>

<task type="auto">
  <name>Task 2: Rewards Catalog + Wallet Buttons + Transaction History</name>
  <files>
    apps/web/components/loyalty/RewardsCatalog.tsx
    apps/web/components/loyalty/WalletButtons.tsx
    apps/web/components/loyalty/TransactionHistory.tsx
  </files>
  <action>
**RewardsCatalog** (`apps/web/components/loyalty/RewardsCatalog.tsx`):

Props: `{ rewards: Reward[]; cardPointsBalance: number; cardUuid: string; onRedeem?: (rewardId: number) => void; className?: string }`

Create a grid of reward cards:
- Each reward card shows:
  - Reward name (bold)
  - Description (truncated to 2 lines)
  - Points cost (large, colored): e.g., "500 bodů"
  - Reward type badge (discount_percentage -> "Sleva %", discount_fixed -> "Sleva Kč", free_service -> "Služba zdarma", gift -> "Dárek")
  - Availability indicator: if maxRedemptions, show "X/Y redeemed"
  - "Vyměnit" (Redeem) button:
    - Enabled if cardPointsBalance >= pointsCost AND reward is available
    - Disabled with tooltip if insufficient points
    - Uses `useRedeemReward()` mutation hook on click
- Responsive grid: 1 col mobile, 2 cols tablet, 3 cols desktop
- Empty state: "Zatím nejsou k dispozici žádné odměny" (No rewards available yet)

Use shadcn/ui: Card, Button, Badge, Tooltip

**WalletButtons** (`apps/web/components/loyalty/WalletButtons.tsx`):

Props: `{ cardUuid: string; applePassUrl?: string | null; googlePassUrl?: string | null; className?: string }`

Create two wallet buttons:
- "Add to Apple Wallet" button:
  - Links to `/api/v1/loyalty/cards/${cardUuid}/apple-pass` (direct download)
  - Apple Wallet badge/icon styling (black button with Apple logo text)
  - Render as `<a>` tag for direct download
- "Add to Google Wallet" button:
  - First call `/api/v1/loyalty/cards/${cardUuid}/google-pass` to get saveUrl
  - Then redirect to saveUrl
  - Google Wallet badge styling
  - Render as button that fetches then redirects
- Stack vertically on mobile, side-by-side on desktop
- Handle loading state during Google pass URL generation

**TransactionHistory** (`apps/web/components/loyalty/TransactionHistory.tsx`):

Props: `{ cardUuid: string; className?: string }`

Create a transaction history timeline:
- Use `useTransactions(cardUuid)` hook for data fetching
- Each transaction shows:
  - Date/time (formatted for Czech locale: "11. 2. 2026 14:30")
  - Type badge: earn (green), redeem (red), adjust (yellow), expire (gray), stamp (blue)
  - Points change: "+100" (green) or "-500" (red)
  - Balance after: "Zůstatek: 1,250"
  - Description text
- Paginated: show "Načíst další" (Load more) button
- Loading state: skeleton rows
- Empty state: "Zatím žádné transakce" (No transactions yet)

Use shadcn/ui: Card, Badge, Button, Skeleton
  </action>
  <verify>
Run `cd D:/Project/ScheduleBox && pnpm exec tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20` to check for TypeScript errors.
  </verify>
  <done>RewardsCatalog shows redeemable rewards with availability. WalletButtons provide Apple/Google Wallet integration. TransactionHistory shows paginated points timeline.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors in apps/web
- All 5 components export properly from their files
- Components use LoyaltyCard, Reward, LoyaltyTransaction types from @schedulebox/shared
- Wallet buttons correctly link to API endpoints
- Czech locale text used for customer-facing labels
</verification>

<success_criteria>
Customer-facing loyalty UI provides visual card display, tier progress tracking, rewards browsing with redemption, wallet integration buttons, and transaction history. Components are reusable across admin and customer views.
</success_criteria>

<output>
After completion, create `.planning/phases/09-loyalty-program/09-08-SUMMARY.md`
</output>
