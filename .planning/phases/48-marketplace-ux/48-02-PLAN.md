---
phase: 48-marketplace-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/[locale]/[company_slug]/page.tsx
autonomous: true
requirements: [MKT-03, MKT-04]

must_haves:
  truths:
    - "Firm detail page shows a photo gallery section when images are available"
    - "Firm detail page shows an embedded map with the business location"
    - "Firm detail page shows a Featured badge for AI-Powered tier businesses"
    - "Firm detail page shows enhanced reviews section with average rating and review count"
    - "Book Now button links directly to the booking wizard at /{slug}/book"
  artifacts:
    - path: "apps/web/app/[locale]/[company_slug]/page.tsx"
      provides: "Photo gallery, map embed, featured badge, enhanced reviews, Book Now link"
      min_lines: 100
  key_links:
    - from: "apps/web/app/[locale]/[company_slug]/page.tsx"
      to: "/{slug}/book"
      via: "Book Now href"
      pattern: "/book"
---

<objective>
Enhance the firm detail page with photo gallery, embedded map, featured badge, and enhanced reviews display.

Purpose: When a customer clicks on a marketplace listing, they see a rich business profile with photos, location map, reviews, and a clear path to book.
Output: Enhanced `/{locale}/{slug}` page with all MKT-03/MKT-04 requirements met.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-marketplace-ux/48-CONTEXT.md
@.planning/phases/48-marketplace-ux/48-RESEARCH.md

@apps/web/app/[locale]/[company_slug]/page.tsx

<interfaces>
<!-- The firm detail page is a server component that fetches company data directly from the DB.
     The marketplace_listings table has these relevant columns: -->

From packages/database/src/schema/marketplace.ts:
- images: text('images').array() — array of image URLs (may be empty or null)
- latitude: numeric('latitude', { precision: 10, scale: 7 })
- longitude: numeric('longitude', { precision: 10, scale: 7 })
- featured: boolean('featured').default(false)
- averageRating: numeric('average_rating', { precision: 3, scale: 2 })
- reviewCount: integer('review_count').default(0)
- addressStreet, addressCity, addressZip — address fields

The page currently renders: company hero (name, description), services grid, reviews list, JSON-LD structured data.
It fetches the company by slug and joins with marketplace_listings data.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add photo gallery, map embed, featured badge, and enhanced reviews to firm detail page</name>
  <files>apps/web/app/[locale]/[company_slug]/page.tsx</files>
  <action>
Enhance the existing server component `/{locale}/{slug}/page.tsx`:

1. **Photo gallery section** (below hero, above services):
   - Fetch `images` array from the marketplace listing (already joined or query separately)
   - If images exist and are non-empty: render a horizontal scrollable gallery with `overflow-x-auto snap-x`
   - Each image: `<img>` tag with `rounded-xl` class, `loading="lazy"`, aspect-ratio container
   - If no images: skip the gallery section entirely (no placeholder)
   - Use `isomorphic-dompurify` to sanitize any image URLs if sourced from user input (Phase 46 mandate)

2. **Featured badge** (in hero section):
   - If `listing.featured === true`, show a "Featured" badge next to the company name
   - Use glass-accent styling: `bg-gradient-to-r from-blue-500/20 to-purple-500/20 backdrop-blur-sm border border-white/20 text-white px-3 py-1 rounded-full text-sm font-medium`
   - Badge text: translated "Featured" / "Doporuceno"

3. **Map embed section** (below services, above reviews):
   - If `listing.latitude && listing.longitude` are available:
   - Render an OpenStreetMap iframe embed:
     ```
     <iframe
       title="{company.name} location"
       src={`https://www.openstreetmap.org/export/embed.html?bbox=${lng-0.01},${lat-0.01},${lng+0.01},${lat+0.01}&layer=mapnik&marker=${lat},${lng}`}
       className="w-full h-48 rounded-xl border border-white/20"
       loading="lazy"
       referrerPolicy="no-referrer"
     />
     ```
   - Section heading: translated "Location" / "Poloha"
   - If no coordinates: skip map section

4. **Enhanced reviews section**:
   - Show average rating prominently with star icons (filled/empty using lucide-react Star)
   - Show review count: "4.7 (23 reviews)" / "4.7 (23 recenzi)"
   - Keep existing review list rendering but add star ratings to individual reviews if not already shown

5. **Book Now button** (confirm/fix):
   - Verify the "Book Now" / "Rezervovat" button links to `/${locale}/${slug}/book`
   - If button does not exist or links incorrectly, add/fix it
   - Style as primary CTA: `bg-gradient-to-r from-blue-600 to-blue-500 hover:from-blue-700 hover:to-blue-600 text-white px-6 py-3 rounded-xl font-semibold`
   - Place prominently in the hero section and optionally as a sticky footer on mobile

6. **DOMPurify sanitization**:
   - Wrap `company.description` and any user-generated text in `sanitizeHtml()` from the project's existing sanitization utility before rendering
   - This is a Phase 46 mandate for all user-generated content

Add Czech and English translations for new text: "Featured", "Location", "reviews", "Book Now" etc.
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Firm detail page shows photo gallery (when images exist), OpenStreetMap embed (when coordinates exist), Featured badge (when featured), enhanced reviews with star ratings, and a working Book Now button linking to /{slug}/book</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Navigate to a firm detail page — photo gallery renders if images present
- Map embed shows if coordinates present
- Featured badge appears for featured businesses
- Reviews show star rating average and count
- "Book Now" button links to /{slug}/book
</verification>

<success_criteria>
- Firm detail page renders description, photos, reviews, services, and map
- Book Now button links directly to booking wizard
- Featured badge visible for AI-Powered tier businesses
- All user-generated content is DOMPurify-sanitized
</success_criteria>

<output>
After completion, create `.planning/phases/48-marketplace-ux/48-02-SUMMARY.md`
</output>
