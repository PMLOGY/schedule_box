---
phase: 48-marketplace-ux
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/[locale]/(dashboard)/settings/video-meetings/page.tsx
  - apps/web/app/api/v1/settings/video-meeting-url/route.ts
  - apps/web/lib/navigation.ts
autonomous: true
requirements: [UX-04]

must_haves:
  truths:
    - "Owners can navigate to Settings > Video Meetings from the sidebar"
    - "Owners can paste a custom video meeting URL and save it"
    - "The saved URL persists across page reloads"
    - "Video meeting link is displayed and editable on the settings page"
  artifacts:
    - path: "apps/web/app/[locale]/(dashboard)/settings/video-meetings/page.tsx"
      provides: "Video meetings settings page with URL input form"
      min_lines: 40
    - path: "apps/web/app/api/v1/settings/video-meeting-url/route.ts"
      provides: "GET/PATCH endpoint for custom meeting URL"
      exports: ["GET", "PATCH"]
  key_links:
    - from: "apps/web/app/[locale]/(dashboard)/settings/video-meetings/page.tsx"
      to: "/api/v1/settings/video-meeting-url"
      via: "TanStack Query fetch + mutation"
      pattern: "video-meeting-url"
    - from: "apps/web/lib/navigation.ts"
      to: "settings/video-meetings"
      via: "Sidebar nav link"
      pattern: "video-meetings"
---

<objective>
Create the Video Meetings settings page where owners can paste a custom video meeting URL (Zoom, Teams, Meet, etc.) to attach to bookings.

Purpose: Owners who offer video consultations can set a meeting link once, and it gets included in booking confirmation emails.
Output: New settings sub-page at Settings > Video Meetings with URL input, save, and display.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-marketplace-ux/48-CONTEXT.md
@.planning/phases/48-marketplace-ux/48-RESEARCH.md

@apps/web/lib/navigation.ts
@apps/web/app/[locale]/(dashboard)/settings/page.tsx

<interfaces>
<!-- Research recommendation: add custom_meeting_url to companies table
     rather than using the existing video_meetings table (provider CHECK constraint issue).
     Simplest approach: store as a company-level setting. -->

From research:
- video_meetings table has CHECK (provider IN ('zoom', 'google_meet', 'ms_teams')) — cannot use 'custom'
- Recommended: add custom_meeting_url VARCHAR(500) column to companies table
- Or: store in company settings as a simple key-value (if such a pattern exists)
- Settings page pattern: see apps/web/app/[locale]/(dashboard)/settings/billing/ for structure reference

From navigation:
- Sidebar has Settings section with sub-pages
- Need to add "Video Meetings" link with Video icon (lucide-react)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add custom_meeting_url to companies + API endpoint + settings page + nav link</name>
  <files>packages/database/src/schema/companies.ts, apps/web/app/api/v1/settings/video-meeting-url/route.ts, apps/web/app/[locale]/(dashboard)/settings/video-meetings/page.tsx, apps/web/lib/navigation.ts</files>
  <action>
1. **DB schema change** — In `packages/database/src/schema/companies.ts` (or wherever the companies table is defined):
   - Add column: `customMeetingUrl: varchar('custom_meeting_url', { length: 500 })`
   - Run `pnpm --filter @schedulebox/database db:push` to apply (or note that drizzle-kit push will handle it)

2. **API endpoint** — Create `apps/web/app/api/v1/settings/video-meeting-url/route.ts`:
   - **GET**: Authenticated, returns `{ custom_meeting_url: string | null }` for the current user's company
   - **PATCH**: Authenticated, accepts `{ custom_meeting_url: string }` (Zod validated: `.url().max(500).or(z.literal(''))` — empty string clears it), updates the company record
   - Use existing `createRouteHandler` with `requiresAuth: true`
   - Get companyId from the authenticated session/token
   - Return 200 with the updated value

3. **Settings page** — Create `apps/web/app/[locale]/(dashboard)/settings/video-meetings/page.tsx`:
   - Page title: "Video Meetings" / "Video schuzky"
   - Description text: "Paste your video meeting link (Zoom, Teams, Meet, etc.) to include it in booking confirmation emails."
   - URL input field with label, placeholder "https://zoom.us/j/..."
   - Save button (submit via useMutation to PATCH endpoint)
   - Show current saved URL with a "Copy" button (navigator.clipboard)
   - Show upcoming bookings that will include this link (query from bookings with future dates — optional, can be a simple info text "This link will be included in all future booking confirmation emails")
   - Success toast on save (sonner)
   - Glass Card styling consistent with other settings pages
   - Use useTranslations for all text, add en/cs keys

4. **Navigation** — In `apps/web/lib/navigation.ts`:
   - Add "Video Meetings" link under Settings section
   - Icon: `Video` from lucide-react
   - Path: `settings/video-meetings`
   - Label translated: "Video Meetings" / "Video schuzky"
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Settings > Video Meetings page accessible from sidebar. Owner can paste URL, save, and see it persist. API endpoint returns and updates the custom meeting URL.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Sidebar shows "Video Meetings" under Settings
- Navigate to Settings > Video Meetings — page renders
- Paste a URL, click Save — success toast appears
- Reload page — saved URL still displayed
- Clear URL, save — URL cleared
</verification>

<success_criteria>
- Video Meetings settings page renders and is navigable from sidebar
- Custom video meeting URL can be saved and persists
- URL is validated as a proper URL
- Glass UI styling matches other settings pages
</success_criteria>

<output>
After completion, create `.planning/phases/48-marketplace-ux/48-04-SUMMARY.md`
</output>
