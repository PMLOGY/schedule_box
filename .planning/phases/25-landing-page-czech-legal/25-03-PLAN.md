---
phase: 25-landing-page-czech-legal
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - apps/web/app/[locale]/(marketing)/pricing/page.tsx
  - apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx
  - apps/web/app/[locale]/(marketing)/_components/social-proof.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /pricing shows three pricing tiers (Free / CZK 299 / CZK 699) with feature lists"
    - "Annual/monthly toggle shows discounted annual prices"
    - "Each pricing card has a Zacit zdarma CTA button linking to /register"
    - "Social proof section exists with placeholder content for testimonials"
  artifacts:
    - path: "apps/web/app/[locale]/(marketing)/pricing/page.tsx"
      provides: "Pricing page composing pricing table and social proof"
      contains: "setRequestLocale"
    - path: "apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx"
      provides: "3-tier pricing cards with annual toggle"
      contains: "use client"
    - path: "apps/web/app/[locale]/(marketing)/_components/social-proof.tsx"
      provides: "Testimonial/social proof section with placeholders"
      contains: "landing.social"
  key_links:
    - from: "apps/web/app/[locale]/(marketing)/pricing/page.tsx"
      to: "apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx"
      via: "import PricingTable"
      pattern: "PricingTable"
    - from: "apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx"
      to: "/register"
      via: "CTA button Link href"
      pattern: "href.*register"
---

<objective>
Build the pricing page with three-tier pricing cards (Free / CZK 299 / CZK 699), annual discount toggle, and a social proof section with placeholder testimonials.

Purpose: Clear pricing is the #1 conversion driver for SaaS. Czech SMB owners need to see prices in CZK with no ambiguity. The annual toggle incentivizes commitment. Social proof builds trust even with placeholder content initially.

Output: Complete /pricing page with interactive pricing table and social proof section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-landing-page-czech-legal/25-RESEARCH.md
@.planning/phases/25-landing-page-czech-legal/25-01-SUMMARY.md

@apps/web/app/[locale]/(marketing)/layout.tsx
@apps/web/messages/cs.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pricing table with 3 tiers and annual toggle</name>
  <files>
    apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx
  </files>
  <action>
1. Create `apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx`:
   - This IS a 'use client' component (needs state for annual/monthly toggle)
   - Import `useTranslations` from `next-intl` (client hook)
   - Import `Link` from `@/lib/i18n/navigation`
   - Import `Button` from `@/components/ui/button`, `Card`/`CardContent`/`CardHeader`/`CardFooter` from `@/components/ui/card`, `Badge` from `@/components/ui/badge`
   - Import `Check` icon from `lucide-react`

2. Define pricing plans as a constant array (NOT fetched from DB — static marketing content):
   ```typescript
   const PLANS = [
     {
       id: 'free',
       nameKey: 'free',
       price: 0,
       annualPrice: 0,
       featured: false,
       features: ['onlineBooking', 'basicCalendar', 'emailNotifications', 'oneStaff', 'fiftyBookings'],
     },
     {
       id: 'pro',
       nameKey: 'pro',
       price: 299,
       annualPrice: 249,
       featured: true, // "Nejoblibenejsi" badge
       features: ['everythingFree', 'smsReminders', 'payments', 'crm', 'fiveStaff', 'unlimitedBookings'],
     },
     {
       id: 'business',
       nameKey: 'business',
       price: 699,
       annualPrice: 579,
       featured: false,
       features: ['everythingPro', 'aiPredictions', 'dynamicPricing', 'apiAccess', 'unlimitedStaff', 'prioritySupport'],
     },
   ] as const;
   ```

3. Component state: `const [isAnnual, setIsAnnual] = useState(false)`

4. Toggle UI: Centered monthly/annual toggle above the cards. Use two buttons or a switch-style toggle. When annual is selected, show the `t('annualSaving')` ("Usetrite 2 mesice") badge.

5. Pricing cards layout: `grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto`

6. Each card:
   - Header: plan name from `t('pricing.' + plan.nameKey + '.name')`, description from `t('pricing.' + plan.nameKey + '.description')`
   - If `plan.featured`: add `ring-2 ring-primary` border and a "Nejoblibenejsi" badge
   - Price display: Show `isAnnual ? plan.annualPrice : plan.price` formatted as e.g. "299 Kc/mes" using `t('currency')`
   - For free tier: show "Zdarma" instead of "0 Kc/mes"
   - Feature list: Render each feature with a Check icon and translated text from `t('pricing.features.' + featureKey)`
   - CTA button: `t('cta')` ("Zacit zdarma") linking to `/register` — use `Button` with `asChild` wrapping `Link`
   - Featured card CTA uses default (primary) variant; others use outline variant

7. Add feature translation keys to the i18n consideration. Since plan 25-01 creates the landing namespace, include a note: if `landing.pricing.features.*` keys are missing from cs.json, add them. The feature keys should include: onlineBooking, basicCalendar, emailNotifications, oneStaff, fiftyBookings, everythingFree, smsReminders, payments, crm, fiveStaff, unlimitedBookings, everythingPro, aiPredictions, dynamicPricing, apiAccess, unlimitedStaff, prioritySupport.

   If these keys don't exist in cs.json yet, ADD them under `landing.pricing.features`:
   ```json
   "features": {
     "onlineBooking": "Online rezervace 24/7",
     "basicCalendar": "Zakladni kalendar",
     "emailNotifications": "Emailove notifikace",
     "oneStaff": "1 zamestnanec",
     "fiftyBookings": "50 rezervaci/mesic",
     "everythingFree": "Vse z Free",
     "smsReminders": "SMS pripomenuti",
     "payments": "Online platby",
     "crm": "CRM a verni zakaznici",
     "fiveStaff": "5 zamestnancu",
     "unlimitedBookings": "Neomezene rezervace",
     "everythingPro": "Vse z Pro",
     "aiPredictions": "AI predikce a doporuceni",
     "dynamicPricing": "Dynamicke ceny",
     "apiAccess": "API pristup",
     "unlimitedStaff": "Neomezene zamestnancu",
     "prioritySupport": "Prioritni podpora"
   }
   ```
   Add equivalent keys in en.json and sk.json as well.

IMPORTANT: All text comes from i18n translations (not hardcoded Czech strings). The component uses `useTranslations` hook.
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/web exec tsc --noEmit`. Verify the component has 'use client' and uses `useState`: `grep -c "use client" apps/web/app/\\[locale\\]/\\(marketing\\)/_components/pricing-table.tsx` should be 1. Verify all three pricing tiers are defined: `grep -c "id:" apps/web/app/\\[locale\\]/\\(marketing\\)/_components/pricing-table.tsx` should be 3.
  </verify>
  <done>
    PricingTable component renders 3 pricing cards (Free/Pro/Business) with monthly (0/299/699 CZK) and annual (0/249/579 CZK) toggle. Pro card is visually highlighted with ring and badge. Each card has feature list with check icons and CTA linking to /register. All text is i18n-translated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create social proof section and pricing page</name>
  <files>
    apps/web/app/[locale]/(marketing)/_components/social-proof.tsx
    apps/web/app/[locale]/(marketing)/pricing/page.tsx
  </files>
  <action>
1. Create `apps/web/app/[locale]/(marketing)/_components/social-proof.tsx`:
   - This is a SERVER component (static content, no interactivity)
   - Use `getTranslations('landing.social')` from `next-intl/server`
   - Section title: `t('title')` ("Firmy, ktere nam duveri")
   - Render 3 placeholder testimonial cards in a grid (`grid-cols-1 md:grid-cols-3 gap-6`)
   - Each card: placeholder avatar (gray circle with initials), placeholder name, placeholder company, placeholder quote text, star rating (5 stars using Star icon from lucide-react)
   - Use realistic Czech placeholder content:
     - "Jana N., Salon Krasa Praha" — "ScheduleBox nam usetril hodiny prace s rezervacemi. Zakaznici si rezervuji sami a my se muzeme soustreditna svou praci."
     - "Martin V., FitZone Gym" — "Diky AI predikcim jsme snizili absence o 35 %. Nejlepsi investice do naseho fitka."
     - "Petra K., Masazni studio Relax" — "Konecne system, ktery rozumi ceskemu trhu. Platby pres Comgate funguji bezchybne."
   - Below the cards: `t('placeholder')` note in small muted text: "Recenze zakazniku budou doplneny pred spustenim."
   - Add a `// TODO: Replace placeholder testimonials with real customer reviews before launch` comment

2. Create `apps/web/app/[locale]/(marketing)/pricing/page.tsx`:
   - This is an async SERVER component
   - Import and call `setRequestLocale(locale)` as the FIRST operation
   - Export `generateStaticParams` returning `routing.locales.map((locale) => ({ locale }))`
   - Export `generateMetadata` with `getTranslations({ locale, namespace: 'landing.meta' })` — title: `t('pricingTitle')`, description about pricing
   - Page structure:
     - Section header: `t('pricing.title')` ("Transparentni ceny bez prekvapeni") centered
     - `<PricingTable />` component
     - `<SocialProof />` component below with some vertical spacing
   - Wrap in max-w-6xl centered container with vertical padding
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/web exec tsc --noEmit`. Verify pricing page has generateStaticParams: `grep "generateStaticParams" apps/web/app/\\[locale\\]/\\(marketing\\)/pricing/page.tsx`. Verify social proof component exists: `ls apps/web/app/\\[locale\\]/\\(marketing\\)/_components/social-proof.tsx`.
  </verify>
  <done>
    Pricing page at /pricing renders a section header, 3-tier pricing table with annual toggle, and social proof section with 3 placeholder testimonials. Page has SEO metadata, generateStaticParams, and setRequestLocale for static rendering. Social proof has TODO comment for real content replacement.
  </done>
</task>

</tasks>

<verification>
1. Visiting /pricing shows three pricing cards (Free / 299 CZK / 699 CZK)
2. Clicking annual toggle shows discounted prices (0 / 249 / 579 CZK)
3. Pro card is visually highlighted as "Nejoblibenejsi"
4. Each card has a feature list and "Zacit zdarma" CTA linking to /register
5. Social proof section shows 3 placeholder testimonials with star ratings
6. Pricing page has proper SEO metadata
7. TypeScript compiles without errors
</verification>

<success_criteria>
- Three pricing tiers (Free / CZK 299 / CZK 699) displayed with feature lists
- Annual/monthly toggle shows discounted annual prices (save 2 months)
- Pro tier visually highlighted as most popular
- All CTAs link to /register
- Social proof section present with placeholder testimonials
- Page statically rendered with setRequestLocale + generateStaticParams
</success_criteria>

<output>
After completion, create `.planning/phases/25-landing-page-czech-legal/25-03-SUMMARY.md`
</output>
