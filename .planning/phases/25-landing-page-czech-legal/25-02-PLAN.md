---
phase: 25-landing-page-czech-legal
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - apps/web/app/[locale]/(marketing)/page.tsx
  - apps/web/app/[locale]/(marketing)/_components/hero-section.tsx
  - apps/web/app/[locale]/(marketing)/_components/live-widget-preview.tsx
  - apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx
  - apps/web/app/[locale]/(marketing)/_components/trust-badges.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor on / sees a Czech-language hero section with headline, subheadline, and Zacit zdarma CTA"
    - "Hero section contains a live embedded booking widget demo in an iframe (not a screenshot)"
    - "Below the fold, 6 feature cards with icons are visible in a responsive grid"
    - "Trust badge row shows GDPR, Czech hosting, Comgate, and bank-level security badges"
    - "Lighthouse performance score is above 90 via server-component hero section with CSS-only animations and below-fold Motion FeatureGrid client component"
  artifacts:
    - path: "apps/web/app/[locale]/(marketing)/page.tsx"
      provides: "Marketing home page composing hero, features, and trust sections"
      contains: "setRequestLocale"
    - path: "apps/web/app/[locale]/(marketing)/_components/hero-section.tsx"
      provides: "Hero section with headline, CTA, and widget embed"
      contains: "Zacit zdarma|landing.hero"
    - path: "apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx"
      provides: "6-card feature grid with Motion scroll animations"
      contains: "motion/react"
    - path: "apps/web/app/[locale]/(marketing)/_components/trust-badges.tsx"
      provides: "GDPR, hosting, payment, security trust badge row"
      contains: "landing.trust"
  key_links:
    - from: "apps/web/app/[locale]/(marketing)/page.tsx"
      to: "apps/web/app/[locale]/(marketing)/_components/hero-section.tsx"
      via: "import HeroSection"
      pattern: "HeroSection"
    - from: "apps/web/app/[locale]/(marketing)/_components/hero-section.tsx"
      to: "/embed/salon-krasa"
      via: "iframe src pointing to embed widget"
      pattern: "iframe.*embed"
    - from: "apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx"
      to: "motion/react"
      via: "Motion stagger animation"
      pattern: "import.*motion.*motion/react"
---

<objective>
Build the marketing home page with a Czech-language hero section containing a live booking widget demo, a 6-card feature grid with scroll animations, and a trust badge row.

Purpose: This is the primary landing experience for Czech SMB visitors. The live widget demo differentiates ScheduleBox from competitors who only show screenshots. The feature grid and trust badges build credibility.

Output: Complete marketing home page at / with hero, features, and trust sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-landing-page-czech-legal/25-RESEARCH.md
@.planning/phases/25-landing-page-czech-legal/25-01-SUMMARY.md

@apps/web/app/[locale]/(marketing)/layout.tsx
@apps/web/app/embed/[company_slug]/page.tsx
@apps/web/messages/cs.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hero section with live widget embed</name>
  <files>
    apps/web/app/[locale]/(marketing)/_components/hero-section.tsx
    apps/web/app/[locale]/(marketing)/_components/live-widget-preview.tsx
  </files>
  <action>
1. Create `apps/web/app/[locale]/(marketing)/_components/hero-section.tsx`:
   - This is an async SERVER component (NOT 'use client') — critical for Lighthouse >90
   - Use `getTranslations('landing.hero')` from `next-intl/server`
   - Use `Link` from `@/lib/i18n/navigation`
   - Import `Button` from `@/components/ui/button`
   - Layout: Two-column grid on desktop (`lg:grid-cols-2`), stacked on mobile
   - Left column:
     - Optional badge/pill: `t('badge')` in a small rounded badge (e.g., `<span className="inline-flex items-center rounded-full bg-primary/10 px-3 py-1 text-sm font-medium text-primary">`)
     - Headline: `t('headline')` in `text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight`
     - Subheadline: `t('subheadline')` in `text-lg text-muted-foreground mt-4`
     - CTA row: Primary button `t('cta')` ("Zacit zdarma") linking to `/register`, secondary outline button `t('ctaSecondary')` ("Zobrazit demo") as anchor to `#demo`
   - Right column: `<LiveWidgetPreview />` component
   - Section wrapper: `<section className="py-16 md:py-24 lg:py-32">` with max-w-6xl centered container
   - For hero entrance animations, use CSS only (tailwindcss-animate is already installed) — NOT Motion. This keeps client JS off the critical path. Example: `animate-in fade-in slide-in-from-bottom-4 duration-700` classes from tailwindcss-animate.

2. Create `apps/web/app/[locale]/(marketing)/_components/live-widget-preview.tsx`:
   - This is a SERVER component (iframe does not need client JS)
   - Renders the live booking widget in an iframe
   - Use the existing seed company slug `salon-krasa` (confirmed in database seeds) — NOT `schedulebox-demo` which doesn't exist
   - iframe src: `/embed/salon-krasa?locale=cs&theme=light`
   - Wrap in a decorative container: `<div className="rounded-xl border shadow-2xl overflow-hidden bg-white">` with a fixed height of ~480px
   - Add a "browser chrome" mock at top: a small gray bar with 3 dots to make it look like a browser window preview
   - Set `title="Ukazka rezervacniho widgetu"` for accessibility
   - Use `loading="lazy"` on the iframe — the widget is in the right column, and on mobile it's below the fold
   - Add `id="demo"` to the section wrapper so the "Zobrazit demo" button can anchor-link to it

IMPORTANT: Do NOT import Motion in the hero section. Use tailwindcss-animate CSS classes for entrance animations to protect Lighthouse >90 score (per research Pitfall 7).

IMPORTANT: The embed route fetches from the database. In development, ensure seeds are run (`pnpm --filter @schedulebox/database db:setup`). In production/build, the iframe loads client-side so no SSR database call is needed from the marketing page.
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/web exec tsc --noEmit` to verify no TypeScript errors. Verify the hero section file does NOT contain 'use client' or 'motion/react' imports (to protect Lighthouse score): `grep -c "use client\|motion/react" apps/web/app/\\[locale\\]/\\(marketing\\)/_components/hero-section.tsx` should return 0.
  </verify>
  <done>
    Hero section renders headline, subheadline, CTA buttons, and a live widget embed via iframe pointing at `/embed/salon-krasa`. Hero section is a server component with CSS-only animations. LiveWidgetPreview is a server component rendering an iframe.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create feature grid with Motion animations, trust badges, and home page</name>
  <files>
    apps/web/app/[locale]/(marketing)/page.tsx
    apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx
    apps/web/app/[locale]/(marketing)/_components/trust-badges.tsx
  </files>
  <action>
1. Create `apps/web/app/[locale]/(marketing)/_components/feature-grid.tsx`:
   - This IS a 'use client' component — Motion requires it
   - `import { motion } from 'motion/react'` (NOT framer-motion)
   - `import { useTranslations } from 'next-intl'` (client-side hook)
   - Import icons from `lucide-react`: Brain (AI), Calendar, Bell, CreditCard, Users, BarChart3
   - Define 6 features array with icon, translationKey pairs:
     - { icon: Brain, key: 'aiPrediction' }
     - { icon: Calendar, key: 'onlineBooking' }
     - { icon: Bell, key: 'notifications' }
     - { icon: CreditCard, key: 'payments' }
     - { icon: Users, key: 'crm' }
     - { icon: BarChart3, key: 'analytics' }
   - Use `Card` from `@/components/ui/card` for each feature card
   - Animation: Use Motion variants with staggerChildren pattern from research:
     ```
     const container = { hidden: { opacity: 0 }, show: { opacity: 1, transition: { staggerChildren: 0.1 } } }
     const item = { hidden: { opacity: 0, y: 20 }, show: { opacity: 1, y: 0 } }
     ```
   - Apply `whileInView="show"` with `viewport={{ once: true }}` so animation plays once on scroll
   - Grid: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
   - Each card: icon (48x48, text-primary), feature name (`t(key)`), feature description (`t(key + 'Desc')`)
   - Section wrapper with id="features" so navbar anchor link works

2. Create `apps/web/app/[locale]/(marketing)/_components/trust-badges.tsx`:
   - This is a SERVER component (no animations needed, simple badges)
   - Use `getTranslations('landing.trust')` from `next-intl/server`
   - Import icons from `lucide-react`: Shield (GDPR), Server (Czech hosting), CreditCard (Comgate), Lock (security)
   - Render 4 badges in a horizontal row (`flex flex-wrap justify-center gap-8`)
   - Each badge: icon + text label, muted styling (`text-muted-foreground text-sm`)
   - Center the row, add top/bottom padding

3. Create `apps/web/app/[locale]/(marketing)/page.tsx`:
   - This is an async SERVER component (the page itself)
   - The function receives `{ params }` where params is `Promise<{ locale: string }>`
   - **Await params first:** `const { locale } = await params;`
   - Call `setRequestLocale(locale)` as the FIRST operation after awaiting params
   - Export `generateStaticParams` returning `routing.locales.map((locale) => ({ locale }))`
   - Export `generateMetadata` using `getTranslations({ locale, namespace: 'landing.meta' })` — set title, description, openGraph with cs_CZ locale, alternates with canonical URL. Note: `generateMetadata` also receives `{ params }` as `Promise<{ locale: string }>` — await it there too.
   - Compose the page: `<HeroSection />` then `<FeatureGrid />` then `<TrustBadges />`
   - The page.tsx does NOT use 'use client' — it composes server and client components

IMPORTANT: The FeatureGrid is the ONLY 'use client' component on the home page. It's below the fold, so Motion JS doesn't block initial render. The hero section and trust badges are server components.
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/web exec tsc --noEmit`. Verify FeatureGrid has 'use client' and imports motion/react: `grep "use client" apps/web/app/\\[locale\\]/\\(marketing\\)/_components/feature-grid.tsx`. Verify page.tsx has generateStaticParams: `grep "generateStaticParams" apps/web/app/\\[locale\\]/\\(marketing\\)/page.tsx`. Verify page.tsx does NOT have 'use client': `grep -c "use client" apps/web/app/\\[locale\\]/\\(marketing\\)/page.tsx` should be 0.
  </verify>
  <done>
    Marketing home page at / renders hero section (server component with live widget embed), feature grid (client component with Motion stagger animations, 6 cards with lucide icons), and trust badges (server component with 4 badges). Page has SEO metadata, generateStaticParams, and setRequestLocale for static rendering. Only the below-fold FeatureGrid is a client component.
  </done>
</task>

</tasks>

<verification>
1. Visiting / shows hero section with Czech headline, CTA, and live widget iframe
2. Widget iframe loads from `/embed/salon-krasa?locale=cs&theme=light`
3. Scrolling down reveals 6 feature cards with staggered fade-in animation
4. Trust badge row shows 4 badges (GDPR, Czech hosting, Comgate, bank security)
5. Page has proper SEO metadata (title, description, openGraph)
6. Hero section is a server component (no 'use client', no Motion import)
7. Only FeatureGrid uses 'use client' and Motion (below fold)
8. TypeScript compiles without errors
</verification>

<success_criteria>
- Hero section shows Czech headline, subheadline, "Zacit zdarma" CTA, and live widget demo iframe
- Feature grid displays 6 cards with icons and Motion scroll animations
- Trust badges row shows GDPR, Czech hosting, Comgate, bank-level security
- Page uses setRequestLocale + generateStaticParams for static rendering (with proper await params)
- No above-fold client JS (hero uses CSS animations, not Motion)
- SEO metadata present with Czech language OpenGraph tags
</success_criteria>

<output>
After completion, create `.planning/phases/25-landing-page-czech-legal/25-02-SUMMARY.md`
</output>
