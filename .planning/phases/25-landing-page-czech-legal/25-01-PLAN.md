---
phase: 25-landing-page-czech-legal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/[locale]/(marketing)/layout.tsx
  - apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx
  - apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx
  - apps/web/messages/cs.json
  - apps/web/messages/en.json
  - apps/web/messages/sk.json
autonomous: true

must_haves:
  truths:
    - "Visiting / shows a page wrapped in marketing layout (navbar + footer, no sidebar, no AuthGuard)"
    - "Footer displays company ICO, DIC, and registered address on every marketing page"
    - "Navbar has ScheduleBox logo, Funkce and Ceny links, locale switcher, and Zacit zdarma CTA"
    - "Marketing pages are statically rendered (not dynamic SSR)"
  artifacts:
    - path: "apps/web/app/[locale]/(marketing)/layout.tsx"
      provides: "Marketing route group layout with navbar, footer, cookie consent slot"
      contains: "setRequestLocale"
    - path: "apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx"
      provides: "Marketing navigation bar"
      contains: "getTranslations"
    - path: "apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx"
      provides: "Legal footer with ICO, DIC, address"
      contains: "NEXT_PUBLIC_COMPANY"
  key_links:
    - from: "apps/web/app/[locale]/(marketing)/layout.tsx"
      to: "apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx"
      via: "import MarketingNavbar"
      pattern: "MarketingNavbar"
    - from: "apps/web/app/[locale]/(marketing)/layout.tsx"
      to: "apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx"
      via: "import MarketingFooter"
      pattern: "MarketingFooter"
    - from: "apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx"
      to: "apps/web/messages/cs.json"
      via: "getTranslations with landing.nav namespace"
      pattern: "landing\\.nav"
---

<objective>
Create the (marketing) route group with its layout, navbar, and legal footer — the foundation all other landing page plans build on.

Purpose: Establishes the marketing shell that wraps all public-facing pages (home, pricing, privacy, terms) with a professional navbar and legally-compliant footer. This is the structural prerequisite for all Wave 2 plans.

Output: Working marketing layout with sticky navbar, legal footer, and Czech i18n messages for the entire landing namespace.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-landing-page-czech-legal/25-RESEARCH.md

@apps/web/app/[locale]/(dashboard)/layout.tsx
@apps/web/app/[locale]/(auth)/layout.tsx
@apps/web/app/[locale]/layout.tsx
@apps/web/lib/i18n/routing.ts
@apps/web/lib/i18n/navigation.ts
@apps/web/middleware.ts
@apps/web/components/i18n/locale-switcher.tsx
@apps/web/messages/cs.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Motion and add Czech i18n landing namespace</name>
  <files>
    apps/web/package.json
    apps/web/messages/cs.json
    apps/web/messages/en.json
    apps/web/messages/sk.json
  </files>
  <action>
1. Install the `motion` package (MIT, locked decision — NOT GSAP):
   ```bash
   pnpm --filter @schedulebox/web add motion
   ```

2. Add the full `landing` namespace to `apps/web/messages/cs.json`. Append the entire `"landing"` key with all sub-namespaces (meta, nav, hero, features, trust, pricing, social, cookie) as specified in the research document's "Czech i18n Messages Namespace Structure" section. This includes ALL translations needed by plans 25-01 through 25-04, so subsequent plans only consume existing keys.

   Key namespaces to add:
   - `landing.meta` — SEO titles and descriptions (homeTitle, homeDescription, pricingTitle, privacyTitle, termsTitle)
   - `landing.nav` — navbar items (features, pricing, login, cta)
   - `landing.hero` — hero section (badge, headline, subheadline, cta, ctaSecondary, widgetTitle)
   - `landing.features` — 6 feature cards (title + each feature name and description)
   - `landing.trust` — 4 trust badges (gdpr, hosting, payment, security)
   - `landing.pricing` — pricing page (title, monthly, annual, annualSaving, free/pro/business tiers, cta, currency)
   - `landing.social` — social proof (title, placeholder)
   - `landing.cookie` — cookie consent (message, accept, reject, privacyLink)
   - `landing.privacy` — privacy policy page (title, placeholder)
   - `landing.terms` — terms page (title, placeholder)
   - `landing.footer` — footer section labels (product, legal, privacyLink, termsLink, copyright, registryNote)

3. Add equivalent English translations to `apps/web/messages/en.json` under the same `landing` namespace structure.

4. Add equivalent Slovak translations to `apps/web/messages/sk.json` under the same `landing` namespace structure. Slovak is close to Czech — translate naturally but keep product names like "ScheduleBox" and "Comgate" untranslated.

IMPORTANT: Read the existing cs.json, en.json, and sk.json files first to understand the structure. The `landing` key should be added at the top level alongside existing namespaces (common, auth, dashboard, etc.). Do NOT overwrite existing content.
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/web exec tsc --noEmit` to verify no TypeScript errors. Validate each JSON file is valid JSON with `node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/cs.json'))"` (repeat for en.json and sk.json). Verify motion is in package.json with `grep motion apps/web/package.json`.
  </verify>
  <done>
    motion is installed in apps/web/package.json. All three message files (cs, en, sk) contain a complete `landing` namespace with meta, nav, hero, features, trust, pricing, social, cookie, privacy, terms, and footer sub-namespaces. JSON is valid.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create marketing layout, navbar, and legal footer</name>
  <files>
    apps/web/app/[locale]/(marketing)/layout.tsx
    apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx
    apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx
  </files>
  <action>
1. Create `apps/web/app/[locale]/(marketing)/layout.tsx`:
   - Import `setRequestLocale` from `next-intl/server` and `routing` from `@/lib/i18n/routing`
   - Export `generateStaticParams` returning `routing.locales.map((locale) => ({ locale }))` — REQUIRED for static rendering
   - The layout function is async, receives `{ children, params }` where params is `Promise<{ locale: string }>`
   - Await params, call `setRequestLocale(locale)` as the FIRST operation
   - Render: `<div className="flex min-h-screen flex-col"><MarketingNavbar /><main className="flex-1">{children}</main><MarketingFooter />{/* CookieConsentBanner will be added in plan 25-04 */}</div>`
   - Follow the exact pattern from (dashboard)/layout.tsx and (auth)/layout.tsx for the params typing

2. Create `apps/web/app/[locale]/(marketing)/_components/marketing-navbar.tsx`:
   - This is an async SERVER component (NOT 'use client')
   - Use `getTranslations('landing.nav')` from `next-intl/server` for text
   - Use `Link` from `@/lib/i18n/navigation` for locale-aware links (NOT from `next/link`)
   - Structure: sticky header (`sticky top-0 z-50 border-b bg-white/95 backdrop-blur supports-[backdrop-filter]:bg-white/60`)
   - Content: ScheduleBox text logo on left, nav links (Funkce as `#features` anchor, Ceny as `/pricing`) in center, locale switcher + "Zacit zdarma" CTA button on right
   - Import `LocaleSwitcher` from `@/components/i18n/locale-switcher`
   - Import `Button` from `@/components/ui/button`
   - CTA button links to `/register` (existing auth route)
   - Include a mobile hamburger menu: for simplicity, on `md:hidden` show only the logo and CTA button; hide nav links. Full mobile menu can be enhanced later.

3. Create `apps/web/app/[locale]/(marketing)/_components/marketing-footer.tsx`:
   - This is an async SERVER component
   - Use `getTranslations('landing.footer')` for section labels
   - Use `Link` from `@/lib/i18n/navigation` for locale-aware links
   - LAND-07 compliance: Display ICO, DIC, registered address using environment variables:
     - `process.env.NEXT_PUBLIC_COMPANY_NAME` (default: "ScheduleBox s.r.o.")
     - `process.env.NEXT_PUBLIC_COMPANY_ADDRESS` (default: "Prikladna 1, 110 00 Praha 1")
     - `process.env.NEXT_PUBLIC_COMPANY_ICO` (default: "12345678")
     - `process.env.NEXT_PUBLIC_COMPANY_DIC` (default: "CZ12345678")
   - Use `<address>` HTML element with `not-italic` class for legal info
   - Structure: 4-column grid (Company info with legal details, Produkt column, Pravni column with /privacy and /terms links, Kontakt column)
   - Bottom bar: copyright line with current year, commercial register note
   - Add these env vars to `.env.example` if it exists (placeholder values)

IMPORTANT: Use `Link` from `@/lib/i18n/navigation` (which is `createNavigation(routing)`) for all internal links. This handles locale prefixing automatically with `localePrefix: 'as-needed'`.
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/web exec tsc --noEmit` to verify no TypeScript errors. Create a temporary `apps/web/app/[locale]/(marketing)/page.tsx` with minimal content (`export default function Page() { return <div>test</div> }`) and run `pnpm --filter @schedulebox/web build 2>&1 | head -50` to verify the route group is recognized and builds without errors. Delete the temp page after verification.
  </verify>
  <done>
    Marketing route group exists at `apps/web/app/[locale]/(marketing)/` with layout.tsx rendering navbar and footer. Navbar has logo, nav links, locale switcher, CTA. Footer shows ICO, DIC, address (from env vars with defaults). Layout calls setRequestLocale and exports generateStaticParams. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. The `(marketing)` route group directory exists alongside `(auth)` and `(dashboard)`
2. Layout renders navbar and footer (no sidebar, no AuthGuard)
3. Footer shows ICO, DIC, registered address (defaults visible)
4. Navbar shows ScheduleBox logo, nav links, locale switcher, CTA button
5. All three message files have the complete `landing` namespace
6. `motion` package is in package.json dependencies
7. TypeScript compiles without errors
</verification>

<success_criteria>
- Marketing layout wraps child pages with navbar + footer (no auth, no sidebar)
- Footer displays ICO, DIC, registered address on every marketing page
- Navbar has logo, Funkce/Ceny links, locale switcher, "Zacit zdarma" CTA
- cs.json, en.json, sk.json all have complete `landing` i18n namespace
- `motion` is installed as a dependency
- `setRequestLocale` and `generateStaticParams` are in place for static rendering
</success_criteria>

<output>
After completion, create `.planning/phases/25-landing-page-czech-legal/25-01-SUMMARY.md`
</output>
