---
phase: 04-frontend-shell
plan: 07
type: execute
wave: 3
depends_on: ["04-05"]
files_modified:
  - apps/web/app/(dashboard)/calendar/page.tsx
  - apps/web/components/calendar/calendar-view.tsx
  - apps/web/components/calendar/calendar-toolbar.tsx
  - apps/web/styles/calendar.css
  - apps/web/package.json
autonomous: true

must_haves:
  truths:
    - "Calendar page renders with FullCalendar in resource timeline view"
    - "Calendar shows day, week, and month view toggles"
    - "Calendar displays employee columns as resources"
    - "Calendar shows placeholder/mock events with different colors"
    - "Toast notifications work via sonner (success, error, info, warning)"
  artifacts:
    - path: "apps/web/app/(dashboard)/calendar/page.tsx"
      provides: "Calendar page"
      contains: "CalendarView"
    - path: "apps/web/components/calendar/calendar-view.tsx"
      provides: "FullCalendar wrapper with resource timeline"
      contains: "FullCalendar"
    - path: "apps/web/components/calendar/calendar-toolbar.tsx"
      provides: "Custom toolbar for calendar controls"
      contains: "CalendarToolbar"
    - path: "apps/web/styles/calendar.css"
      provides: "FullCalendar CSS overrides for ScheduleBox theme"
      contains: "fc-"
  key_links:
    - from: "apps/web/components/calendar/calendar-view.tsx"
      to: "apps/web/stores/calendar.store.ts"
      via: "Calendar view state (date, view, filters)"
      pattern: "useCalendarStore"
    - from: "apps/web/components/calendar/calendar-view.tsx"
      to: "@fullcalendar/react"
      via: "FullCalendar React component"
      pattern: "FullCalendar"
    - from: "apps/web/app/(dashboard)/calendar/page.tsx"
      to: "apps/web/components/calendar/calendar-view.tsx"
      via: "CalendarView component render"
      pattern: "CalendarView"
---

<objective>
Build the calendar page with FullCalendar resource timeline view and set up the sonner toast notification system. The calendar shows employee columns with placeholder events in day/week/month views.

Purpose: The calendar is the core UI for managing bookings. In Phase 4 it renders with placeholder events; Phase 5 will connect it to real booking data. The toast system is needed for user feedback across all pages.

Output: Calendar page with FullCalendar, CSS overrides, calendar toolbar, toast system.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-shell/04-01-SUMMARY.md
@.planning/phases/04-frontend-shell/04-02-SUMMARY.md
@.planning/phases/04-frontend-shell/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install FullCalendar and create calendar page with resource timeline</name>
  <files>
    apps/web/package.json
    apps/web/app/(dashboard)/calendar/page.tsx
    apps/web/components/calendar/calendar-view.tsx
    apps/web/components/calendar/calendar-toolbar.tsx
    apps/web/styles/calendar.css
  </files>
  <action>
    1. Install FullCalendar packages:
       ```bash
       cd apps/web
       pnpm add @fullcalendar/react @fullcalendar/core @fullcalendar/daygrid @fullcalendar/timegrid @fullcalendar/resource @fullcalendar/resource-timeline @fullcalendar/interaction
       pnpm add date-fns
       ```

    NOTE: @fullcalendar/resource-timeline is a PREMIUM plugin. The package can be installed but requires a license key for production use. For development, it works without a key (shows watermark). If installation fails or the premium package is not available, FALL BACK to using @fullcalendar/timegrid (free) with a simpler layout where employees are shown as tabs/filters rather than columns. Document which approach was used.

    2. Create `apps/web/styles/calendar.css`:
       - Override FullCalendar CSS to match ScheduleBox theme:
         - .fc toolbar: match header styling, font-family inherit
         - .fc-button: match shadcn/ui button styling (primary color, rounded-md)
         - .fc-button-active: primary color background
         - .fc-event: rounded-md, font-size 0.875rem
         - .fc-timegrid-slot: lighter borders
         - .fc-resource-header: match table header styling
         - .fc-scrollgrid: border-color matching theme border color
         - .fc-today-highlight: subtle primary color background (opacity 5-10%)
       - Keep overrides minimal and specific (don't override everything)

    3. Create `apps/web/components/calendar/calendar-toolbar.tsx` (Client Component):
       - 'use client'
       - Custom toolbar above FullCalendar:
         - Left: prev/next buttons (Button outline), Today button
         - Center: current date range title (formatted with date-fns)
         - Right: Day/Week/Month view toggle buttons (Button group)
       - Employee filter: multi-select or checkbox list of employees (placeholder data for now)
       - Show/hide cancelled toggle
       - Use useCalendarStore for view state, selected date, employee filter
       - Use useTranslations('calendar') for labels

    4. Create `apps/web/components/calendar/calendar-view.tsx` (Client Component):
       - 'use client'
       - Import FullCalendar from @fullcalendar/react
       - Import plugins: resourceTimelinePlugin (or timeGridPlugin as fallback), interactionPlugin, dayGridPlugin
       - Import calendar.css for styling overrides
       - Use useCalendarStore for: view, selectedDate, selectedEmployeeIds, showCancelled
       - MOCK DATA for resources (employees):
         ```
         [
           { id: '1', title: 'Jana Nováková' },
           { id: '2', title: 'Petr Svoboda' },
           { id: '3', title: 'Marie Dvořáková' },
           { id: '4', title: 'Tomáš Černý' },
         ]
         ```
       - MOCK DATA for events (bookings):
         ```
         [
           { id: '1', resourceId: '1', title: 'Střih + Barva', start: today 9:00, end: today 10:30, backgroundColor: '#3B82F6' },
           { id: '2', resourceId: '2', title: 'Pánský střih', start: today 10:00, end: today 10:45, backgroundColor: '#22C55E' },
           { id: '3', resourceId: '1', title: 'Manikúra', start: today 11:00, end: today 12:00, backgroundColor: '#F59E0B' },
           { id: '4', resourceId: '3', title: 'Masáž', start: today 14:00, end: today 15:00, backgroundColor: '#8B5CF6' },
           { id: '5', resourceId: '4', title: 'Pedikúra', start: today 9:30, end: today 10:30, backgroundColor: '#EC4899' },
           { id: '6', resourceId: '2', title: 'Barvení', start: today 13:00, end: today 14:30, backgroundColor: '#3B82F6' },
         ]
         ```
         (Use new Date() to generate today's events dynamically)
       - FullCalendar configuration:
         - plugins: [resourceTimelinePlugin, interactionPlugin] (or fallback)
         - initialView: from calendarStore.view (default: 'resourceTimelineDay')
         - resources: filtered by selectedEmployeeIds (or all if empty)
         - events: mock data
         - headerToolbar: false (we use custom CalendarToolbar)
         - slotDuration: '00:15:00'
         - snapDuration: '00:15:00'
         - slotMinTime: '07:00:00'
         - slotMaxTime: '21:00:00'
         - height: 'auto'
         - locale: 'cs' (Czech locale)
         - selectable: true (for future slot selection)
         - editable: true (for future drag & drop)
         - eventClick: placeholder handler (console.log event for now)
         - select: placeholder handler (console.log selection for now)
       - Sync FullCalendar API with Zustand store: when store changes view/date, call calendarApi.changeView() / calendarApi.gotoDate()
       - Use useRef to get FullCalendar API reference

    5. Create `apps/web/app/(dashboard)/calendar/page.tsx`:
       - Export metadata: { title: 'Calendar' }
       - Renders CalendarToolbar + CalendarView
       - PageHeader with "Calendar" title
  </action>
  <verify>
    - `pnpm type-check --filter @schedulebox/web` passes
    - Calendar page renders FullCalendar with resource timeline (or timegrid fallback)
    - Mock resources (4 employees) show as columns
    - Mock events (6 bookings) show in correct time slots with colors
    - View toggle switches between day/week/month
    - Today button navigates to current date
    - CSS overrides apply ScheduleBox theme to calendar
    - CalendarToolbar shows navigation and view toggle buttons
  </verify>
  <done>
    Calendar page with FullCalendar resource timeline showing mock employees as columns and mock bookings as colored events. Custom toolbar with navigation and view toggles. CSS overrides matching ScheduleBox theme. Calendar state synced with Zustand store.
  </done>
</task>

<task type="auto">
  <name>Task 2: Set up toast notification system and create placeholder pages</name>
  <files>
    apps/web/app/(dashboard)/customers/page.tsx
    apps/web/app/(dashboard)/services/page.tsx
    apps/web/app/(dashboard)/employees/page.tsx
    apps/web/app/(dashboard)/settings/page.tsx
    apps/web/app/(dashboard)/bookings/page.tsx
  </files>
  <action>
    1. Verify sonner is installed (should be from Plan 05 Task 1 when updating root layout). If not:
       ```bash
       cd apps/web && pnpm add sonner
       ```

    2. Verify Toaster is in root layout (added in Plan 05). If not, add it:
       - Import { Toaster } from 'sonner'
       - Add <Toaster position="top-right" richColors closeButton /> before closing </body>

    3. Create placeholder pages for dashboard routes that will be fully implemented in later phases. Each page:
       - Uses PageHeader with translated title
       - Shows EmptyState with appropriate icon and "Coming in Phase X" message (in English for now, since these are placeholder)
       - Ensures navigation links work (clicking sidebar nav item lands on the page)

       Create these placeholder pages:
       - `apps/web/app/(dashboard)/customers/page.tsx`: "Customers" page with Users icon, "Customer management coming soon"
       - `apps/web/app/(dashboard)/services/page.tsx`: "Services" page with Scissors icon, "Service catalog coming soon"
       - `apps/web/app/(dashboard)/employees/page.tsx`: "Employees" page with UserCog icon, "Employee management coming soon"
       - `apps/web/app/(dashboard)/settings/page.tsx`: "Settings" page with Settings icon, "Settings panel coming soon"
       - `apps/web/app/(dashboard)/bookings/page.tsx`: "Bookings" page with BookOpen icon, "Booking list coming soon"

    4. Each placeholder page:
       - Export metadata with title
       - Import PageHeader, EmptyState
       - Import appropriate Lucide icon
       - Use useTranslations('nav') for the page title in PageHeader
  </action>
  <verify>
    - `pnpm type-check --filter @schedulebox/web` passes
    - Sonner Toaster renders in root layout
    - toast.success('Test') shows a green toast (manual test)
    - All placeholder pages load without error
    - Clicking sidebar nav items navigates to correct placeholder page
    - Each placeholder page shows meaningful empty state
  </verify>
  <done>
    Toast notification system operational via sonner. 5 placeholder pages created for customers, services, employees, settings, bookings — ensuring all sidebar navigation links work. Each shows empty state with appropriate icon.
  </done>
</task>

</tasks>

<verification>
1. `pnpm type-check --filter @schedulebox/web` passes
2. Calendar renders with employee columns and colored events
3. Calendar view toggles between day/week/month
4. Calendar CSS matches ScheduleBox theme
5. Toast notifications display correctly (success, error)
6. All sidebar navigation links lead to working pages
7. Placeholder pages show empty states
</verification>

<success_criteria>
- Calendar page fully renders with FullCalendar, mock employees, mock events
- Custom toolbar with prev/next/today and view toggle
- Calendar CSS overrides match ScheduleBox branding
- Calendar state synced with Zustand store
- Sonner toast system operational
- All dashboard routes have pages (no 404s from sidebar nav)
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-shell/04-07-SUMMARY.md`
</output>
