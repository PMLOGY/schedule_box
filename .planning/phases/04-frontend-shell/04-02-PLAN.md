---
phase: 04-frontend-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/api-client.ts
  - apps/web/lib/query-client.ts
  - apps/web/stores/auth.store.ts
  - apps/web/stores/ui.store.ts
  - apps/web/stores/calendar.store.ts
  - apps/web/app/providers.tsx
  - apps/web/package.json
autonomous: true

must_haves:
  truths:
    - "API requests to /api/v1/* include Authorization header automatically when user is logged in"
    - "After logout, auth state is cleared and subsequent API calls do not include a token"
    - "login() throws an error on invalid credentials (401) or network failure, allowing the calling form to display the error"
    - "Sidebar collapse state persists across page refreshes (persisted in localStorage)"
    - "Calendar store resets to defaults on page refresh (not persisted)"
  artifacts:
    - path: "apps/web/stores/auth.store.ts"
      provides: "Auth state with login/logout/setUser"
      contains: "useAuthStore"
    - path: "apps/web/stores/ui.store.ts"
      provides: "UI state with sidebar and modal management"
      contains: "useUIStore"
    - path: "apps/web/stores/calendar.store.ts"
      provides: "Calendar view state (view type, selected date, filters)"
      contains: "useCalendarStore"
    - path: "apps/web/lib/api-client.ts"
      provides: "Fetch wrapper with auth header injection"
      contains: "apiClient"
    - path: "apps/web/app/providers.tsx"
      provides: "Client provider wrapper for TanStack Query"
      contains: "QueryClientProvider"
  key_links:
    - from: "apps/web/lib/api-client.ts"
      to: "apps/web/stores/auth.store.ts"
      via: "getState() for token"
      pattern: "useAuthStore\\.getState\\(\\)"
    - from: "apps/web/app/providers.tsx"
      to: "apps/web/lib/query-client.ts"
      via: "QueryClient configuration"
      pattern: "QueryClientProvider"
---

<objective>
Set up state management (Zustand stores + TanStack Query) and API client so all frontend components have a consistent way to fetch data and manage application state.

Purpose: Every data-fetching component and every stateful interaction (auth, sidebar, calendar) depends on these stores and the API client. This is the "plumbing" that connects UI to API.

Output: 3 Zustand stores, TanStack Query provider, API client with auth interceptor, providers wrapper.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-shell/04-RESEARCH.md
@apps/web/package.json
@apps/web/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install state management dependencies and create Zustand stores</name>
  <files>
    apps/web/package.json
    apps/web/stores/auth.store.ts
    apps/web/stores/ui.store.ts
    apps/web/stores/calendar.store.ts
  </files>
  <action>
    1. Install dependencies in apps/web:
       ```bash
       cd apps/web
       pnpm add @tanstack/react-query zustand
       pnpm add -D @tanstack/react-query-devtools
       ```

    2. Create `apps/web/stores/auth.store.ts`:
       - Interface AuthState: user (User | null), accessToken (string | null), isAuthenticated (boolean)
       - User type: { id: string (UUID), email: string, firstName: string, lastName: string, role: string, companyId: string (UUID), companyName: string }
       - Actions: login(email, password), logout(), setUser(user), setAccessToken(token), refreshToken()
       - Use persist middleware with name 'schedulebox-auth', partialize to only persist user (NOT tokens — tokens go in httpOnly cookies from API, accessToken in memory only)
       - Export useAuthStore

       **login() error handling (CRITICAL):**
       - login() uses apiClient.post('/auth/login', { email, password }) (imported from lib/api-client.ts)
       - login() is an async function that THROWS on error — it does NOT swallow errors
       - On success: stores user + accessToken, sets isAuthenticated=true
       - On failure: the error propagates to the caller (login form catches it)
       - Pattern:
         ```typescript
         login: async (email: string, password: string) => {
           // Throws ApiError on 401, 422, network error — caller must catch
           const response = await apiClient.post<LoginResponse>('/auth/login', { email, password });
           set({ user: response.user, accessToken: response.accessToken, isAuthenticated: true });
         },
         ```
       - The login form in Plan 04-04 wraps the call in try/catch and displays the error message to the user

       **logout():**
       - Calls apiClient.post('/auth/logout') (fire-and-forget, catch errors silently)
       - Clears state: set({ user: null, accessToken: null, isAuthenticated: false })

       **refreshToken():**
       - Calls apiClient.post('/auth/refresh')
       - On success: updates accessToken
       - On failure: calls logout() to clear state

    3. Create `apps/web/stores/ui.store.ts`:
       - Interface UIState: sidebarCollapsed (boolean), sidebarMobileOpen (boolean), activeModal (string | null), modalData (unknown)
       - Actions: toggleSidebar(), setSidebarCollapsed(collapsed), toggleMobileSidebar(), closeMobileSidebar()
       - Actions: openModal(modalId, data?), closeModal()
       - Use persist middleware with name 'schedulebox-ui', partialize to only persist sidebarCollapsed
       - Export useUIStore

    4. Create `apps/web/stores/calendar.store.ts`:
       - Interface CalendarState: view ('resourceTimelineDay' | 'resourceTimelineWeek' | 'dayGridMonth'), selectedDate (Date), selectedEmployeeIds (string[]), showCancelled (boolean)
       - Actions: setView(view), setSelectedDate(date), toggleEmployee(id), setEmployeeFilter(ids), toggleShowCancelled()
       - NO persist middleware (calendar state resets on page refresh)
       - Export useCalendarStore

    IMPORTANT: 'use client' is NOT needed in store files — they are plain TS modules. The 'use client' is only needed in React components that use them.

    NOTE on circular dependency: auth.store.ts imports apiClient from lib/api-client.ts, and api-client.ts imports useAuthStore from stores/auth.store.ts (for getState().accessToken). This is safe because both are lazy — apiClient reads the store at request time (not import time), and auth.store reads apiClient at login time (not import time). Both use getState() which works outside React.
  </action>
  <verify>
    - `pnpm type-check --filter @schedulebox/web` passes
    - auth.store.ts exports useAuthStore with login, logout, setUser, refreshToken
    - ui.store.ts exports useUIStore with toggleSidebar, openModal, closeModal
    - calendar.store.ts exports useCalendarStore with setView, setSelectedDate
  </verify>
  <done>
    Three Zustand stores created: auth (user/tokens/login/logout), UI (sidebar/modal state), calendar (view/date/filters). All type-safe with TypeScript interfaces.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create API client and TanStack Query provider</name>
  <files>
    apps/web/lib/api-client.ts
    apps/web/lib/query-client.ts
    apps/web/app/providers.tsx
  </files>
  <action>
    1. Create `apps/web/lib/api-client.ts`:
       - Class ApiClient with private request<T>(endpoint, options) method
       - Reads accessToken from useAuthStore.getState().accessToken
       - Base URL: process.env.NEXT_PUBLIC_API_URL || '/api/v1'
       - Auto-sets Content-Type: application/json and Authorization: Bearer {token}
       - On 401 response: attempt token refresh via useAuthStore.getState().refreshToken(), retry once, if still 401 call logout()
       - Methods: get<T>(endpoint, params?), post<T>(endpoint, data?), put<T>(endpoint, data?), patch<T>(endpoint, data?), delete<T>(endpoint)
       - For GET with params: construct URLSearchParams and append to URL
       - Error handling: parse error response body, throw structured error with { code, message, details, statusCode }
       - Export singleton: `export const apiClient = new ApiClient();`

    2. Create `apps/web/lib/query-client.ts`:
       - Export function `createQueryClient()` that returns new QueryClient with:
         - defaultOptions.queries.staleTime = 60 * 1000 (1 minute)
         - defaultOptions.queries.retry = 1
         - defaultOptions.queries.refetchOnWindowFocus = false
         - defaultOptions.mutations.onError: toast.error if sonner available (defer, just console.error for now)

    3. Create `apps/web/app/providers.tsx` (Client Component):
       - 'use client' directive
       - Use useState to create QueryClient (prevents sharing across requests)
       - Wrap children in QueryClientProvider
       - Include ReactQueryDevtools (initialIsOpen: false) in development only
       - Export function Providers({ children })

    4. NOTE: Do NOT update app/layout.tsx yet — that happens in Plan 05 when the full app shell layout is created. The providers.tsx is just prepared here.
  </action>
  <verify>
    - `pnpm type-check --filter @schedulebox/web` passes
    - api-client.ts exports apiClient with get, post, put, patch, delete methods
    - query-client.ts exports createQueryClient function
    - providers.tsx exports Providers component with QueryClientProvider
    - providers.tsx creates QueryClient inside useState (not at module level)
  </verify>
  <done>
    API client with auth interceptor and 401 retry, TanStack Query config with sensible defaults, Providers wrapper component ready for integration in app layout. QueryClient created safely inside component state.
  </done>
</task>

</tasks>

<verification>
1. `pnpm type-check --filter @schedulebox/web` passes
2. Zustand stores are importable and have correct TypeScript types
3. apiClient.get('/customers') would produce correct fetch call with auth header
4. providers.tsx wraps children in QueryClientProvider
5. No 'use client' in store files (they're plain modules)
</verification>

<success_criteria>
- 3 Zustand stores: auth, ui, calendar with documented interfaces and actions
- API client class with auth header injection and 401 retry logic
- TanStack Query provider ready to wrap the app
- All code type-checks cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-shell/04-02-SUMMARY.md`
</output>
