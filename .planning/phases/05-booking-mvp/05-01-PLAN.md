---
phase: 05-booking-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/booking.ts
  - packages/shared/src/schemas/availability.ts
  - packages/shared/src/schemas/index.ts
  - packages/shared/src/types/booking.ts
  - packages/shared/src/types/availability.ts
  - packages/shared/src/types/index.ts
autonomous: true

must_haves:
  truths:
    - "Booking create/update schemas validate all required fields per API spec"
    - "Availability request schema validates company_slug, service_id, date range"
    - "TypeScript types exist for Booking, BookingCreate, BookingUpdate, AvailabilitySlot, AvailabilityRequest"
  artifacts:
    - path: "packages/shared/src/schemas/booking.ts"
      provides: "Zod schemas for bookingCreateSchema, bookingUpdateSchema, bookingCancelSchema, bookingRescheduleSchema, bookingListQuerySchema"
      exports: ["bookingCreateSchema", "bookingUpdateSchema", "bookingCancelSchema", "bookingRescheduleSchema", "bookingListQuerySchema"]
    - path: "packages/shared/src/schemas/availability.ts"
      provides: "Zod schema for availability query parameters"
      exports: ["availabilityRequestSchema"]
    - path: "packages/shared/src/types/booking.ts"
      provides: "TypeScript types for booking domain"
      exports: ["Booking", "BookingCreate", "BookingUpdate", "BookingStatus", "BookingSource"]
    - path: "packages/shared/src/types/availability.ts"
      provides: "TypeScript types for availability domain"
      exports: ["AvailabilitySlot", "AvailabilityRequest", "AvailabilityResponse"]
  key_links:
    - from: "packages/shared/src/schemas/booking.ts"
      to: "packages/shared/src/types/booking.ts"
      via: "z.infer type inference"
      pattern: "z\\.infer"
    - from: "packages/shared/src/schemas/index.ts"
      to: "packages/shared/src/schemas/booking.ts"
      via: "re-export"
      pattern: "export.*from.*booking"
---

<objective>
Create shared Zod validation schemas and TypeScript types for the booking and availability domains.

Purpose: All subsequent plans (API routes, frontend forms, calendar) depend on shared types and validation schemas. Creating these first ensures type safety and consistent validation across backend and frontend.

Output: Validated schemas and types in packages/shared that can be imported by both API routes and frontend components.
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-booking-mvp/05-RESEARCH.md

@packages/database/src/schema/bookings.ts
@packages/database/src/schema/services.ts
@packages/database/src/schema/employees.ts
@packages/shared/src/schemas/index.ts
@packages/shared/src/types/index.ts
@schedulebox_complete_documentation.md (lines 4510-4570 for BookingCreate, BookingUpdate, AvailabilitySlot schemas)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Booking and Availability Zod Schemas</name>
  <files>
    packages/shared/src/schemas/booking.ts
    packages/shared/src/schemas/availability.ts
    packages/shared/src/schemas/index.ts
  </files>
  <action>
Create `packages/shared/src/schemas/booking.ts` with these Zod schemas:

1. **bookingCreateSchema** — Per API spec (lines 4532-4544):
   - `customer_id`: z.number().int().positive() (required)
   - `service_id`: z.number().int().positive() (required)
   - `employee_id`: z.number().int().positive().optional()
   - `start_time`: z.string().datetime() (required, ISO 8601)
   - `notes`: z.string().max(1000).optional()
   - `source`: z.enum(['online', 'admin', 'phone', 'walk_in', 'voice_ai', 'marketplace', 'api', 'widget']).default('online')
   - `coupon_code`: z.string().optional()
   - `gift_card_code`: z.string().optional()
   - `resource_ids`: z.array(z.number().int().positive()).optional()

2. **bookingUpdateSchema** — Per API spec (lines 4546-4553):
   - `employee_id`: z.number().int().positive().optional()
   - `start_time`: z.string().datetime().optional()
   - `notes`: z.string().max(1000).optional()
   - `internal_notes`: z.string().max(2000).optional()
   - `status`: z.enum(['pending', 'confirmed', 'cancelled', 'completed', 'no_show']).optional()

3. **bookingCancelSchema**:
   - `reason`: z.string().max(500).optional()

4. **bookingRescheduleSchema**:
   - `start_time`: z.string().datetime() (required)
   - `employee_id`: z.number().int().positive().optional()

5. **bookingListQuerySchema** — Per API spec (lines 2489-2508):
   - `page`: z.coerce.number().int().min(1).default(1)
   - `limit`: z.coerce.number().int().min(1).max(100).default(20)
   - `status`: z.enum(['pending', 'confirmed', 'cancelled', 'completed', 'no_show']).optional()
   - `customer_id`: z.coerce.number().int().positive().optional()
   - `employee_id`: z.coerce.number().int().positive().optional()
   - `service_id`: z.coerce.number().int().positive().optional()
   - `date_from`: z.string().date().optional()
   - `date_to`: z.string().date().optional()
   - `source`: z.enum(['online', 'admin', 'phone', 'walk_in', 'voice_ai', 'marketplace', 'api', 'widget']).optional()

Create `packages/shared/src/schemas/availability.ts`:

6. **availabilityRequestSchema** — Per API spec (lines 2663-2685):
   - `company_slug`: z.string().min(1) (required)
   - `service_id`: z.coerce.number().int().positive() (required)
   - `employee_id`: z.coerce.number().int().positive().optional()
   - `date_from`: z.string().date() (required)
   - `date_to`: z.string().date() (required)
   - Add `.refine()`: date_to must be >= date_from, and date range max 31 days

Update `packages/shared/src/schemas/index.ts` to re-export all schemas from both files.

Use camelCase for schema names with `Schema` suffix per conventions. Export each schema and its inferred type.
  </action>
  <verify>
Run `cd D:/Project/ScheduleBox && pnpm exec tsc --noEmit -p packages/shared/tsconfig.json` to verify TypeScript compiles without errors.
  </verify>
  <done>All 6 Zod schemas exist, compile, and are re-exported from packages/shared/src/schemas/index.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Booking and Availability TypeScript Types</name>
  <files>
    packages/shared/src/types/booking.ts
    packages/shared/src/types/availability.ts
    packages/shared/src/types/index.ts
  </files>
  <action>
Create `packages/shared/src/types/booking.ts` with these TypeScript types:

1. **BookingStatus** — `'pending' | 'confirmed' | 'cancelled' | 'completed' | 'no_show'`
2. **BookingSource** — `'online' | 'admin' | 'phone' | 'walk_in' | 'voice_ai' | 'marketplace' | 'api' | 'widget'`
3. **CancelledBy** — `'customer' | 'employee' | 'admin' | 'system'`
4. **Booking** — Full booking object matching API response (lines 4512-4530):
   - `id: number`
   - `uuid: string`
   - `customer: { id: number; uuid: string; name: string; email: string | null; phone: string | null }`
   - `service: { id: number; uuid: string; name: string; durationMinutes: number; price: string; color: string | null }`
   - `employee: { id: number; uuid: string; name: string; color: string | null } | null`
   - `startTime: string` (ISO 8601)
   - `endTime: string` (ISO 8601)
   - `status: BookingStatus`
   - `source: BookingSource`
   - `price: string`
   - `currency: string`
   - `discountAmount: string`
   - `notes: string | null`
   - `internalNotes: string | null`
   - `noShowProbability: number | null`
   - `cancelledAt: string | null`
   - `cancellationReason: string | null`
   - `cancelledBy: CancelledBy | null`
   - `createdAt: string`
   - `updatedAt: string`
5. **BookingCreate** — Inferred from bookingCreateSchema: `z.infer<typeof bookingCreateSchema>`
6. **BookingUpdate** — Inferred from bookingUpdateSchema: `z.infer<typeof bookingUpdateSchema>`
7. **BookingListQuery** — Inferred from bookingListQuerySchema

Create `packages/shared/src/types/availability.ts`:

8. **AvailabilitySlot** — Per API spec (lines 4561-4569):
   - `date: string` (YYYY-MM-DD)
   - `startTime: string` (HH:mm)
   - `endTime: string` (HH:mm)
   - `employeeId: number`
   - `employeeName: string`
   - `isAvailable: boolean`
9. **AvailabilityRequest** — Inferred from availabilityRequestSchema
10. **AvailabilityResponse** — `{ slots: AvailabilitySlot[] }`

Update `packages/shared/src/types/index.ts` to re-export all types from both files using `export type` syntax.

Import schemas in type files using: `import type { z } from 'zod'` and `import { bookingCreateSchema } from '../schemas/booking'`. Use `export type` for all type exports per project conventions.
  </action>
  <verify>
Run `cd D:/Project/ScheduleBox && pnpm exec tsc --noEmit -p packages/shared/tsconfig.json` to verify all types compile.
  </verify>
  <done>All booking and availability TypeScript types exist, compile, and are re-exported from packages/shared/src/types/index.ts.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit -p packages/shared/tsconfig.json` passes with zero errors
- All schemas are importable: `import { bookingCreateSchema, availabilityRequestSchema } from '@schedulebox/shared'`
- All types are importable: `import type { Booking, AvailabilitySlot } from '@schedulebox/shared'`
</verification>

<success_criteria>
Booking and availability Zod schemas validate correctly per API spec. TypeScript types match database schema and API response format. All exports accessible from @schedulebox/shared barrel files.
</success_criteria>

<output>
After completion, create `.planning/phases/05-booking-mvp/05-01-SUMMARY.md`
</output>
