---
phase: 02-database-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/schema/customers.ts
  - packages/database/src/schema/services.ts
  - packages/database/src/schema/employees.ts
  - packages/database/src/schema/resources.ts
  - packages/database/src/schema/index.ts
autonomous: true

must_haves:
  truths:
    - 'Customer tables include AI-computed fields (health_score, clv_predicted, no_show_count)'
    - 'Services table has dynamic pricing fields (price_min, price_max, dynamic_pricing_enabled)'
    - 'Working hours table supports both company-level and employee-level hours (nullable employee_id)'
    - 'Working hours overrides support day-off entries (is_day_off, nullable start_time/end_time)'
    - 'Service_resources junction table tracks quantity_needed per resource'
    - 'All 12 tables have correct FK references (will reference auth.ts tables)'
  artifacts:
    - path: 'packages/database/src/schema/customers.ts'
      provides: 'Customer schema (3 tables: customers, tags, customer_tags)'
      exports: ['customers', 'tags', 'customerTags']
    - path: 'packages/database/src/schema/services.ts'
      provides: 'Service schema (2 tables: service_categories, services)'
      exports: ['serviceCategories', 'services']
    - path: 'packages/database/src/schema/employees.ts'
      provides: 'Employee schema (4 tables: employees, employee_services, working_hours, working_hours_overrides)'
      exports: ['employees', 'employeeServices', 'workingHours', 'workingHoursOverrides']
    - path: 'packages/database/src/schema/resources.ts'
      provides: 'Resource schema (3 tables: resource_types, resources, service_resources)'
      exports: ['resourceTypes', 'resources', 'serviceResources']
  key_links:
    - from: 'packages/database/src/schema/customers.ts'
      to: 'packages/database/src/schema/auth.ts'
      via: 'FK references to companies and users'
      pattern: 'references.*companies'
    - from: 'packages/database/src/schema/employees.ts'
      to: 'packages/database/src/schema/services.ts'
      via: 'employee_services FK to services'
      pattern: 'references.*services'
---

<objective>
Define the core business entity schemas (12 tables) in Drizzle ORM: customers (3 tables), services (2 tables), employees (4 tables), and resources (3 tables), matching the documentation SQL exactly.

Purpose: These core entity tables are referenced by bookings, payments, and all business feature tables. They form the second layer of the FK dependency tree after auth.
Output: Four schema files with all 12 tables, columns, indexes, and constraints.
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-foundation/02-RESEARCH.md
@schedulebox_complete_documentation.md (lines 1109-1304 — Customers, Services, Employees, Resources SQL)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create customers.ts and services.ts schema files</name>
  <files>
    packages/database/src/schema/customers.ts
    packages/database/src/schema/services.ts
  </files>
  <action>
    **customers.ts** — Import companies and users from './auth.js'.

    **customers table** (lines 1109-1141):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - userId: integer FK to users.id onDelete 'set null'
    - name: varchar(255) notNull
    - email: varchar(255)
    - phone: varchar(50)
    - dateOfBirth: date (use `date` type from drizzle)
    - gender: varchar(10) (CHECK: 'male','female','other' — nullable allowed)
    - notes: text
    - source: varchar(50) default 'manual' (CHECK: 'manual','online','import','marketplace','api')
    - healthScore: smallint (CHECK 0-100)
    - clvPredicted: numeric(10,2)
    - noShowCount: integer default 0
    - totalBookings: integer default 0
    - totalSpent: numeric(12,2) default '0'
    - lastVisitAt: timestamp with timezone
    - marketingConsent: boolean default false
    - preferredContact: varchar(20) default 'email' (CHECK: 'email','sms','phone')
    - preferredReminderMinutes: integer default 1440
    - isActive: boolean default true
    - deletedAt: timestamp('deleted_at', { withTimezone: true }) (nullable, for soft delete — DB-06)
    - createdAt, updatedAt: timestamp with timezone, defaultNow, notNull
    - UNIQUE(email, companyId)
    - Indexes: company, email, phone, user, health (composite company_id + health_score)

    **tags table** (lines 1143-1151):
    - id: serial PK
    - companyId: integer notNull FK to companies.id onDelete cascade
    - name: varchar(100) notNull
    - color: varchar(7) default '#3B82F6'
    - createdAt: timestamp with timezone defaultNow
    - UNIQUE(companyId, name)
    - Index: company

    **customer_tags table** (lines 1153-1157):
    - customerId: integer notNull FK to customers.id onDelete cascade
    - tagId: integer notNull FK to tags.id onDelete cascade
    - Composite PK on (customerId, tagId)

    ---

    **services.ts** — Import companies from './auth.js'.

    **service_categories table** (lines 1163-1173):
    - id: serial PK
    - companyId: integer notNull FK to companies.id onDelete cascade
    - name: varchar(255) notNull
    - description: text
    - sortOrder: integer default 0
    - isActive: boolean default true
    - createdAt: timestamp with timezone defaultNow
    - UNIQUE(companyId, name)
    - Index: company

    **services table** (lines 1175-1210):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - categoryId: integer FK to serviceCategories.id onDelete 'set null'
    - name: varchar(255) notNull
    - description: text
    - durationMinutes: integer notNull (CHECK > 0)
    - bufferBeforeMinutes: integer default 0
    - bufferAfterMinutes: integer default 0
    - price: numeric(10,2) notNull (CHECK >= 0)
    - currency: varchar(3) default 'CZK'
    - dynamicPricingEnabled: boolean default false
    - priceMin: numeric(10,2)
    - priceMax: numeric(10,2)
    - maxCapacity: integer default 1
    - onlineBookingEnabled: boolean default true
    - requiresPayment: boolean default false
    - cancellationPolicyHours: integer default 24
    - isOnline: boolean default false
    - videoProvider: varchar(20) (CHECK: 'zoom','google_meet','ms_teams' — nullable)
    - color: varchar(7) default '#3B82F6'
    - imageUrl: varchar(500)
    - sortOrder: integer default 0
    - isActive: boolean default true
    - deletedAt: timestamp('deleted_at', { withTimezone: true }) (nullable, for soft delete — DB-06)
    - createdAt, updatedAt: timestamp with timezone, defaultNow, notNull
    - Indexes: company, category, active (composite company_id + is_active)
  </action>
  <verify>
    TypeScript compiles without errors.
    customers.ts exports customers, tags, customerTags (3 tables).
    services.ts exports serviceCategories, services (2 tables).
    All FK references resolve correctly.
  </verify>
  <done>5 tables (customers, tags, customer_tags, service_categories, services) defined with all columns, constraints, indexes matching documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Create employees.ts and resources.ts schema files, update barrel export</name>
  <files>
    packages/database/src/schema/employees.ts
    packages/database/src/schema/resources.ts
    packages/database/src/schema/index.ts
  </files>
  <action>
    **employees.ts** — Import companies, users from './auth.js', services from './services.js'.

    **employees table** (lines 1216-1234):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - userId: integer FK to users.id onDelete 'set null'
    - name: varchar(255) notNull
    - email: varchar(255)
    - phone: varchar(50)
    - title: varchar(100)
    - bio: text
    - avatarUrl: varchar(500)
    - color: varchar(7) default '#3B82F6'
    - sortOrder: integer default 0
    - isActive: boolean default true
    - deletedAt: timestamp('deleted_at', { withTimezone: true }) (nullable, for soft delete — DB-06)
    - createdAt, updatedAt: timestamp with timezone, defaultNow, notNull
    - Indexes: company, user

    **employee_services table** (lines 1236-1240):
    - employeeId: integer notNull FK to employees.id onDelete cascade
    - serviceId: integer notNull FK to services.id onDelete cascade
    - Composite PK on (employeeId, serviceId)

    **working_hours table** (lines 1242-1255):
    - id: serial PK
    - companyId: integer notNull FK to companies.id onDelete cascade
    - employeeId: integer FK to employees.id onDelete cascade (nullable — NULL means company default hours)
    - dayOfWeek: smallint notNull (CHECK 0-6, 0=Sunday)
    - startTime: time notNull (use `time` from drizzle-orm/pg-core)
    - endTime: time notNull
    - isActive: boolean default true
    - createdAt: timestamp with timezone defaultNow
    - CHECK(endTime > startTime)
    - Indexes: company, employee

    **working_hours_overrides table** (lines 1257-1269):
    - id: serial PK
    - companyId: integer notNull FK to companies.id onDelete cascade
    - employeeId: integer FK to employees.id onDelete cascade
    - date: date notNull
    - startTime: time (nullable — NULL means full day off)
    - endTime: time (nullable)
    - isDayOff: boolean default false
    - reason: varchar(255)
    - createdAt: timestamp with timezone defaultNow
    - Indexes: company_date (composite), employee_date (composite)

    ---

    **resources.ts** — Import companies from './auth.js', services from './services.js'.

    **resource_types table** (lines 1275-1282):
    - id: serial PK
    - companyId: integer notNull FK to companies.id onDelete cascade
    - name: varchar(100) notNull
    - description: text
    - createdAt: timestamp with timezone defaultNow
    - UNIQUE(companyId, name)

    **resources table** (lines 1284-1297):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - resourceTypeId: integer FK to resourceTypes.id onDelete 'set null'
    - name: varchar(255) notNull
    - description: text
    - quantity: integer default 1 (CHECK > 0)
    - isActive: boolean default true
    - createdAt, updatedAt: timestamp with timezone, defaultNow, notNull
    - Indexes: company, type

    **service_resources table** (lines 1299-1304):
    - serviceId: integer notNull FK to services.id onDelete cascade
    - resourceId: integer notNull FK to resources.id onDelete cascade
    - quantityNeeded: integer default 1
    - Composite PK on (serviceId, resourceId)

    ---

    Update packages/database/src/schema/index.ts to re-export all:
    ```typescript
    export * from './auth.js';
    export * from './customers.js';
    export * from './services.js';
    export * from './employees.js';
    export * from './resources.js';
    ```
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/database type-check` passes.
    employees.ts exports employees, employeeServices, workingHours, workingHoursOverrides (4 tables).
    resources.ts exports resourceTypes, resources, serviceResources (3 tables).
    All cross-file FK references resolve correctly.
    Schema index exports all 12 + 8 = 20 tables total.
  </verify>
  <done>All 12 core entity tables defined across 4 schema files. Employee working hours support company-level and per-employee overrides. Resource quantity tracking implemented. All FK dependencies to auth tables wired correctly.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @schedulebox/database type-check` passes with zero errors
2. customers.ts exports 3 tables (customers, tags, customerTags)
3. services.ts exports 2 tables (serviceCategories, services)
4. employees.ts exports 4 tables (employees, employeeServices, workingHours, workingHoursOverrides)
5. resources.ts exports 3 tables (resourceTypes, resources, serviceResources)
6. All CHECK constraints present (gender, source, video_provider, day_of_week range)
7. All unique constraints present (email+company, company+name combos)
8. Cross-file FK references compile correctly
</verification>

<success_criteria>
- 12 core entity tables defined matching documentation schema exactly
- All columns, types, defaults, constraints, and indexes present
- Cross-file imports between auth and entity schemas work
- TypeScript compilation succeeds across entire package
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-foundation/02-03-SUMMARY.md`
</output>
