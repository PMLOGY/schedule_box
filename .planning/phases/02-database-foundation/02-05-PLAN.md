---
phase: 02-database-foundation
plan: 05
type: execute
wave: 2
depends_on: ['02-02', '02-03']
files_modified:
  - packages/database/src/schema/coupons.ts
  - packages/database/src/schema/gift-cards.ts
  - packages/database/src/schema/loyalty.ts
  - packages/database/src/schema/index.ts
autonomous: true

must_haves:
  truths:
    - 'Coupons support both percentage and fixed discount types with CHECK constraint'
    - 'Coupons have applicable_service_ids as integer array (NULL means all services)'
    - 'Gift cards track balance with CHECK constraint (current_balance >= 0)'
    - 'Gift card transactions record purchase, redemption, and refund with balance_after'
    - 'Loyalty programs support 3 types: points, stamps, tiers'
    - 'Loyalty cards have both points_balance and stamps_balance for dual tracking'
    - 'Rewards support 4 types: discount_percentage, discount_fixed, free_service, gift'
  artifacts:
    - path: 'packages/database/src/schema/coupons.ts'
      provides: 'Coupon schema (2 tables: coupons, coupon_usage)'
      exports: ['coupons', 'couponUsage']
    - path: 'packages/database/src/schema/gift-cards.ts'
      provides: 'Gift card schema (2 tables: gift_cards, gift_card_transactions)'
      exports: ['giftCards', 'giftCardTransactions']
    - path: 'packages/database/src/schema/loyalty.ts'
      provides: 'Loyalty schema (5 tables: loyalty_programs, loyalty_tiers, loyalty_cards, loyalty_transactions, rewards)'
      exports: ['loyaltyPrograms', 'loyaltyTiers', 'loyaltyCards', 'loyaltyTransactions', 'rewards']
  key_links:
    - from: 'packages/database/src/schema/coupons.ts'
      to: 'packages/database/src/schema/bookings.ts'
      via: 'coupon_usage FK to bookings'
      pattern: 'references.*bookings'
    - from: 'packages/database/src/schema/loyalty.ts'
      to: 'packages/database/src/schema/customers.ts'
      via: 'loyalty_cards FK to customers'
      pattern: 'references.*customers'
---

<objective>
Define the business feature schemas (9 tables) in Drizzle ORM: coupons (2 tables), gift cards (2 tables), and loyalty program (5 tables), matching the documentation SQL exactly.

Purpose: These tables power the marketing, promotional, and retention features of ScheduleBox. They reference customers, bookings, and services for discount application and loyalty tracking.
Output: Three schema files with all 9 tables, columns, indexes, and constraints.
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-foundation/02-RESEARCH.md
@schedulebox_complete_documentation.md (lines 1435-1579 — Coupons, Gift Cards, Loyalty SQL)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create coupons.ts and gift-cards.ts schema files</name>
  <files>
    packages/database/src/schema/coupons.ts
    packages/database/src/schema/gift-cards.ts
  </files>
  <action>
    **coupons.ts** — Import companies from './auth.js', customers from './customers.js', bookings from './bookings.js'.

    **coupons table** (lines 1435-1456):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - code: varchar(50) notNull
    - description: varchar(255)
    - discountType: varchar(20) notNull (CHECK: 'percentage','fixed')
    - discountValue: numeric(10,2) notNull (CHECK > 0)
    - minBookingAmount: numeric(10,2) default '0'
    - maxUses: integer (nullable — NULL means unlimited)
    - currentUses: integer default 0
    - maxUsesPerCustomer: integer default 1
    - applicableServiceIds: integer array (use `integer('applicable_service_ids').array()`) — nullable, NULL = all services
    - validFrom: timestamp with timezone defaultNow
    - validUntil: timestamp with timezone (nullable)
    - isActive: boolean default true
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - UNIQUE(companyId, code)
    - Indexes: company, code (composite company+code)

    **coupon_usage table** (lines 1458-1467):
    - id: serial PK
    - couponId: integer notNull FK to coupons.id onDelete cascade
    - customerId: integer notNull FK to customers.id onDelete cascade
    - bookingId: integer notNull FK to bookings.id onDelete cascade
    - discountApplied: numeric(10,2) notNull
    - usedAt: timestamp with timezone defaultNow
    - Indexes: coupon, customer

    ---

    **gift-cards.ts** — Import companies from './auth.js', customers from './customers.js', bookings from './bookings.js'.

    **gift_cards table** (lines 1469-1487):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - code: varchar(50) unique notNull
    - initialBalance: numeric(10,2) notNull (CHECK > 0)
    - currentBalance: numeric(10,2) notNull (CHECK >= 0)
    - currency: varchar(3) default 'CZK'
    - purchasedByCustomerId: integer FK to customers.id onDelete 'set null'
    - recipientEmail: varchar(255)
    - recipientName: varchar(255)
    - message: text
    - validUntil: timestamp with timezone
    - isActive: boolean default true
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - Indexes: company, code

    **gift_card_transactions table** (lines 1489-1498):
    - id: serial PK
    - giftCardId: integer notNull FK to giftCards.id onDelete cascade
    - bookingId: integer FK to bookings.id onDelete 'set null'
    - type: varchar(20) notNull (CHECK: 'purchase','redemption','refund')
    - amount: numeric(10,2) notNull
    - balanceAfter: numeric(10,2) notNull
    - createdAt: timestamp with timezone defaultNow
    - Index: gift_card (idx_gct_gift_card)
  </action>
  <verify>
    TypeScript compiles without errors.
    coupons.ts exports coupons, couponUsage (2 tables).
    gift-cards.ts exports giftCards, giftCardTransactions (2 tables).
    Integer array column on coupons.applicableServiceIds works.
  </verify>
  <done>4 coupon and gift card tables defined with all discount types, balance tracking, usage limits, and CHECK constraints matching documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Create loyalty.ts with 5 loyalty tables, update barrel export</name>
  <files>
    packages/database/src/schema/loyalty.ts
    packages/database/src/schema/index.ts
  </files>
  <action>
    **loyalty.ts** — Import companies from './auth.js', customers from './customers.js', services from './services.js', bookings from './bookings.js'.

    **loyalty_programs table** (lines 1504-1517):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - name: varchar(255) notNull
    - description: text
    - type: varchar(20) notNull (CHECK: 'points','stamps','tiers')
    - pointsPerCurrency: numeric(5,2) default '1'
    - isActive: boolean default true
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - UNIQUE(companyId) — one program per company
    - Index: company

    **loyalty_tiers table** (lines 1519-1529):
    - id: serial PK
    - programId: integer notNull FK to loyaltyPrograms.id onDelete cascade
    - name: varchar(100) notNull
    - minPoints: integer notNull default 0
    - benefits: jsonb default '{}'
    - color: varchar(7) default '#3B82F6'
    - sortOrder: integer default 0
    - createdAt: timestamp with timezone defaultNow
    - Index: program

    **loyalty_cards table** (lines 1531-1549):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - programId: integer notNull FK to loyaltyPrograms.id onDelete cascade
    - customerId: integer notNull FK to customers.id onDelete cascade
    - cardNumber: varchar(50) unique notNull
    - pointsBalance: integer default 0
    - stampsBalance: integer default 0
    - tierId: integer FK to loyaltyTiers.id onDelete 'set null'
    - applePassUrl: varchar(500)
    - googlePassUrl: varchar(500)
    - isActive: boolean default true
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - UNIQUE(programId, customerId)
    - Indexes: program, customer

    **loyalty_transactions table** (lines 1551-1562):
    - id: serial PK
    - cardId: integer notNull FK to loyaltyCards.id onDelete cascade
    - bookingId: integer FK to bookings.id onDelete 'set null'
    - type: varchar(20) notNull (CHECK: 'earn','redeem','expire','adjust','stamp')
    - points: integer notNull
    - balanceAfter: integer notNull
    - description: varchar(255)
    - createdAt: timestamp with timezone defaultNow
    - Indexes: card, booking

    **rewards table** (lines 1564-1579):
    - id: serial PK
    - programId: integer notNull FK to loyaltyPrograms.id onDelete cascade
    - name: varchar(255) notNull
    - description: text
    - pointsCost: integer notNull (CHECK > 0)
    - rewardType: varchar(30) notNull (CHECK: 'discount_percentage','discount_fixed','free_service','gift')
    - rewardValue: numeric(10,2)
    - applicableServiceId: integer FK to services.id onDelete 'set null'
    - maxRedemptions: integer (nullable — NULL means unlimited)
    - currentRedemptions: integer default 0
    - isActive: boolean default true
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - Index: program

    Update packages/database/src/schema/index.ts to add:
    ```typescript
    export * from './coupons.js';
    export * from './gift-cards.js';
    export * from './loyalty.js';
    ```
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/database type-check` passes.
    loyalty.ts exports loyaltyPrograms, loyaltyTiers, loyaltyCards, loyaltyTransactions, rewards (5 tables).
    UNIQUE(companyId) on loyalty_programs enforces one program per company.
    UNIQUE(programId, customerId) on loyalty_cards enforces one card per customer per program.
    Schema index now exports all 34 tables total.
  </verify>
  <done>9 business feature tables defined covering coupons, gift cards, and full loyalty program with tiers, cards, transactions, and rewards. All CHECK constraints, unique constraints, and indexes match documentation.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @schedulebox/database type-check` passes
2. coupons.ts exports 2 tables; gift-cards.ts exports 2 tables; loyalty.ts exports 5 tables
3. Integer array type works for applicableServiceIds on coupons
4. All CHECK constraints present (discount_type, transaction_type, reward_type, loyalty_type)
5. Gift card balance constraints (initial > 0, current >= 0) enforced
6. Loyalty one-per-company and one-card-per-customer constraints present
7. All 9 tables have correct FK references
</verification>

<success_criteria>
- 9 business feature tables defined matching documentation exactly
- Coupon discount types, gift card balance tracking, loyalty points/stamps/tiers all implemented
- All CHECK constraints, unique constraints, indexes present
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-foundation/02-05-SUMMARY.md`
</output>
