---
phase: 02-database-foundation
plan: 09
type: execute
wave: 3
depends_on: ['02-04', '02-05', '02-06']
files_modified:
  - packages/database/src/functions/updated-at.sql
  - packages/database/src/functions/audit-trail.sql
  - packages/database/src/functions/double-booking.sql
  - packages/database/src/functions/customer-metrics.sql
  - packages/database/src/functions/marketplace-rating.sql
  - packages/database/src/functions/coupon-usage.sql
  - packages/database/src/functions/soft-delete.sql
  - packages/database/src/functions/deferred-fks.sql
autonomous: true

must_haves:
  truths:
    - 'updated_at trigger fires on all tables with updated_at column'
    - 'btree_gist extension and exclusion constraint prevent overlapping bookings'
    - 'Advisory lock function exists for booking creation transactions'
    - 'Customer metrics trigger updates totals when bookings change'
    - 'Audit trail trigger captures changes on critical tables'
    - 'Soft delete partial indexes created for 6 tables (deleted_at columns already defined in Drizzle schemas)'
    - 'Deferred FK constraints added for bookings.coupon_id, gift_card_id, video_meeting_id'
  artifacts:
    - path: 'packages/database/src/functions/updated-at.sql'
      provides: 'Auto-update trigger for updated_at columns'
      contains: 'update_updated_at_column'
    - path: 'packages/database/src/functions/double-booking.sql'
      provides: 'Exclusion constraint and extension for double-booking prevention'
      contains: 'btree_gist'
    - path: 'packages/database/src/functions/audit-trail.sql'
      provides: 'Audit trail trigger for critical tables'
      contains: 'audit_log_changes'
    - path: 'packages/database/src/functions/customer-metrics.sql'
      provides: 'Customer metrics auto-update trigger'
      contains: 'update_customer_metrics'
    - path: 'packages/database/src/functions/marketplace-rating.sql'
      provides: 'Marketplace rating auto-update trigger'
      contains: 'update_marketplace_rating'
    - path: 'packages/database/src/functions/coupon-usage.sql'
      provides: 'Coupon usage counter trigger'
      contains: 'increment_coupon_usage'
    - path: 'packages/database/src/functions/soft-delete.sql'
      provides: 'Soft delete partial indexes (columns defined in Drizzle schemas)'
      contains: 'idx_.*_active'
    - path: 'packages/database/src/functions/deferred-fks.sql'
      provides: 'Deferred FK constraints for bookings cross-references'
      contains: 'fk_bookings_coupon'
  key_links:
    - from: 'packages/database/src/functions/double-booking.sql'
      to: 'bookings table'
      via: 'Exclusion constraint on bookings'
      pattern: 'no_overlapping_bookings'
    - from: 'packages/database/src/functions/deferred-fks.sql'
      to: 'coupons, gift_cards, video_meetings tables'
      via: 'FK constraints linking bookings to Wave 2 tables'
      pattern: 'fk_bookings_'
---

<objective>
Create all SQL trigger functions, double-booking prevention constraint, soft delete partial indexes, and deferred FK constraints. These SQL files will be applied via the migration system after table creation.

Purpose: Triggers automate data consistency (updated_at, customer metrics, marketplace ratings, coupon usage, audit trail). The exclusion constraint prevents double-booking at the DB level. Soft delete indexes optimize queries on active records. Deferred FKs complete cross-references between Wave 2 tables.
Output: SQL files in src/functions/ directory ready to be included in migrations.
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-foundation/02-RESEARCH.md
@schedulebox_complete_documentation.md (lines 1918-1989, 2099-2231 — Triggers, Double-Booking, Soft Delete SQL)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create trigger functions (updated_at, audit trail, customer metrics, marketplace rating, coupon usage)</name>
  <files>
    packages/database/src/functions/updated-at.sql
    packages/database/src/functions/audit-trail.sql
    packages/database/src/functions/customer-metrics.sql
    packages/database/src/functions/marketplace-rating.sql
    packages/database/src/functions/coupon-usage.sql
  </files>
  <action>
    Create packages/database/src/functions/ directory.

    **updated-at.sql** (lines 1918-1942):
    - Create update_updated_at_column() trigger function
    - Apply trigger to ALL tables with updated_at column using DO $$ loop

    **audit-trail.sql** (DB-07 requirement):
    - Create audit_log_changes() trigger function that:
      - Captures OLD and NEW values as JSONB
      - Reads company_id, user_id from session variables
      - Inserts into audit_logs table
      - Handles INSERT (old_values NULL), UPDATE (both), DELETE (new_values NULL)
    - Apply trigger to critical tables: bookings, customers, services, employees, payments
    - Use AFTER INSERT OR UPDATE OR DELETE trigger

    **customer-metrics.sql** (lines 1944-1960):
    - Create update_customer_metrics() trigger function matching documentation
    - Apply AFTER INSERT OR UPDATE OR DELETE trigger on bookings

    **marketplace-rating.sql** (lines 1962-1976):
    - Create update_marketplace_rating() trigger function matching documentation
    - Apply AFTER INSERT OR UPDATE OR DELETE trigger on reviews

    **coupon-usage.sql** (lines 1978-1989):
    - Create increment_coupon_usage() trigger function
    - Apply AFTER INSERT trigger on coupon_usage
  </action>
  <verify>
    All 5 SQL files created in packages/database/src/functions/.
    updated-at.sql has trigger function and dynamic application via DO $$ loop.
    audit-trail.sql has trigger function and applied to 5 critical tables.
    customer-metrics.sql, marketplace-rating.sql, coupon-usage.sql each have trigger function and trigger application.
  </verify>
  <done>5 trigger function SQL files created: updated_at auto-trigger for all tables, audit trail on critical tables, customer metrics on booking changes, marketplace rating on review changes, coupon usage counter on usage inserts.</done>
</task>

<task type="auto">
  <name>Task 2: Create double-booking constraint, soft delete indexes, and deferred FK constraints</name>
  <files>
    packages/database/src/functions/double-booking.sql
    packages/database/src/functions/soft-delete.sql
    packages/database/src/functions/deferred-fks.sql
  </files>
  <action>
    **double-booking.sql** (lines 2099-2165):
    - CREATE EXTENSION IF NOT EXISTS btree_gist;
    - Add exclusion constraint on bookings:
      ```sql
      ALTER TABLE bookings ADD CONSTRAINT no_overlapping_bookings
        EXCLUDE USING GIST (
          employee_id WITH =,
          tstzrange(start_time, end_time) WITH &&
        )
        WHERE (status NOT IN ('cancelled'));
      ```
    - NOTE: The WHERE clause with NOT IN may need adjustment for EXCLUDE constraint syntax. Test with: `WHERE (status <> 'cancelled')` if NOT IN doesn't work in exclusion constraint WHERE.

    **soft-delete.sql** (lines 2189-2213):
    - NOTE: deleted_at columns are already defined in the Drizzle schema files (plans 02-03, 02-04, 02-06). Do NOT add ALTER TABLE statements here. Only create the partial indexes.
    - Create partial indexes for active records (these cannot be expressed in Drizzle, so raw SQL is needed):
      - idx_bookings_active ON bookings(company_id, start_time) WHERE deleted_at IS NULL
      - idx_customers_active ON customers(company_id) WHERE deleted_at IS NULL
      - idx_services_active ON services(company_id, is_active) WHERE deleted_at IS NULL
      - idx_employees_active ON employees(company_id) WHERE deleted_at IS NULL
      - idx_payments_active ON payments(company_id) WHERE deleted_at IS NULL
      - idx_reviews_active ON reviews(company_id) WHERE deleted_at IS NULL

    **deferred-fks.sql** (lines 2216-2231):
    - ALTER TABLE bookings ADD CONSTRAINT fk_bookings_coupon FOREIGN KEY (coupon_id) REFERENCES coupons(id) ON DELETE SET NULL
    - ALTER TABLE bookings ADD CONSTRAINT fk_bookings_gift_card FOREIGN KEY (gift_card_id) REFERENCES gift_cards(id) ON DELETE SET NULL
    - ALTER TABLE bookings ADD CONSTRAINT fk_bookings_video_meeting FOREIGN KEY (video_meeting_id) REFERENCES video_meetings(id) ON DELETE SET NULL
  </action>
  <verify>
    double-booking.sql has btree_gist extension and exclusion constraint.
    soft-delete.sql creates 6 partial indexes (no ALTER TABLE for columns).
    deferred-fks.sql adds 3 FK constraints to bookings table.
  </verify>
  <done>Double-booking exclusion constraint with btree_gist, 6 soft delete partial indexes (columns already in Drizzle schemas), and 3 deferred FK constraints for bookings cross-references all created.</done>
</task>

</tasks>

<verification>
1. All 8 SQL files exist in src/functions/ directory
2. btree_gist extension and exclusion constraint prevent overlapping bookings
3. updated_at trigger dynamically applied to all tables with that column
4. Audit trail triggers on bookings, customers, services, employees, payments
5. Customer metrics, marketplace rating, coupon usage triggers present
6. Soft delete partial indexes on 6 tables (no ALTER TABLE — columns in Drizzle schemas)
7. 3 deferred FK constraints on bookings table
</verification>

<success_criteria>
- All trigger functions match documentation SQL
- Double-booking prevention uses exclusion constraint (defense in depth)
- Soft delete partial indexes optimize active-record queries
- Deferred FKs complete the bookings table cross-references to Wave 2 tables
- Audit trail captures changes on critical business tables
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-foundation/02-09-SUMMARY.md`
</output>
