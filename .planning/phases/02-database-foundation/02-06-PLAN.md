---
phase: 02-database-foundation
plan: 06
type: execute
wave: 2
depends_on: ['02-02', '02-03']
files_modified:
  - packages/database/src/schema/notifications.ts
  - packages/database/src/schema/reviews.ts
  - packages/database/src/schema/ai.ts
  - packages/database/src/schema/marketplace.ts
  - packages/database/src/schema/video.ts
  - packages/database/src/schema/apps.ts
  - packages/database/src/schema/automation.ts
  - packages/database/src/schema/analytics.ts
  - packages/database/src/schema/index.ts
autonomous: true

must_haves:
  truths:
    - 'Notification templates have 10 type variants and 3 channel types with UNIQUE(company,type,channel)'
    - 'Notifications have scheduled_at for AI smart reminder timing and status tracking through sent/delivered/opened/clicked'
    - 'Reviews support redirect routing to Google/Facebook/internal'
    - 'AI predictions table is polymorphic (entity_type + entity_id) with 7 prediction types'
    - 'Marketplace listings have geolocation (latitude/longitude) with geo index'
    - 'Automation rules support 11 trigger types and 8 action types'
    - 'Analytics events and audit logs have ip_address as INET type'
    - 'Competitor data tracks 4 data types (pricing, services, reviews, availability)'
  artifacts:
    - path: 'packages/database/src/schema/notifications.ts'
      provides: 'Notification schema (2 tables)'
      exports: ['notificationTemplates', 'notifications']
    - path: 'packages/database/src/schema/reviews.ts'
      provides: 'Review schema (1 table)'
      exports: ['reviews']
    - path: 'packages/database/src/schema/ai.ts'
      provides: 'AI schema (2 tables)'
      exports: ['aiPredictions', 'aiModelMetrics']
    - path: 'packages/database/src/schema/marketplace.ts'
      provides: 'Marketplace schema (1 table)'
      exports: ['marketplaceListings']
    - path: 'packages/database/src/schema/video.ts'
      provides: 'Video schema (1 table)'
      exports: ['videoMeetings']
    - path: 'packages/database/src/schema/apps.ts'
      provides: 'App schema (1 table)'
      exports: ['whitelabelApps']
    - path: 'packages/database/src/schema/automation.ts'
      provides: 'Automation schema (2 tables)'
      exports: ['automationRules', 'automationLogs']
    - path: 'packages/database/src/schema/analytics.ts'
      provides: 'Analytics schema (3 tables)'
      exports: ['analyticsEvents', 'auditLogs', 'competitorData']
  key_links:
    - from: 'packages/database/src/schema/analytics.ts'
      to: 'packages/database/src/schema/auth.ts'
      via: 'FK to companies and users'
      pattern: 'references.*companies'
---

<objective>
Define the remaining platform schemas (12 tables) in Drizzle ORM across 8 schema files: notifications (2), reviews (1), AI (2), marketplace (1), video (1), apps (1), automation (2), and analytics/audit (3), matching the documentation SQL exactly.

Purpose: These tables complete the full 47-table schema. They power notifications, reviews, AI predictions, marketplace, video conferencing, white-label apps, automation, and analytics/audit logging.
Output: Eight schema files with all 12 remaining tables plus barrel export updates.
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-foundation/02-RESEARCH.md
@schedulebox_complete_documentation.md (lines 1585-1873 — Notifications through Analytics SQL)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notifications.ts, reviews.ts, ai.ts, and marketplace.ts schema files</name>
  <files>
    packages/database/src/schema/notifications.ts
    packages/database/src/schema/reviews.ts
    packages/database/src/schema/ai.ts
    packages/database/src/schema/marketplace.ts
  </files>
  <action>
    **notifications.ts** — Import companies from './auth.js', customers from './customers.js', bookings from './bookings.js'.

    **notification_templates table** (lines 1585-1603):
    - id: serial PK
    - companyId: integer notNull FK to companies.id onDelete cascade
    - type: varchar(50) notNull (CHECK: 'booking_confirmation','booking_reminder','booking_cancellation','payment_confirmation','payment_reminder','review_request','welcome','loyalty_update','follow_up','custom')
    - channel: varchar(20) notNull (CHECK: 'email','sms','push')
    - subject: varchar(255)
    - bodyTemplate: text notNull
    - isActive: boolean default true
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - UNIQUE(companyId, type, channel)
    - Index: company

    **notifications table** (lines 1605-1629):
    - id: serial PK
    - companyId: integer notNull FK to companies.id onDelete cascade
    - customerId: integer FK to customers.id onDelete 'set null'
    - bookingId: integer FK to bookings.id onDelete 'set null'
    - templateId: integer FK to notificationTemplates.id onDelete 'set null'
    - channel: varchar(20) notNull (CHECK: 'email','sms','push')
    - recipient: varchar(255) notNull
    - subject: varchar(255)
    - body: text notNull
    - status: varchar(20) default 'pending' (CHECK: 'pending','sent','delivered','failed','opened','clicked')
    - scheduledAt: timestamp with timezone
    - sentAt: timestamp with timezone
    - openedAt: timestamp with timezone
    - clickedAt: timestamp with timezone
    - errorMessage: text
    - metadata: jsonb default '{}'
    - createdAt: timestamp with timezone defaultNow
    - Indexes: company, customer, booking, status, scheduled (partial index WHERE status='pending' — use sql`` for WHERE clause in index)

    ---

    **reviews.ts** — Import companies from './auth.js', customers from './customers.js', bookings from './bookings.js', services from './services.js', employees from './employees.js'.

    **reviews table** (lines 1635-1655):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - customerId: integer notNull FK to customers.id onDelete cascade
    - bookingId: integer FK to bookings.id onDelete 'set null'
    - serviceId: integer FK to services.id onDelete 'set null'
    - employeeId: integer FK to employees.id onDelete 'set null'
    - rating: smallint notNull (CHECK 1-5)
    - comment: text
    - redirectedTo: varchar(50) (CHECK: 'google','facebook','internal' — nullable)
    - isPublished: boolean default true
    - reply: text
    - repliedAt: timestamp with timezone
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - Indexes: company, customer, rating (composite company+rating)

    ---

    **ai.ts** — Import companies from './auth.js'.

    **ai_predictions table** (lines 1661-1678):
    - id: serial PK
    - companyId: integer notNull FK to companies.id onDelete cascade
    - type: varchar(30) notNull (CHECK: 'no_show','clv','demand','churn','upsell','optimal_price','reminder_timing')
    - entityType: varchar(30) notNull (CHECK: 'booking','customer','service','timeslot')
    - entityId: integer notNull
    - score: real notNull
    - confidence: real (CHECK 0-1)
    - details: jsonb default '{}'
    - modelVersion: varchar(50)
    - expiresAt: timestamp with timezone
    - createdAt: timestamp with timezone defaultNow
    - Indexes: company, entity (composite entity_type+entity_id), type (composite company+type)

    **ai_model_metrics table** (lines 1680-1689):
    - id: serial PK
    - modelName: varchar(100) notNull
    - modelVersion: varchar(50) notNull
    - metricName: varchar(50) notNull
    - metricValue: real notNull
    - evaluatedAt: timestamp with timezone defaultNow
    - metadata: jsonb default '{}'
    - Index: model (composite model_name+model_version)

    NOTE: ai_model_metrics does NOT have company_id — it's a global table for ML model tracking.

    ---

    **marketplace.ts** — Import companies from './auth.js'.

    **marketplace_listings table** (lines 1695-1722):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - title: varchar(255) notNull
    - description: text
    - category: varchar(100)
    - subcategory: varchar(100)
    - addressStreet: varchar(255)
    - addressCity: varchar(100)
    - addressZip: varchar(20)
    - latitude: numeric(10,7)
    - longitude: numeric(10,7)
    - images: text array notNull default sql`'{}'::text[]`
    - averageRating: numeric(3,2) default '0'
    - reviewCount: integer default 0
    - priceRange: varchar(10) (CHECK: '$','$$','$$$','$$$$' — nullable)
    - featured: boolean default false
    - verified: boolean default false
    - isActive: boolean default true
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - UNIQUE(companyId) — one listing per company
    - Indexes: category, city, rating (DESC), geo (composite latitude+longitude)
  </action>
  <verify>
    TypeScript compiles without errors.
    notifications.ts exports notificationTemplates, notifications.
    reviews.ts exports reviews.
    ai.ts exports aiPredictions, aiModelMetrics.
    marketplace.ts exports marketplaceListings.
    All CHECK constraints and indexes match documentation.
  </verify>
  <done>6 tables defined across 4 schema files covering notifications, reviews, AI predictions, and marketplace listings with all columns, constraints, and indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Create video.ts, apps.ts, automation.ts, analytics.ts, and update barrel export</name>
  <files>
    packages/database/src/schema/video.ts
    packages/database/src/schema/apps.ts
    packages/database/src/schema/automation.ts
    packages/database/src/schema/analytics.ts
    packages/database/src/schema/index.ts
  </files>
  <action>
    **video.ts** — Import companies from './auth.js', bookings from './bookings.js'.

    **video_meetings table** (lines 1728-1746):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - bookingId: integer notNull FK to bookings.id onDelete cascade
    - provider: varchar(20) notNull (CHECK: 'zoom','google_meet','ms_teams')
    - meetingUrl: varchar(500) notNull
    - meetingId: varchar(255)
    - hostUrl: varchar(500)
    - password: varchar(50)
    - startTime: timestamp with timezone notNull
    - durationMinutes: integer notNull
    - status: varchar(20) default 'scheduled' (CHECK: 'scheduled','started','ended','cancelled')
    - providerResponse: jsonb
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - Indexes: company, booking

    ---

    **apps.ts** — Import companies from './auth.js'.

    **whitelabel_apps table** (lines 1752-1771):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - appName: varchar(100) notNull
    - bundleId: varchar(255)
    - logoUrl: varchar(500)
    - primaryColor: varchar(7) default '#3B82F6'
    - secondaryColor: varchar(7) default '#1E40AF'
    - features: jsonb default '{"booking": true, "loyalty": true, "push": true}'
    - iosStatus: varchar(20) default 'draft' (CHECK: 'draft','building','submitted','published','rejected')
    - androidStatus: varchar(20) default 'draft' (CHECK same as ios)
    - iosAppStoreUrl: varchar(500)
    - androidPlayStoreUrl: varchar(500)
    - lastBuildAt: timestamp with timezone
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - UNIQUE(companyId) — one app per company
    - Index: company

    ---

    **automation.ts** — Import companies from './auth.js', bookings from './bookings.js', customers from './customers.js'.

    **automation_rules table** (lines 1777-1803):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - name: varchar(255) notNull
    - description: text
    - triggerType: varchar(50) notNull (CHECK: 'booking_created','booking_confirmed','booking_completed','booking_cancelled','booking_no_show','payment_received','customer_created','time_before_booking','time_after_booking','customer_inactive','review_received')
    - triggerConfig: jsonb default '{}'
    - actionType: varchar(50) notNull (CHECK: 'send_email','send_sms','send_push','update_booking_status','add_loyalty_points','create_task','webhook','ai_follow_up')
    - actionConfig: jsonb default '{}'
    - delayMinutes: integer default 0
    - isActive: boolean default true
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - Indexes: company, trigger

    **automation_logs table** (lines 1805-1817):
    - id: serial PK
    - ruleId: integer notNull FK to automationRules.id onDelete cascade
    - bookingId: integer FK to bookings.id onDelete 'set null'
    - customerId: integer FK to customers.id onDelete 'set null'
    - status: varchar(20) default 'pending' (CHECK: 'pending','executed','failed','skipped')
    - result: jsonb default '{}'
    - errorMessage: text
    - executedAt: timestamp with timezone
    - createdAt: timestamp with timezone defaultNow
    - Indexes: rule, status

    ---

    **analytics.ts** — Import companies from './auth.js', users from './auth.js'.

    **analytics_events table** (lines 1823-1838):
    - id: serial PK
    - companyId: integer notNull FK to companies.id onDelete cascade
    - eventType: varchar(50) notNull
    - entityType: varchar(30)
    - entityId: integer
    - userId: integer FK to users.id onDelete 'set null'
    - properties: jsonb default '{}'
    - ipAddress: use `inet('ip_address')` from drizzle-orm/pg-core (if inet not available, use varchar(45))
    - userAgent: text
    - sessionId: varchar(100)
    - createdAt: timestamp with timezone defaultNow
    - Indexes: company, type, created (composite company+created_at)

    NOTE for ipAddress: Drizzle ORM may not have `inet` type natively. Use `customType` or `sql` column if needed, or fallback to `varchar(45)` with a comment noting INET in PostgreSQL. Check drizzle docs — recent versions support `inet()` from pg-core.

    **audit_logs table** (lines 1840-1857):
    - id: serial PK
    - companyId: integer FK to companies.id onDelete 'set null' (note: nullable — audit logs should survive company deletion)
    - userId: integer FK to users.id onDelete 'set null'
    - action: varchar(100) notNull
    - entityType: varchar(50) notNull
    - entityId: integer
    - oldValues: jsonb
    - newValues: jsonb
    - ipAddress: same as analytics_events (inet or varchar(45))
    - userAgent: text
    - createdAt: timestamp with timezone defaultNow
    - Indexes: company, user, action, entity (composite entity_type+entity_id), created

    **competitor_data table** (lines 1863-1873):
    - id: serial PK
    - companyId: integer notNull FK to companies.id onDelete cascade
    - competitorName: varchar(255) notNull
    - competitorUrl: varchar(500)
    - dataType: varchar(50) notNull (CHECK: 'pricing','services','reviews','availability')
    - data: jsonb notNull
    - scrapedAt: timestamp with timezone defaultNow
    - Indexes: company, type

    ---

    Update packages/database/src/schema/index.ts to add ALL new exports:
    ```typescript
    export * from './notifications.js';
    export * from './reviews.js';
    export * from './ai.js';
    export * from './marketplace.js';
    export * from './video.js';
    export * from './apps.js';
    export * from './automation.js';
    export * from './analytics.js';
    ```
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/database type-check` passes.
    video.ts exports videoMeetings. apps.ts exports whitelabelApps.
    automation.ts exports automationRules, automationLogs. analytics.ts exports analyticsEvents, auditLogs, competitorData.
    Schema index now exports ALL 46 tables (47 table constants — remember roles/permissions don't have company_id).
    Total count: 8 + 12 + 5 + 9 + 12 = 46 tables + role_permissions = 47 total table definitions.
  </verify>
  <done>All 12 remaining platform tables defined. Notifications with smart timing, reviews with redirect routing, AI predictions polymorphic, marketplace with geo, video meetings, white-label apps, automation with 11 triggers and 8 actions, analytics with INET support, audit logs, competitor data. Full 47-table schema complete.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @schedulebox/database type-check` passes
2. All 8 new schema files export correct table constants
3. Schema barrel file re-exports from all 13 schema files
4. Total of 47 table definitions across all schema files
5. All CHECK constraints present for all enum-like columns
6. INET type or fallback for IP address columns
7. Partial index on notifications (scheduled_at WHERE status='pending')
8. All indexes match documentation names and columns
</verification>

<success_criteria>
- All 12 remaining tables defined matching documentation exactly
- Complete 47-table schema across 13 schema files
- All columns, types, defaults, constraints, and indexes present
- TypeScript compilation succeeds for entire package
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-foundation/02-06-SUMMARY.md`
</output>
