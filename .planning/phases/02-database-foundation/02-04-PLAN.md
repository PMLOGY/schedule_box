---
phase: 02-database-foundation
plan: 04
type: execute
wave: 2
depends_on: ['02-02', '02-03']
files_modified:
  - packages/database/src/schema/bookings.ts
  - packages/database/src/schema/payments.ts
  - packages/database/src/schema/index.ts
autonomous: true

must_haves:
  truths:
    - 'Bookings table has all pricing snapshot fields (price, currency, discount_amount, coupon_id, gift_card_id)'
    - 'Bookings table has AI field (no_show_probability) and cancellation tracking fields'
    - 'Booking sources include all 8 types: online, admin, phone, walk_in, voice_ai, marketplace, api, widget'
    - 'Payments table tracks gateway transactions with idempotency fields'
    - 'Invoices table has company-scoped unique invoice_number'
    - 'Availability slots support precomputed slot lookup by company+date and employee+date'
  artifacts:
    - path: 'packages/database/src/schema/bookings.ts'
      provides: 'Booking schema (3 tables: bookings, booking_resources, availability_slots)'
      exports: ['bookings', 'bookingResources', 'availabilitySlots']
    - path: 'packages/database/src/schema/payments.ts'
      provides: 'Payment schema (2 tables: payments, invoices)'
      exports: ['payments', 'invoices']
  key_links:
    - from: 'packages/database/src/schema/bookings.ts'
      to: 'packages/database/src/schema/auth.ts'
      via: 'FK to companies'
      pattern: 'references.*companies'
    - from: 'packages/database/src/schema/bookings.ts'
      to: 'packages/database/src/schema/customers.ts'
      via: 'FK to customers'
      pattern: 'references.*customers'
    - from: 'packages/database/src/schema/bookings.ts'
      to: 'packages/database/src/schema/employees.ts'
      via: 'FK to employees'
      pattern: 'references.*employees'
    - from: 'packages/database/src/schema/payments.ts'
      to: 'packages/database/src/schema/bookings.ts'
      via: 'FK to bookings'
      pattern: 'references.*bookings'
---

<objective>
Define the booking and payment schema groups (5 tables) in Drizzle ORM: bookings, booking_resources, availability_slots, payments, and invoices, matching the documentation SQL exactly.

Purpose: Bookings are the core transactional entity of ScheduleBox. They reference customers, services, employees, and resources. Payments reference bookings. These tables form the heart of the booking workflow.
Output: Two schema files with all 5 tables, columns, indexes, and constraints.
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-foundation/02-RESEARCH.md
@.planning/phases/02-database-foundation/02-02-SUMMARY.md
@.planning/phases/02-database-foundation/02-03-SUMMARY.md
@schedulebox_complete_documentation.md (lines 1310-1430 — Bookings and Payments SQL)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bookings.ts with bookings, booking_resources, and availability_slots tables</name>
  <files>packages/database/src/schema/bookings.ts</files>
  <action>
    Import companies from './auth.js', customers from './customers.js', services from './services.js', employees from './employees.js', resources from './resources.js'.

    **bookings table** (lines 1310-1352):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - customerId: integer notNull FK to customers.id onDelete restrict
    - serviceId: integer notNull FK to services.id onDelete restrict
    - employeeId: integer FK to employees.id onDelete 'set null'
    - startTime: timestamp with timezone notNull
    - endTime: timestamp with timezone notNull
    - status: varchar(20) default 'pending' (CHECK: 'pending','confirmed','cancelled','completed','no_show')
    - source: varchar(30) default 'online' (CHECK: 'online','admin','phone','walk_in','voice_ai','marketplace','api','widget')
    - notes: text
    - internalNotes: text
    - price: numeric(10,2) notNull
    - currency: varchar(3) default 'CZK'
    - discountAmount: numeric(10,2) default '0'
    - couponId: integer (FK added later in migration — references coupons which is defined in plan 05)
    - giftCardId: integer (FK added later in migration — references gift_cards which is defined in plan 05)
    - videoMeetingId: integer (FK added later — references video_meetings defined in plan 06)
    - noShowProbability: real
    - cancelledAt: timestamp with timezone
    - cancellationReason: text
    - cancelledBy: varchar(20) (CHECK: 'customer','employee','admin','system' — nullable)
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - CHECK(endTime > startTime) via check() constraint

    NOTE: couponId, giftCardId, and videoMeetingId columns are created WITHOUT FK references in the Drizzle schema (just integer columns). The FK constraints will be added via raw SQL in the migration/functions plan (02-07) after all tables exist. This avoids circular dependency issues in schema definition.

    Indexes (7 total):
    - idx_bookings_company: companyId
    - idx_bookings_customer: customerId
    - idx_bookings_service: serviceId
    - idx_bookings_employee: employeeId
    - idx_bookings_start: (companyId, startTime)
    - idx_bookings_status: (companyId, status)
    - idx_bookings_date_range: (companyId, startTime, endTime)

    **booking_resources table** (lines 1354-1362):
    - id: serial PK
    - bookingId: integer notNull FK to bookings.id onDelete cascade
    - resourceId: integer notNull FK to resources.id onDelete restrict
    - quantity: integer default 1
    - UNIQUE(bookingId, resourceId)
    - Indexes: booking, resource

    **availability_slots table** (lines 1364-1376):
    - id: serial PK
    - companyId: integer notNull FK to companies.id onDelete cascade
    - employeeId: integer FK to employees.id onDelete cascade
    - date: date notNull
    - startTime: time notNull
    - endTime: time notNull
    - isAvailable: boolean default true
    - createdAt: timestamp with timezone defaultNow
    - Indexes: company_date (composite), employee_date (composite)
  </action>
  <verify>
    TypeScript compiles without errors.
    bookings.ts exports bookings, bookingResources, availabilitySlots.
    All CHECK constraints present for status, source, cancelled_by.
    All 7 booking indexes created.
  </verify>
  <done>3 booking tables defined with all columns, indexes, CHECK constraints. Cross-table FK columns for coupon/gift_card/video are plain integer columns (FK constraints deferred to migration).</done>
</task>

<task type="auto">
  <name>Task 2: Create payments.ts with payments and invoices tables, update barrel export</name>
  <files>
    packages/database/src/schema/payments.ts
    packages/database/src/schema/index.ts
  </files>
  <action>
    Import companies from './auth.js', bookings from './bookings.js', customers from './customers.js'.

    **payments table** (lines 1382-1409):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - bookingId: integer notNull FK to bookings.id onDelete restrict
    - customerId: integer notNull FK to customers.id onDelete restrict
    - amount: numeric(10,2) notNull (CHECK > 0)
    - currency: varchar(3) default 'CZK'
    - status: varchar(20) default 'pending' (CHECK: 'pending','paid','failed','refunded','partially_refunded')
    - gateway: varchar(20) notNull (CHECK: 'comgate','qrcomat','cash','bank_transfer','gift_card')
    - gatewayTransactionId: varchar(255)
    - gatewayResponse: jsonb
    - refundAmount: numeric(10,2) default '0'
    - refundReason: text
    - paidAt: timestamp with timezone
    - refundedAt: timestamp with timezone
    - createdAt, updatedAt: timestamp with timezone defaultNow notNull
    - Indexes: company, booking, customer, status (company+status), gateway_tx (gateway+gatewayTransactionId)

    **invoices table** (lines 1411-1429):
    - id: serial PK
    - uuid: uuid defaultRandom unique notNull
    - companyId: integer notNull FK to companies.id onDelete cascade
    - paymentId: integer notNull FK to payments.id onDelete restrict
    - customerId: integer notNull FK to customers.id onDelete restrict
    - invoiceNumber: varchar(50) notNull
    - amount: numeric(10,2) notNull
    - taxAmount: numeric(10,2) default '0'
    - currency: varchar(3) default 'CZK'
    - status: varchar(20) default 'issued' (CHECK: 'draft','issued','paid','cancelled')
    - issuedAt: date notNull default sql`CURRENT_DATE`
    - dueAt: date
    - pdfUrl: varchar(500)
    - createdAt: timestamp with timezone defaultNow
    - UNIQUE(companyId, invoiceNumber)
    - Indexes: company, customer

    Update packages/database/src/schema/index.ts to add:
    ```typescript
    export * from './bookings.js';
    export * from './payments.js';
    ```
  </action>
  <verify>
    Run `pnpm --filter @schedulebox/database type-check` passes.
    payments.ts exports payments, invoices.
    All FK references to bookings, customers, companies resolve.
    All CHECK constraints and indexes match documentation.
    Schema index now exports all 25 tables (8 auth + 12 entities + 5 bookings/payments).
  </verify>
  <done>5 booking/payment tables defined with all columns, constraints, indexes. Payment gateway tracking with idempotency fields. Invoice unique numbering per company. All cross-file FK references resolve correctly.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @schedulebox/database type-check` passes
2. bookings.ts exports 3 tables; payments.ts exports 2 tables
3. All CHECK constraints present (booking status, source, payment status, gateway, invoice status)
4. Booking table has 7 indexes matching documentation
5. Payment gateway_tx composite index exists
6. Invoice company+number unique constraint exists
7. Cross-schema FK imports compile correctly
</verification>

<success_criteria>
- 5 booking and payment tables defined matching documentation exactly
- All pricing fields, AI fields, cancellation tracking present
- Deferred FK columns (coupon_id, gift_card_id, video_meeting_id) created as plain integers
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-foundation/02-04-SUMMARY.md`
</output>
