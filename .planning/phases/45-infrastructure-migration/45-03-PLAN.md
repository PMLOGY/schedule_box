---
phase: 45-infrastructure-migration
plan: 03
type: execute
wave: 2
depends_on: [45-01, 45-02]
files_modified:
  - apps/web/next.config.mjs
  - apps/web/app/api/readiness/route.ts
  - .env.example
  - services/notification-worker
autonomous: false
requirements: [INFRA-04]
must_haves:
  truths:
    - "next.config.mjs has ioredis removed from serverExternalPackages"
    - "next.config.mjs has output: standalone removed for Vercel-native deployment"
    - "Readiness route checks UPSTASH_REDIS_REST_URL instead of REDIS_URL"
    - "Readiness route has no RabbitMQ check"
    - ".env.example documents UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, DATABASE_URL_UNPOOLED"
    - ".env.example removes RABBITMQ_URL and related vars"
    - "notification-worker service directory is removed"
    - "pnpm build completes with zero errors"
    - "User has confirmed Vercel deployment is live and returning 200 at schedulebox.vercel.app"
  artifacts:
    - path: "apps/web/next.config.mjs"
      provides: "Vercel-native Next.js config without standalone output"
    - path: "apps/web/app/api/readiness/route.ts"
      provides: "Health check for Neon + Upstash without RabbitMQ"
    - path: ".env.example"
      provides: "Complete environment variable documentation for Vercel deployment"
  key_links:
    - from: "apps/web/next.config.mjs"
      to: "Vercel build system"
      via: "Next.js config consumed by Vercel during deployment"
    - from: "apps/web/app/api/readiness/route.ts"
      to: "Upstash Redis + Neon PostgreSQL"
      via: "env var checks for new infrastructure"
    - from: ".env.example"
      to: "Vercel dashboard env vars"
      via: "documentation of required variables"
---

<objective>
Configure Vercel deployment, update readiness route, clean up env vars, and remove dead services

Purpose: Finalize the infrastructure migration by updating all configuration and health-check files to reflect the new Neon + Upstash stack, remove the dead notification-worker service, and prepare for Vercel deployment.
Output: Vercel-ready config, updated health checks, clean .env.example, removed dead service
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-infrastructure-migration/45-RESEARCH.md

@apps/web/next.config.mjs
@apps/web/app/api/readiness/route.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vercel config, readiness route, env cleanup, and service removal (INFRA-04)</name>
  <files>apps/web/next.config.mjs, apps/web/app/api/readiness/route.ts, .env.example, services/notification-worker</files>
  <action>
1. **Update apps/web/next.config.mjs:**
   - Remove `output: 'standalone'` — Vercel handles Next.js deployment natively; standalone is for Docker/self-hosted only and adds unnecessary bundling overhead on Vercel
   - Remove `'ioredis'` from `serverExternalPackages` array (package removed in Plan 02)
   - Add `'@neondatabase/serverless'` to `serverExternalPackages` to prevent edge bundling issues
   - Keep all other entries (pdfkit, argon2, jsonwebtoken, etc.)

   The serverExternalPackages array should become:
   ```javascript
   serverExternalPackages: [
     'pdfkit',
     '@react-pdf/renderer',
     'argon2',
     'jsonwebtoken',
     'otplib',
     'opossum',
     'handlebars',
     'google-auth-library',
     'passkit-generator',
     'drizzle-orm',
     '@schedulebox/database',
     '@neondatabase/serverless',
     '@opentelemetry/sdk-node',
     '@opentelemetry/api',
     '@opentelemetry/instrumentation',
     'require-in-the-middle',
   ],
   ```

2. **Update apps/web/app/api/readiness/route.ts:**
   - Change the Redis check from `REDIS_URL` to check both `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN`
   - Remove the entire RabbitMQ check block (lines 43-51) — RabbitMQ is gone
   - Update the route JSDoc comment to remove "RabbitMQ" mention — now checks "PostgreSQL and Redis (Upstash)"
   - Update "Used by Kubernetes readiness probes" to "Used by Vercel health checks"
   - Optionally add an actual DB ping: `SELECT 1` via the db export from @schedulebox/database — wrap in try/catch so the readiness route still works even if the DB is temporarily unreachable

3. **Update .env.example:**
   - Add new section "# Upstash Redis (serverless HTTP)" with:
     ```
     UPSTASH_REDIS_REST_URL=https://your-instance.upstash.io
     UPSTASH_REDIS_REST_TOKEN=your-upstash-token
     ```
   - Add `DATABASE_URL_UNPOOLED` below DATABASE_URL with comment:
     ```
     # Neon direct connection for migrations (no pooler)
     DATABASE_URL_UNPOOLED=postgresql://user:pass@ep-xyz.us-east-2.aws.neon.tech/schedulebox
     ```
   - Remove the entire RabbitMQ section (RABBITMQ_URL, RABBITMQ_HOST, RABBITMQ_PORT, RABBITMQ_MANAGEMENT_PORT, RABBITMQ_USER, RABBITMQ_PASSWORD)
   - Remove `REDIS_URL`, `REDIS_HOST`, `REDIS_PORT` — replaced by Upstash vars
   - Update the DATABASE_URL comment to note "Neon pooled connection for runtime queries"
   - Keep all other sections (Payment, Notifications, AI, Platform) unchanged

4. **Remove services/notification-worker/ directory entirely:**
   ```bash
   rm -rf services/notification-worker/
   ```
   Rationale: The notification-worker consumed RabbitMQ queues. Without RabbitMQ there is nothing to consume. Notifications are already sent inline via API routes (v2.0 change). Phase 47 will implement proper email/SMS delivery.

5. **Run final build verification:**
   ```bash
   pnpm build
   ```
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && pnpm build 2>&1 | tail -10 && echo "---" && ! grep -q "standalone" apps/web/next.config.mjs && echo "standalone removed OK" && ! grep -q "ioredis" apps/web/next.config.mjs && echo "ioredis removed OK" && grep "UPSTASH" apps/web/app/api/readiness/route.ts && ls services/notification-worker 2>&1 | grep -q "No such file" && echo "notification-worker removed OK"</automated>
  </verify>
  <done>next.config.mjs is Vercel-native (no standalone, no ioredis). Readiness route checks Upstash env vars, no RabbitMQ. .env.example updated with Upstash/Neon vars, RabbitMQ removed. notification-worker deleted. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete infrastructure migration</name>
  <files>none</files>
  <action>
  User verifies the complete infrastructure migration works end-to-end locally before Vercel deployment.
  </action>
  <what-built>Complete infrastructure migration: RabbitMQ removed (no-op), PostgreSQL driver swapped to Neon serverless, Redis swapped to Upstash HTTP, Next.js config updated for Vercel, billing bug fixed, dead services removed</what-built>
  <how-to-verify>
    1. Run `pnpm build` — should complete with zero errors
    2. Run `pnpm dev` — app should start without RabbitMQ, Redis, or connection errors
    3. Visit http://localhost:3000 (or 3001) — app loads normally with local PostgreSQL
    4. Visit Settings > Billing — AI-Powered plan should show "Unlimited" bookings (not "0")
    5. Check `grep -r "amqplib" packages/` — should return nothing
    6. Check `grep -r "ioredis" apps/web/package.json` — should return nothing
    7. Verify .env.example has Upstash vars and no RabbitMQ vars
    8. Verify `services/notification-worker/` directory no longer exists

    NOTE: Vercel deployment (INFRA-04 live verification) requires pushing to GitHub and configuring env vars in Vercel dashboard. This checkpoint verifies local build correctness. Vercel deployment is a user-action step done outside of code execution.
  </how-to-verify>
  <verify>User confirms all 8 checks pass</verify>
  <done>User has verified the complete infrastructure migration works locally</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` passes with zero errors
- No references to amqplib, ioredis, RABBITMQ_URL, or REDIS_URL in production code
- .env.example correctly documents all new Vercel/Neon/Upstash variables
- next.config.mjs has no `output: 'standalone'` and no `ioredis` in serverExternalPackages
- Readiness route checks UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN
- services/notification-worker/ directory does not exist
</verification>

<success_criteria>
- Infrastructure fully migrated: RabbitMQ gone, Neon + Upstash configured, Vercel-ready
- Build passes locally against local PostgreSQL
- All environment variable documentation updated
- Dead notification-worker service removed
- User confirms local verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/45-infrastructure-migration/45-03-SUMMARY.md`
</output>
</output>
