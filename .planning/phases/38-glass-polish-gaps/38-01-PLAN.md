---
phase: 38-glass-polish-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/shared/page-skeleton.tsx
  - apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx
  - apps/web/components/ui/button.tsx
  - apps/web/tailwind.config.ts
autonomous: true
gap_closure: true
requirements:
  - COMP-02
  - POLSH-02

must_haves:
  truths:
    - "PageSkeleton cards/form/detail variants use GlassShimmer instead of base Skeleton for large rectangular placeholders"
    - "PricingTable renders Card with variant='glass' gaining hover:shadow-glass-hover transition"
    - "No orphaned Button glass CVA variants exist with zero consumers"
    - "No orphaned backdropBlur Tailwind extensions exist with zero consumers"
  artifacts:
    - path: "apps/web/components/shared/page-skeleton.tsx"
      provides: "GlassShimmer usage in CardsVariant, FormVariant, DetailVariant"
      contains: "GlassShimmer"
    - path: "apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx"
      provides: "Card variant='glass' usage instead of raw glass-surface className"
      contains: "variant.*glass"
    - path: "apps/web/components/ui/button.tsx"
      provides: "Clean button variants with no dead glass code"
    - path: "apps/web/tailwind.config.ts"
      provides: "Clean config with no orphaned backdropBlur extensions"
  key_links:
    - from: "apps/web/components/shared/page-skeleton.tsx"
      to: "apps/web/components/shared/glass-shimmer.tsx"
      via: "import GlassShimmer"
      pattern: "GlassShimmer"
    - from: "apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx"
      to: "apps/web/components/ui/card.tsx"
      via: "Card variant='glass' prop"
      pattern: "variant.*glass"
---

<objective>
Close four v1.4 audit gaps: extend GlassShimmer to remaining PageSkeleton variants, migrate PricingTable to Card CVA glass variant, remove orphaned Button glass variants, and remove orphaned Tailwind backdropBlur extensions.

Purpose: Eliminate tech debt identified in v1.4-MILESTONE-AUDIT.md so the glass design system has zero orphaned code and consistent usage patterns across all components.
Output: Four cleaned-up source files with no dead glass code and consistent CVA variant usage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.4-MILESTONE-AUDIT.md

@apps/web/components/shared/page-skeleton.tsx
@apps/web/components/shared/glass-shimmer.tsx
@apps/web/components/ui/button.tsx
@apps/web/components/ui/card.tsx
@apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx
@apps/web/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GlassShimmer to all PageSkeleton variants and fix PricingTable CVA</name>
  <files>
    apps/web/components/shared/page-skeleton.tsx
    apps/web/app/[locale]/(marketing)/_components/pricing-table.tsx
  </files>
  <action>
    **PageSkeleton (page-skeleton.tsx):**
    The GlassShimmer import already exists. Replace base `Skeleton` with `GlassShimmer` in three variants:

    1. **CardsVariant** (line 54): Replace `<Skeleton key={i} className="h-[180px] rounded-xl" />` with `<GlassShimmer key={i} className="h-[180px]" />` (GlassShimmer already has rounded-xl built in).

    2. **FormVariant** (lines 67-70): Replace the large field placeholders. Keep the small `<Skeleton className="h-4 w-24" />` label skeletons as-is (they are small inline elements, not glass-context blocks). Replace only the `<Skeleton className="h-10 w-full" />` input placeholders with `<GlassShimmer className="h-10 w-full" />`.

    3. **DetailVariant** (lines 83-90): Replace all five `<Skeleton className="h-[...]  rounded-xl" />` blocks with `<GlassShimmer className="h-[...]" />` (drop rounded-xl since GlassShimmer includes it).

    Do NOT change HeaderSkeleton (small inline text skeletons), TableVariant (opaque data table context), or DashboardVariant (already uses GlassShimmer).

    **PricingTable (pricing-table.tsx):**
    The Card component is already imported. Change the Card rendering (line 113-118) from:
    ```
    <Card className={cn('relative flex flex-col', isFeatured ? 'glass-surface ring-2 ring-primary/70' : 'glass-surface-subtle')} >
    ```
    to:
    ```
    <Card variant="glass" className={cn('relative flex flex-col', isFeatured && 'ring-2 ring-primary/70')} >
    ```
    This replaces the raw `glass-surface`/`glass-surface-subtle` className strings with the Card CVA `variant="glass"` prop, which provides `glass-surface border-glass transition-shadow duration-200 hover:shadow-glass-hover`. The featured tier gets the additional ring. Non-featured tiers now get the standard glass-surface (not glass-surface-subtle) which is acceptable since the Card CVA variant only has one glass level — per Decision 23, the intensity differentiation was via className, but the audit specifically asks us to use the CVA variant for consistency and the hover transition.
  </action>
  <verify>
    Run `npx tsc --noEmit` from apps/web to confirm no type errors.
    Grep for `glass-surface-subtle` in pricing-table.tsx — should return zero matches.
    Grep for `Skeleton` imports in page-skeleton.tsx — should still exist (used in HeaderSkeleton and FormVariant labels) but CardsVariant, FormVariant input fields, and DetailVariant should only use GlassShimmer for large blocks.
  </verify>
  <done>
    CardsVariant renders 6 GlassShimmer blocks. FormVariant renders GlassShimmer for input placeholders and Skeleton for labels. DetailVariant renders 5 GlassShimmer blocks. PricingTable uses Card variant="glass" with no raw glass-surface classNames.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned Button glass variants and Tailwind backdropBlur extensions</name>
  <files>
    apps/web/components/ui/button.tsx
    apps/web/tailwind.config.ts
  </files>
  <action>
    **Button (button.tsx):**
    Remove the two orphaned CVA variants that have zero consumers anywhere in the codebase:
    - Delete `'glass-secondary': 'glass-surface-subtle border-glass text-secondary-foreground hover:bg-glass-subtle/80'` (lines 21-22)
    - Delete `'glass-ghost': 'glass-surface-subtle border-glass hover:bg-glass-subtle/80'` (line 23)

    Keep all other variants (default, destructive, outline, secondary, ghost, link) unchanged.

    **Tailwind config (tailwind.config.ts):**
    Remove the entire `backdropBlur` extension block (lines 64-68):
    ```
    backdropBlur: {
      'glass-sm': '8px',
      'glass-md': '16px',
      'glass-lg': '24px',
    },
    ```
    These generate `backdrop-blur-glass-{sm,md,lg}` utility classes that are never used in any .tsx file. The glass-plugin.ts handles blur directly with hardcoded pixel values per Decision 17/18 (Safari constraint).

    Do NOT touch any other part of the tailwind config or button component.
  </action>
  <verify>
    Run `npx tsc --noEmit` from apps/web to confirm no type errors.
    Grep for `glass-secondary\|glass-ghost` in button.tsx — should return zero matches.
    Grep for `backdropBlur` in tailwind.config.ts — should return zero matches.
    Grep for `glass-secondary\|glass-ghost` across all .tsx files — should return zero matches (confirming nothing depended on them).
  </verify>
  <done>
    Button component has exactly 6 variants (default, destructive, outline, secondary, ghost, link) with no glass-specific variants. Tailwind config has no backdropBlur extension. Zero orphaned glass code remains.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors from apps/web
2. `grep -r "glass-secondary\|glass-ghost" apps/web/` returns zero matches
3. `grep "backdropBlur" apps/web/tailwind.config.ts` returns zero matches
4. `grep "glass-surface-subtle" apps/web/app/\[locale\]/(marketing)/_components/pricing-table.tsx` returns zero matches
5. `grep "GlassShimmer" apps/web/components/shared/page-skeleton.tsx` shows usage in DashboardVariant, CardsVariant, FormVariant, and DetailVariant
6. Dev server (`pnpm dev`) starts without errors
</verification>

<success_criteria>
1. All PageSkeleton variants (cards, form, detail) use GlassShimmer for large block placeholders — POLSH-02 fully satisfied
2. PricingTable uses Card variant="glass" with hover:shadow-glass-hover transition — integration gap closed
3. Zero orphaned Button glass variants remain — COMP-02 resolved (dead code removed)
4. Zero orphaned Tailwind backdropBlur extensions remain — tech debt closed
5. TypeScript compilation passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/38-glass-polish-gaps/38-01-SUMMARY.md`
</output>
