---
phase: 53-deployment-go-live
plan: 03
type: execute
wave: 2
depends_on: [53-01]
files_modified: []
autonomous: false
requirements: [DEP-02, DEP-04]

must_haves:
  truths:
    - "Custom domain resolves to Coolify deployment with valid SSL certificate when domain is provided"
    - "HTTP redirects to HTTPS automatically via Traefik"
    - "Comgate recurring subscription billing works on production with merchant 498621"
    - "Subscription state transitions from trial/free to paid plan on test upgrade"
  artifacts: []
  key_links:
    - from: "Traefik reverse proxy"
      to: "Coolify app container"
      via: "Custom domain DNS + SSL"
      pattern: "HTTPS redirect"
    - from: "Subscription upgrade flow"
      to: "Comgate recurring API"
      via: "Platform Comgate merchant 498621"
      pattern: "comgate.*recurring"

user_setup:
  - service: coolify
    why: "Custom domain DNS configuration"
    env_vars: []
    dashboard_config:
      - task: "Add custom domain to Coolify app resource"
        location: "Coolify UI -> Application -> Settings -> Domains"
      - task: "Point DNS A record to Coolify server IP"
        location: "Domain registrar DNS settings"
  - service: comgate
    why: "Recurring billing activation for merchant 498621"
    env_vars:
      - name: COMGATE_MERCHANT_ID
        source: "Already configured: 498621"
      - name: COMGATE_SECRET
        source: "Comgate merchant dashboard"
    dashboard_config:
      - task: "Activate recurring payments for merchant 498621"
        location: "Contact Comgate support or merchant dashboard"
---

<objective>
Configure custom domain with SSL on Coolify and verify Comgate recurring billing works on production.

Purpose: DEP-02 requires the app accessible on a custom domain with HTTPS. DEP-04 requires Comgate recurring billing verified working on production. Both require human action (domain provision + Comgate activation).
Output: Production app on custom domain with SSL, verified billing
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-deployment-go-live/53-01-SUMMARY.md

@docker-compose.coolify.yml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide on custom domain for production</name>
  <files></files>
  <action>
DEP-02 requires a custom domain with SSL. The app is currently accessible via the Coolify-generated URL. To configure a custom domain, we need:
1. The domain name (e.g., app.schedulebox.cz)
2. DNS access to point an A record to the Coolify server IP
3. Coolify will auto-provision SSL via Let's Encrypt through Traefik

If no domain is ready yet, we can mark DEP-02 as "ready to configure" with documentation and proceed with DEP-04.

Options:
- configure-now: Provide domain name, configure DNS + Coolify now. Full go-live complete.
- defer-domain: Use Coolify URL for now. DEP-02 remains open until domain is configured.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting user decision on domain"</automated>
  </verify>
  <done>User has decided whether to configure a custom domain now or defer. If configuring, domain name is provided.</done>
</task>

<task type="auto">
  <name>Task 2: Configure custom domain in Coolify (if domain provided)</name>
  <files></files>
  <action>
If user provided a domain name:

1. Document the Coolify domain configuration steps for the user:
   - In Coolify UI: Go to the ScheduleBox application -> Settings -> Domains
   - Add the custom domain (e.g., app.schedulebox.cz)
   - Coolify + Traefik will auto-provision a Let's Encrypt SSL certificate
   - Enable "Force HTTPS" redirect in Traefik labels

2. Provide DNS configuration instructions:
   - Add an A record: `{domain}` -> `{coolify-server-ip}`
   - If using a subdomain (app.schedulebox.cz), add a CNAME to the Coolify server hostname
   - TTL: 300 (5 min) for fast propagation during setup

3. After DNS propagates, verify:
   - `curl -I https://{domain}` returns 200 with valid SSL
   - `curl -I http://{domain}` returns 301/302 redirect to HTTPS
   - The homepage loads correctly on the custom domain

If user selected "defer-domain": Skip this task, document that DEP-02 is ready to configure when domain is available.
  </action>
  <verify>
    <automated>echo "Manual verification required — domain configuration depends on user input"</automated>
  </verify>
  <done>Custom domain resolves to Coolify deployment with HTTPS, or DEP-02 documented as ready-to-configure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Comgate recurring billing on production</name>
  <files></files>
  <action>
DEP-04 requires verifying Comgate recurring billing works on production with merchant 498621.

Prerequisites (user must complete):
1. Comgate recurring payments must be activated for merchant 498621 (contact Comgate support if not yet done)
2. COMGATE_MERCHANT_ID and COMGATE_SECRET must be set in Coolify env vars

Verification steps:
1. Go to the production app (Coolify URL or custom domain)
2. Log in as a company owner (use demo@schedulebox.cz / password123 if demo seed was run)
3. Navigate to Settings > Subscription (or the subscription upgrade flow)
4. Initiate a test subscription upgrade to a paid plan
5. Complete the Comgate payment flow
6. Verify:
   - Subscription state changes from trial/free to paid plan in the dashboard
   - The invoice PDF is generated and downloadable
   - The Comgate merchant dashboard shows the recurring payment registered

If Comgate recurring is NOT yet activated:
- Mark DEP-04 as blocked on Comgate activation
- Document the exact steps needed when activation is ready
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting user verification of Comgate billing"</automated>
  </verify>
  <done>Comgate recurring billing verified on production — subscription state transitions from trial/free to paid plan, or DEP-04 documented as blocked on Comgate activation with clear next steps.</done>
</task>

</tasks>

<verification>
- Custom domain (if provided) resolves with HTTPS and valid SSL
- HTTP to HTTPS redirect works
- Comgate recurring billing test completes successfully on production
- Subscription state transitions correctly
</verification>

<success_criteria>
- DEP-02: Custom domain configured with SSL (or documented as ready-to-configure if deferred)
- DEP-04: Comgate recurring billing verified on production (or blocked on Comgate activation with clear next steps)
</success_criteria>

<output>
After completion, create `.planning/phases/53-deployment-go-live/53-03-SUMMARY.md`
</output>
