---
phase: 53-deployment-go-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/seeds/production-demo.ts
  - packages/database/package.json
  - docker-compose.coolify.yml
  - .github/workflows/ci.yml
autonomous: true
requirements: [DEP-01, DEP-03]

must_haves:
  truths:
    - "App is reachable at production Coolify URL with HTTPS and returns 200 on /api/health"
    - "Neon production database contains a seeded demo company with services, employees, and bookings"
    - "Logging in as demo owner on production shows a populated dashboard"
  artifacts:
    - path: "packages/database/src/seeds/production-demo.ts"
      provides: "Idempotent production seed script for demo company"
    - path: "packages/database/package.json"
      provides: "db:seed:demo script entry"
      contains: "db:seed:demo"
  key_links:
    - from: "packages/database/src/seeds/production-demo.ts"
      to: "packages/database/src/schema/index"
      via: "drizzle ORM insert"
      pattern: "db\\.insert"
---

<objective>
Harden the Coolify production deployment with correct environment variables and seed a demo company into the Neon production database.

Purpose: The app is already deployed on Coolify but needs production-grade env vars verified and a demo company seeded so stakeholders can see a populated dashboard on first visit.
Output: Verified production deployment + seeded demo company in Neon
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docker-compose.coolify.yml
@docker/Dockerfile
@packages/database/src/seeds/development.ts
@packages/database/src/seeds/helpers.ts
@packages/database/package.json
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create production demo seed script and verify Coolify env vars</name>
  <files>packages/database/src/seeds/production-demo.ts, packages/database/package.json</files>
  <action>
1. Create `packages/database/src/seeds/production-demo.ts` — a production-safe, idempotent seed script that creates ONE demo company for showcase purposes. Model after `development.ts` but with these differences:
   - Creates exactly 1 company: "Demo Salon Krasa" (Czech beauty salon)
   - Creates 1 owner user: demo@schedulebox.cz / password123 (using DEV_PASSWORD_HASH from helpers.ts)
   - Creates 3 employees with Czech names
   - Creates 5 services (haircut, coloring, manicure, pedicure, massage) with realistic CZK prices (300-1500 CZK)
   - Creates 8-10 sample bookings spread across the next 7 days (mix of confirmed, pending, completed)
   - Creates working hours for Mon-Fri 9:00-18:00, Sat 9:00-13:00
   - MUST be idempotent: check if demo company already exists before inserting (use `SELECT` on company slug "demo-salon-krasa" — skip if exists)
   - Import schema from '../schema/index' and db from '../db' (same pattern as development.ts)
   - Use dotenv config at top (same pattern as development.ts)

2. Add `"db:seed:demo": "tsx src/seeds/production-demo.ts"` to packages/database/package.json scripts section.

3. Verify the docker-compose.coolify.yml has all required env vars by comparing against .env.example. Currently it uses local postgres/redis containers — update the comments to note that for Neon production, the DATABASE_URL should point to Neon pooled connection (the compose file already supports this via env var substitution from Coolify UI). Add these missing env vars to the app service environment section if not present:
   - UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN (the app uses Upstash Redis HTTP, not raw Redis — the current redis:// URL may not work if the app expects Upstash HTTP client)
   - DATABASE_URL_UNPOOLED for migrations (Neon direct connection)
   - APP_VERSION with a default

4. Verify the health endpoint exists at `apps/web/app/api/health/` — it should. If it returns a simple 200, that's sufficient.
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && node -e "const ts = require('fs').readFileSync('packages/database/src/seeds/production-demo.ts','utf8'); const hasIdempotent = ts.includes('demo-salon-krasa') || ts.includes('idempotent'); const hasInsert = ts.includes('insert'); console.log('idempotent:', hasIdempotent, 'insert:', hasInsert); if(!hasIdempotent||!hasInsert) process.exit(1);"</automated>
  </verify>
  <done>Production demo seed script exists, is idempotent, creates 1 demo company with services/employees/bookings. package.json has db:seed:demo script. docker-compose.coolify.yml has all required env vars for Neon+Upstash production.</done>
</task>

<task type="auto">
  <name>Task 2: Run production demo seed against local DB to validate, then document Coolify deployment checklist</name>
  <files>packages/database/src/seeds/production-demo.ts</files>
  <action>
1. Run the production demo seed script against the local development database to verify it works:
   `pnpm --filter @schedulebox/database db:seed:demo`
   - It should complete without errors
   - Run it a SECOND time to verify idempotency (should print "already seeded" and skip)

2. If the seed fails, fix the script. Common issues:
   - Missing schema imports (check what tables are needed: companies, users, employees, services, bookings, working_hours, user_companies, user_roles)
   - Foreign key ordering (roles must exist before user_roles, company before employees/services)
   - The development.ts seed checks for existing roles — the demo seed should check for existing demo company by slug

3. After seed works locally, verify the local app shows the demo company:
   - Start dev server if not running: `pnpm dev` (background)
   - Check health: curl http://localhost:3000/api/health
   - The demo owner (demo@schedulebox.cz) should be able to log in and see the dashboard with data

Note: Actually running the seed against Neon production requires the production DATABASE_URL which is configured in Coolify. The user will run `DATABASE_URL=<neon-url> pnpm --filter @schedulebox/database db:seed:demo` or trigger it via the Coolify migrate container. Document this in the SUMMARY.
  </action>
  <verify>
    <automated>cd D:/Project/ScheduleBox && pnpm --filter @schedulebox/database db:seed:demo 2>&1 | tail -5</automated>
  </verify>
  <done>Demo seed runs successfully against local DB. Running it twice proves idempotency (second run skips). Demo company visible in app with services, employees, and bookings.</done>
</task>

</tasks>

<verification>
- `packages/database/src/seeds/production-demo.ts` exists and exports a working seed
- `pnpm --filter @schedulebox/database db:seed:demo` completes without error
- Running seed twice is idempotent (no duplicate data)
- docker-compose.coolify.yml has all env vars needed for Neon + Upstash production
- Health endpoint returns 200
</verification>

<success_criteria>
- Demo seed script creates exactly 1 company with 3 employees, 5 services, 8+ bookings
- Seed is idempotent (safe to run multiple times)
- docker-compose.coolify.yml updated with all production env vars
- Local validation proves the seed works
</success_criteria>

<output>
After completion, create `.planning/phases/53-deployment-go-live/53-01-SUMMARY.md`
</output>
