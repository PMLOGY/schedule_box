/**
 * Marketing Campaigns Alias
 * Proxies to /api/v1/coupons — marketing campaigns are managed through
 * the coupon system (discount codes, promotions, time-limited offers)
 */
export { GET, POST } from '../../coupons/route';
