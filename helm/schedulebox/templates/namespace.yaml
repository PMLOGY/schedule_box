apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.global.namespace }}
  labels:
    name: {{ .Values.global.namespace }}
    app.kubernetes.io/name: {{ include "schedulebox.name" . }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
